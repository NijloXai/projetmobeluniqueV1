---
phase: 15.1-tests-int-gration-supabase
plan: 04
type: execute
wave: 2
depends_on: [15.1-01]
files_modified:
  - src/__tests__/integration/routes/admin-visuals.integration.test.ts
  - src/__tests__/integration/routes/admin-generate.integration.test.ts
  - src/__tests__/integration/routes/simulate.integration.test.ts
autonomous: true
requirements:
  - D-05
  - D-06
  - D-09
  - D-10
  - D-11

must_haves:
  truths:
    - "PUT /api/admin/visuals/[id]/validate passe is_validated a true"
    - "PUT /api/admin/visuals/[id]/publish passe is_published a true, uniquement si is_validated"
    - "POST /api/admin/visuals/bulk-validate valide plusieurs visuels d'un coup"
    - "POST /api/admin/visuals/bulk-publish publie plusieurs visuels valides d'un coup"
    - "GET /api/admin/visuals/export/[modelId] retourne un ZIP avec les visuels"
    - "POST /api/admin/generate cree un visuel avec le mock IA Sharp"
    - "POST /api/admin/generate-all genere les visuels pour tous les angles"
    - "POST /api/simulate retourne un JPEG binaire avec le mock provider"
    - "POST /api/simulate retourne 429 apres 5 appels rapides"
    - "Le middleware updateSession redirige vers /admin/login sans token"
  artifacts:
    - path: "src/__tests__/integration/routes/admin-visuals.integration.test.ts"
      provides: "Tests integration visuels (validate, publish, bulk, export ZIP)"
      min_lines: 100
    - path: "src/__tests__/integration/routes/admin-generate.integration.test.ts"
      provides: "Tests integration generation IA (generate, generate-all)"
      min_lines: 80
    - path: "src/__tests__/integration/routes/simulate.integration.test.ts"
      provides: "Tests integration simulation publique + rate-limit + middleware"
      min_lines: 60
  key_links:
    - from: "admin-visuals.integration.test.ts"
      to: "src/app/api/admin/visuals/[id]/validate/route.ts"
      via: "testApiHandler appHandler"
      pattern: "import.*visuals.*validate"
    - from: "admin-generate.integration.test.ts"
      to: "src/app/api/admin/generate/route.ts"
      via: "testApiHandler appHandler"
      pattern: "import.*admin/generate/route"
    - from: "simulate.integration.test.ts"
      to: "src/app/api/simulate/route.ts"
      via: "testApiHandler appHandler"
      pattern: "import.*simulate/route"
---

<objective>
Tester les routes admin visuels (validate, publish, bulk, export ZIP), les routes de generation IA (generate, generate-all) avec le mock provider Sharp, la route publique simulate avec rate-limit, et le middleware updateSession.

Purpose: Valider le workflow complet visuels (generate -> validate -> publish -> export) et la simulation publique en integration. Le mock Sharp est utilise (NANO_BANANA_API_KEY absente de .env.test.local).
Output: 3 fichiers de test d'integration couvrant visuels workflow + IA generation + simulation publique
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15.1-tests-int-gration-supabase/15.1-RESEARCH.md
@.planning/phases/15.1-tests-int-gration-supabase/15.1-01-SUMMARY.md

<interfaces>
<!-- Routes admin visuels -->
From src/app/api/admin/visuals/[id]/validate/route.ts:
```typescript
// PUT — Valide un visuel (is_validated = true). Retourne le visuel mis a jour ou 404.
export async function PUT(request: NextRequest, { params }: { params: Promise<{ id: string }> }): Promise<NextResponse>
```

From src/app/api/admin/visuals/[id]/publish/route.ts:
```typescript
// PUT — Publie un visuel (is_published = true). REQUIERT is_validated = true, sinon 403.
export async function PUT(request: NextRequest, { params }: { params: Promise<{ id: string }> }): Promise<NextResponse>
```

From src/app/api/admin/visuals/bulk-validate/route.ts:
```typescript
// POST — Valide plusieurs visuels. Body: { ids: string[] }
export async function POST(request: NextRequest): Promise<NextResponse>
```

From src/app/api/admin/visuals/bulk-publish/route.ts:
```typescript
// POST — Publie plusieurs visuels valides. Body: { ids: string[] }
export async function POST(request: NextRequest): Promise<NextResponse>
```

From src/app/api/admin/visuals/export/[modelId]/route.ts:
```typescript
// GET — Export ZIP des visuels publies d'un modele. Retourne application/zip.
export async function GET(request: NextRequest, { params }: { params: Promise<{ modelId: string }> }): Promise<NextResponse>
```

<!-- Routes IA -->
From src/app/api/admin/generate/route.ts:
```typescript
// POST — Genere un visuel IA. Body: { model_id, model_image_id, fabric_id }
// Utilise getIAService() — Mock Sharp si NANO_BANANA_API_KEY absente
export async function POST(request: NextRequest): Promise<NextResponse>
```

From src/app/api/admin/generate-all/route.ts:
```typescript
// POST — Genere visuels pour tous les angles d'un modele. Body: { model_id, fabric_id }
export async function POST(request: NextRequest): Promise<NextResponse>
```

From src/app/api/simulate/route.ts:
```typescript
// POST — Simulation publique. FormData: image (File), model_id, fabric_id (optionnel)
// Rate-limit 5 appels/minute par IP. Retourne image/jpeg binaire.
export async function POST(request: NextRequest): Promise<NextResponse>
```

From src/lib/supabase/middleware.ts:
```typescript
// updateSession(request) — Redirige vers /admin/login si pas authentifie sur /admin/*
export async function updateSession(request: NextRequest): Promise<NextResponse>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests integration visuels (validate, publish, bulk, export ZIP)</name>
  <files>src/__tests__/integration/routes/admin-visuals.integration.test.ts</files>
  <read_first>
    src/app/api/admin/visuals/[id]/validate/route.ts,
    src/app/api/admin/visuals/[id]/publish/route.ts,
    src/app/api/admin/visuals/bulk-validate/route.ts,
    src/app/api/admin/visuals/bulk-publish/route.ts,
    src/app/api/admin/visuals/export/[modelId]/route.ts,
    src/__tests__/integration/helpers/auth.ts,
    src/__tests__/integration/helpers/supabase-admin.ts,
    src/__tests__/integration/helpers/seed.ts
  </read_first>
  <action>
Creer `src/__tests__/integration/routes/admin-visuals.integration.test.ts`.

CRITIQUE : `import { testApiHandler } from 'next-test-api-route-handler'` PREMIER import.

```typescript
import { testApiHandler } from 'next-test-api-route-handler' // PREMIER IMPORT OBLIGATOIRE
import * as validateRoute from '@/app/api/admin/visuals/[id]/validate/route'
import * as publishRoute from '@/app/api/admin/visuals/[id]/publish/route'
import * as bulkValidateRoute from '@/app/api/admin/visuals/bulk-validate/route'
import * as bulkPublishRoute from '@/app/api/admin/visuals/bulk-publish/route'
import * as exportRoute from '@/app/api/admin/visuals/export/[modelId]/route'
import { loginAsAdmin } from '../helpers/auth'
import { seedAdminUser, seedTestData } from '../helpers/seed'
import { adminClient } from '../helpers/supabase-admin'
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
```

**beforeAll:**
1. `seedAdminUser()` + `seedTestData()` + `loginAsAdmin()`
2. Creer un visuel de test NON valide via adminClient pour le workflow validate->publish :
```typescript
await adminClient.from('generated_visuals').upsert({
  id: 'e0000000-0000-0000-0000-000000000010',
  model_id: 'b0000000-0000-0000-0000-000000000001',
  model_image_id: 'd0000000-0000-0000-0000-000000000001',
  fabric_id: 'c0000000-0000-0000-0000-000000000001',
  generated_image_url: 'http://127.0.0.1:54321/storage/v1/object/public/generated-visuals/test.jpg',
  is_validated: false,
  is_published: false,
})
```

NOTE: La contrainte UNIQUE (model_image_id, fabric_id) peut bloquer si le seed a deja insere un visuel pour le meme couple. Utiliser un fabric_id different pour le visuel de test :
- Soit ajouter un 3eme tissu de test dans seedTestData
- Soit utiliser le tissu 'Lin Inactif' (c0000000-...-000000000002) qui n'a pas de visuel seed

**afterAll:** Nettoyer les visuels de test via adminClient.

**Tests a ecrire :**

1. `describe('PUT /api/admin/visuals/[id]/validate')`:
   - `it('valide un visuel non valide')` — PUT avec params `{ id: 'e0000000-...-000000000010' }`. Verifier status 200, is_validated true.
   - `it('retourne 404 pour un visuel inexistant')` — UUID inexistant. Verifier 404.

2. `describe('PUT /api/admin/visuals/[id]/publish')`:
   - `it('publie un visuel valide')` — PUT avec le visuel valide ci-dessus. Verifier status 200, is_published true.
   - `it('retourne 403 si le visuel n\'est pas valide')` — Creer un visuel non valide, tenter de publier. Verifier status 403 (si la route verifie is_validated, sinon adapter le test au comportement reel de la route).

NOTE: Verifier le code de la route publish pour confirmer qu'elle verifie is_validated avant de publier. Si la route n'a pas cette verification, adapter le test.

3. `describe('POST /api/admin/visuals/bulk-validate')`:
   - `it('valide plusieurs visuels d\'un coup')` — Creer 2 visuels non valides, POST avec `{ ids: [id1, id2] }`. Verifier status 200 et les 2 visuels sont valides.

4. `describe('POST /api/admin/visuals/bulk-publish')`:
   - `it('publie plusieurs visuels valides')` — POST avec les 2 visuels valides. Verifier status 200.

5. `describe('GET /api/admin/visuals/export/[modelId]')`:
   - `it('retourne un ZIP des visuels publies')` — GET avec `params: { modelId: 'b0000000-...-000000000001' }`. Verifier status 200, Content-Type contient 'zip' ou 'octet-stream', le body n'est pas vide.
   - `it('retourne 404 ou vide pour un modele sans visuels')` — UUID inexistant ou modele sans visuel publie.
  </action>
  <verify>
    <automated>npx vitest run --project integration src/__tests__/integration/routes/admin-visuals.integration.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/__tests__/integration/routes/admin-visuals.integration.test.ts
    - First import is `import { testApiHandler } from 'next-test-api-route-handler'`
    - File imports validate, publish, bulk-validate, bulk-publish, and export route modules
    - File contains test for validate (is_validated becomes true)
    - File contains test for publish (is_published becomes true)
    - File contains test for bulk-validate and bulk-publish
    - File contains test for export ZIP with Content-Type check
    - File contains at least 8 test cases
    - All tests pass with `npx vitest run --project integration`
  </acceptance_criteria>
  <done>8+ tests d'integration pour le workflow visuels complet (validate -> publish -> bulk -> export ZIP), tous passent au vert</done>
</task>

<task type="auto">
  <name>Task 2: Tests integration IA generate + simulate + rate-limit + middleware (D-05, D-09, D-10)</name>
  <files>
    src/__tests__/integration/routes/admin-generate.integration.test.ts,
    src/__tests__/integration/routes/simulate.integration.test.ts
  </files>
  <read_first>
    src/app/api/admin/generate/route.ts,
    src/app/api/admin/generate-all/route.ts,
    src/app/api/simulate/route.ts,
    src/lib/supabase/middleware.ts,
    src/lib/ai/index.ts,
    src/__tests__/integration/helpers/auth.ts,
    src/__tests__/integration/helpers/supabase-admin.ts
  </read_first>
  <action>
**Fichier 1 : `src/__tests__/integration/routes/admin-generate.integration.test.ts`**

CRITIQUE : `import { testApiHandler } from 'next-test-api-route-handler'` PREMIER import.

```typescript
import { testApiHandler } from 'next-test-api-route-handler' // PREMIER IMPORT OBLIGATOIRE
import * as generateRoute from '@/app/api/admin/generate/route'
import * as generateAllRoute from '@/app/api/admin/generate-all/route'
import { loginAsAdmin } from '../helpers/auth'
import { seedAdminUser, seedTestData } from '../helpers/seed'
import { adminClient } from '../helpers/supabase-admin'
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
```

**IMPORTANT :** S'assurer que `NANO_BANANA_API_KEY` n'est PAS dans `.env.test.local` (Pitfall 7 RESEARCH.md). Le mock Sharp sera utilise automatiquement. Le mock Sharp genere un vrai buffer JPEG — les tests peuvent verifier les uploads Storage.

**beforeAll:** `seedAdminUser()`, `seedTestData()`, `loginAsAdmin()`.

**afterAll:** Nettoyer visuels generes avec adminClient + supprimer les fichiers dans le bucket `generated-visuals`.

**Tests a ecrire :**

1. `describe('POST /api/admin/generate')`:
   - `it('genere un visuel avec le mock provider')` — Body `{ model_id: 'b0000000-...-001', model_image_id: 'd0000000-...-001', fabric_id: 'c0000000-...-001' }`. Verifier status 201, la reponse contient id, generated_image_url, is_validated false, is_published false.
   - `it('retourne 401 sans auth')` — POST sans Cookie. Verifier 401.
   - `it('retourne 400 avec body incomplet')` — `{ model_id: 'xxx' }`. Verifier 400.
   - `it('retourne 404 si le modele n\'existe pas')` — UUID inexistant pour model_id. Verifier 404.

   NOTE: Le generate utilise le model_image existant du seed (d0000000-...-001) dont l'image_url pointe vers le Storage local. Le mock Sharp fetchera cette URL — si le fichier n'existe pas reellement dans le bucket, le mock utilisera une image par defaut. Tester la robustesse du flow.

2. `describe('POST /api/admin/generate-all')`:
   - `it('genere visuels pour tous les angles')` — Body `{ model_id: 'b0000000-...-001', fabric_id: 'c0000000-...-001' }`. Verifier status 200/201, la reponse contient un tableau de visuels (1 par angle du modele).
   - `it('retourne 400 sans model_id')` — Body `{ fabric_id: 'xxx' }`. Verifier 400.

**Fichier 2 : `src/__tests__/integration/routes/simulate.integration.test.ts`**

CRITIQUE : `import { testApiHandler } from 'next-test-api-route-handler'` PREMIER import.

```typescript
import { testApiHandler } from 'next-test-api-route-handler' // PREMIER IMPORT OBLIGATOIRE
import * as simulateRoute from '@/app/api/simulate/route'
import { seedAdminUser, seedTestData } from '../helpers/seed'
import { describe, it, expect, beforeAll } from 'vitest'
```

**IMPORTANT (Pitfall 6 RESEARCH.md) :** Le rate-limiter est en memoire (Map). Apres 5 appels avec la meme IP, le 6eme retourne 429. Utiliser des headers `x-forwarded-for` differents pour eviter le rate-limit dans les tests nominaux. Pour le test rate-limit, utiliser la meme IP volontairement.

**Tests a ecrire :**

1. `describe('POST /api/simulate')`:
   - `it('retourne un JPEG binaire avec le mock provider')` — FormData avec un fichier JPEG 1x1 de test, model_id du seed. Utiliser `x-forwarded-for: 10.0.0.1` pour eviter le rate-limit. Verifier status 200, Content-Type 'image/jpeg', body non vide.
   - `it('retourne 400 sans image')` — FormData avec model_id mais sans fichier. Verifier 400.
   - `it('retourne 400 sans model_id')` — FormData avec image mais sans model_id. Verifier 400.
   - `it('retourne 404 pour un modele inexistant')` — FormData avec image + model_id UUID inexistant. Utiliser `x-forwarded-for: 10.0.0.2`. Verifier 404.

2. `describe('Rate-limit /api/simulate (Pitfall 6)')`:
   - `it('retourne 429 apres 5 appels rapides depuis la meme IP')` — Faire 5 appels valides avec `x-forwarded-for: 10.0.0.99` puis un 6eme. Verifier que les 5 premiers retournent 200 et le 6eme retourne 429 avec un header `Retry-After`.

   NOTE: Ce test est potentiellement lent (5 appels IA meme avec le mock). Si le mock est rapide (<50ms), c'est acceptable. Sinon, considerer de mocker l'IA service pour ce test specifique.

3. `describe('Middleware updateSession (D-09)')`:
   Le middleware `updateSession` dans `src/lib/supabase/middleware.ts` n'est PAS une route API — c'est une fonction middleware. NTARH ne teste pas les middlewares. Tester directement la fonction :

   ```typescript
   import { updateSession } from '@/lib/supabase/middleware'
   import { NextRequest } from 'next/server'
   ```

   - `it('ne redirige pas sur les routes publiques')` — Creer un `NextRequest` pour `/` et verifier que updateSession retourne NextResponse.next().
   - `it('redirige vers /admin/login sans auth sur /admin/dashboard')` — Creer un `NextRequest` pour `/admin/dashboard` sans cookies. Verifier redirect vers `/admin/login`.
   - `it('ne redirige pas sur /admin/login')` — Creer un `NextRequest` pour `/admin/login`. Verifier pas de redirect.

   NOTE: Ces tests middleware appellent `createServerClient` qui utilise les cookies du NextRequest. En contexte Node (pas NTARH), les cookies sont accessibles directement sur le request object. Si `updateSession` appelle `supabase.auth.getUser()` et que l'instance Supabase locale est disponible, le test est une vraie integration. Si l'instance n'est pas disponible, le test echouera — c'est attendu (les tests d'integration necessitent `supabase start`).
  </action>
  <verify>
    <automated>npx vitest run --project integration src/__tests__/integration/routes/admin-generate.integration.test.ts src/__tests__/integration/routes/simulate.integration.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/__tests__/integration/routes/admin-generate.integration.test.ts
    - File exists at src/__tests__/integration/routes/simulate.integration.test.ts
    - Both files have `import { testApiHandler } from 'next-test-api-route-handler'` as first import (where applicable — middleware test uses direct function call)
    - admin-generate test contains POST generate with status 201
    - admin-generate test contains POST generate-all
    - admin-generate test contains 401 test without auth
    - simulate test contains FormData with File/Blob for image upload
    - simulate test contains `x-forwarded-for` header to control IP
    - simulate test contains rate-limit test (429 after 5 calls)
    - simulate test contains Content-Type check for 'image/jpeg'
    - simulate test or separate describe contains middleware updateSession tests
    - At least 12 test cases across both files
    - All tests pass with `npx vitest run --project integration`
  </acceptance_criteria>
  <done>12+ tests d'integration pour la generation IA (generate single + generate-all avec mock Sharp), la simulation publique (FormData, JPEG binaire, rate-limit 429), et le middleware updateSession (redirect non-auth), tous passent au vert</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> simulate | Route publique, rate-limitee, input non fiable (image uploadee) |
| admin auth -> generate | Routes admin protegees par requireAdmin |
| middleware -> admin pages | Redirect vers login si pas authentifie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-10 | Denial of Service | simulate rate-limit | mitigate | Test explicite 429 apres 5 appels rapides dans simulate.integration.test.ts |
| T-15.1-11 | Tampering | generate sans auth | mitigate | Test explicite 401 sans Cookie dans admin-generate.integration.test.ts |
| T-15.1-12 | Spoofing | middleware bypass | mitigate | Test updateSession redirect vers /admin/login sans token |
| T-15.1-13 | Information Disclosure | NANO_BANANA_API_KEY en test | mitigate | .env.test.local ne contient PAS NANO_BANANA_API_KEY — le mock Sharp est utilise |
| T-15.1-14 | Elevation of Privilege | publish sans validate | mitigate | Test explicite 403 si publish sur visuel non valide |
</threat_model>

<verification>
- `npx vitest run --project integration` passe tous les tests (visuels + generate + simulate + middleware)
- Le workflow complet generate -> validate -> publish -> export fonctionne
- Le rate-limit simulate retourne 429 correctement
- Le middleware redirige les non-auth vers /admin/login
- NANO_BANANA_API_KEY absente de .env.test.local (mock Sharp utilise)
</verification>

<success_criteria>
Les routes IA, visuels, simulation et middleware sont testees en integration. Le workflow complet generate->validate->publish->export est couvert. Le rate-limit est verifie. Minimum 20 tests passant au vert.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-tests-int-gration-supabase/15.1-04-SUMMARY.md`
</output>
