---
phase: 01-fondation-header
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/page.module.css
  - src/components/public/Header/Header.tsx
  - src/components/public/Header/Header.module.css
autonomous: false
requirements:
  - FOND-01
  - FOND-02
  - FOND-03
  - FOND-04
  - HEAD-01
  - HEAD-02
  - HEAD-03
  - HEAD-04

must_haves:
  truths:
    - "La page publique s'affiche sans le template Next.js par défaut (fond blanc, pas de logo Next.js)"
    - "Le titre SEO de l'onglet est 'Accueil | Möbel Unique'"
    - "Le header est visible en position fixe en haut de la page, affiche le logo MU et le texte 'Möbel Unique'"
    - "Un lien 'Retour à la boutique' est présent dans le header"
    - "Au scroll de 80px, le header passe de transparent à blanc avec ombre visible en 300ms"
    - "Le header a un effet glassmorphism (backdrop-filter blur 20px) après scroll"
    - "Le skip link 'Aller au contenu' est invisible au repos et visible au focus clavier (Tab)"
    - "scroll-behavior: smooth et scroll-padding-top: 64px sont actifs dans globals.css"
  artifacts:
    - path: "src/components/public/Header/Header.tsx"
      provides: "Composant header avec scroll listener et skip link"
      exports: ["Header"]
    - path: "src/components/public/Header/Header.module.css"
      provides: "Styles header : fixed, transition transparent→blanc, glassmorphism, skip link"
    - path: "src/app/page.tsx"
      provides: "Page publique squelette avec Header et main#main-content"
    - path: "src/app/page.module.css"
      provides: "Styles page propres (sans template Next.js)"
    - path: "src/app/layout.tsx"
      provides: "Metadata globale avec title.template pattern"
    - path: "src/app/globals.css"
      provides: "scroll-behavior et scroll-padding-top dans le bloc html{}"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/public/Header/Header.tsx"
      via: "import named export Header"
      pattern: "import.*Header.*from.*components/public/Header"
    - from: "src/components/public/Header/Header.tsx"
      to: "window scroll event"
      via: "useEffect + addEventListener scroll passive"
      pattern: "addEventListener.*scroll.*passive"
    - from: "src/components/public/Header/Header.tsx"
      to: "main#main-content"
      via: "href='#main-content' dans le skip link"
      pattern: "href.*#main-content"
    - from: "src/app/page.tsx"
      to: "main#main-content"
      via: "id='main-content' sur l'élément main"
      pattern: "id.*main-content"
---

<objective>
Remplacer le template Next.js par défaut par la fondation CSS publique et le Header sticky interactif de Möbel Unique.

Purpose: Cette phase établit la structure de base dont toutes les phases suivantes dépendent — le header fixe définit la hauteur réservée pour le hero (Phase 2), et globals.css fournit les comportements de scroll pour les ancres du site.

Output: 2 nouveaux fichiers (Header.tsx, Header.module.css) + 4 fichiers modifiés (globals.css, layout.tsx, page.tsx, page.module.css). Page publique opérationnelle avec header sticky animé.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-fondation-header/01-RESEARCH.md

@src/app/globals.css
@src/app/layout.tsx
@src/app/page.tsx
@src/app/page.module.css

<interfaces>
<!-- Tokens CSS disponibles dans globals.css — utiliser var() uniquement, ne pas hardcoder -->

Design tokens clés pour cette phase :
```css
--color-primary: #E49400
--color-text: #1D1D1B
--color-background: #FFFFFF
--surface: #FCF9F5
--header-height: 64px
--transition-fast: 300ms ease
--shadow-header: 0 2px 12px rgba(0,0,0,0.08)
--container-padding-mobile: 24px
--container-padding-desktop: 48px
--container-padding-large: 64px
--spacing-sm: 0.5rem
--spacing-md: 1rem
--radius-sm: 4px
--radius-md: 8px
--font-size-sm: 0.875rem
--font-size-lg: 1.125rem
```

État actuel globals.css — bloc html{} existant (lignes 94-97) :
```css
html {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
```
Y ajouter les 2 propriétés de scroll — NE PAS créer un nouveau bloc html{}.

État actuel layout.tsx — metadata à remplacer :
```typescript
export const metadata: Metadata = {
  title: "Möbel Unique — Back-office",
  description: "Administration Möbel Unique — Visualisation IA de canapés personnalisables",
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : globals.css + layout.tsx + page.module.css — fondation CSS et metadata</name>
  <files>
    src/app/globals.css
    src/app/layout.tsx
    src/app/page.module.css
  </files>
  <action>
**globals.css** (per FOND-03, FOND-04) — Ajouter dans le bloc `html {}` existant (lignes 94-97) :
```css
html {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  scroll-behavior: smooth;
  scroll-padding-top: var(--header-height);
}
```
Ne pas créer un second bloc html{}. Ajouter seulement les 2 lignes manquantes.

**layout.tsx** (per FOND-02) — Remplacer le metadata "Back-office" par le pattern title.template :
```typescript
export const metadata: Metadata = {
  title: {
    template: '%s | Möbel Unique',
    default: 'Möbel Unique — Canapés personnalisables Paris',
  },
  description: 'Configurateur IA de canapés personnalisables. Visualisez votre canapé dans le tissu de votre choix.',
};
```
Garder tout le reste de layout.tsx intact (Montserrat, html lang="fr", montserrat.variable).

**page.module.css** (per FOND-01) — Supprimer entièrement le template pollué et le remplacer par :
```css
.page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  background-color: var(--color-background);
}

.main {
  flex: 1;
  display: flex;
  flex-direction: column;
}
```
Aucune variable locale, aucun dark mode, aucune référence à --font-geist-sans.
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - globals.css contient scroll-behavior et scroll-padding-top dans le bloc html{} existant (un seul bloc html)
    - layout.tsx metadata utilise title.template '%s | Möbel Unique' avec default public
    - page.module.css ne contient plus de références à --font-geist-sans, --background, ni @media prefers-color-scheme
    - npx tsc --noEmit retourne 0 erreur
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : Header.tsx + Header.module.css — composant sticky avec scroll</name>
  <files>
    src/components/public/Header/Header.tsx
    src/components/public/Header/Header.module.css
  </files>
  <action>
Créer le dossier `src/components/public/Header/` s'il n'existe pas.

**Header.tsx** (per HEAD-01, HEAD-02, HEAD-03, HEAD-04) :
```typescript
'use client'

import { useState, useEffect } from 'react'
import Link from 'next/link'
import styles from './Header.module.css'

export function Header() {
  const [scrolled, setScrolled] = useState(false)

  useEffect(() => {
    const handleScroll = () => {
      const isScrolled = window.scrollY > 80
      setScrolled(prev => prev !== isScrolled ? isScrolled : prev)
    }
    window.addEventListener('scroll', handleScroll, { passive: true })
    return () => window.removeEventListener('scroll', handleScroll)
  }, [])

  return (
    <>
      <a href="#main-content" className={styles.skipLink}>
        Aller au contenu
      </a>
      <header
        className={`${styles.header}${scrolled ? ` ${styles.scrolled}` : ''}`}
        role="banner"
      >
        <Link href="/" className={styles.brand} aria-label="Möbel Unique — Accueil">
          <div className={styles.logo} aria-hidden="true">MU</div>
          <span className={styles.brandName}>Möbel Unique</span>
        </Link>
        <a
          href="#"
          className={styles.shopifyLink}
          rel="noopener noreferrer"
          aria-label="Retourner à la boutique Shopify"
        >
          Retour à la boutique
        </a>
      </header>
    </>
  )
}
```

Points critiques :
- `'use client'` en première ligne du fichier (avant les imports)
- `useState(false)` — JAMAIS `useState(window.scrollY > 80)` (crash SSR)
- `{ passive: true }` sur l'écouteur scroll (performance mobile)
- `return () => window.removeEventListener(...)` dans useEffect (cleanup)
- Skip link AVANT le `<header>` (standard accessibilité)
- Export nommé `export function Header()` (pas export default)

**Header.module.css** (per HEAD-01, HEAD-02, HEAD-03, HEAD-04) :
```css
/* Skip link accessibilité (HEAD-04) */
.skipLink {
  position: absolute;
  top: -100%;
  left: var(--spacing-md);
  background: var(--color-primary);
  color: var(--color-background);
  padding: var(--spacing-sm) var(--spacing-md);
  border-radius: var(--radius-md);
  font-weight: 600;
  font-size: var(--font-size-sm);
  z-index: 200;
  text-decoration: none;
  transition: top 0.2s ease;
}

.skipLink:focus {
  top: var(--spacing-md);
  outline: 2px solid var(--color-text);
  outline-offset: 2px;
}

/* Header principal (HEAD-01) */
.header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: var(--header-height);
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 var(--container-padding-mobile);
  background: transparent;
  color: var(--color-text);
  transition:
    background var(--transition-fast),
    box-shadow var(--transition-fast),
    color var(--transition-fast);
}

/* Etat scrollé — transition + glassmorphism (HEAD-02, HEAD-03) */
.scrolled {
  background: rgba(252, 249, 245, 0.92);
  -webkit-backdrop-filter: blur(20px);
  backdrop-filter: blur(20px);
  box-shadow: var(--shadow-header);
  color: var(--color-text);
}

/* Logo monogramme MU (HEAD-01) */
.logo {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  background: var(--color-primary);
  color: #ffffff;
  font-weight: 700;
  font-size: var(--font-size-sm);
  border-radius: var(--radius-sm);
  flex-shrink: 0;
  user-select: none;
}

/* Groupe marque (logo + nom) */
.brand {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
  text-decoration: none;
  color: inherit;
}

.brandName {
  font-weight: 600;
  font-size: var(--font-size-lg);
  letter-spacing: -0.01em;
}

/* Lien retour Shopify (HEAD-01) */
.shopifyLink {
  font-size: var(--font-size-sm);
  font-weight: 500;
  color: inherit;
  text-decoration: none;
  opacity: 0.85;
  transition: opacity var(--transition-fast);
}

.shopifyLink:hover {
  opacity: 1;
}

/* Responsive (FOND-03) */
@media (min-width: 1024px) {
  .header {
    padding: 0 var(--container-padding-desktop);
  }
}

@media (min-width: 1280px) {
  .header {
    padding: 0 var(--container-padding-large);
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Header.tsx existe avec 'use client' en première ligne
    - useState initialisé à false (pas window.scrollY)
    - useEffect retourne une fonction cleanup removeEventListener
    - Skip link présent avant le header avec href="#main-content"
    - Export nommé (pas export default)
    - Header.module.css contient .header, .scrolled, .skipLink, .skipLink:focus, .logo, .brand, .shopifyLink
    - Aucun style hardcodé — tous les tokens utilisent var()
    - npx tsc --noEmit retourne 0 erreur
  </done>
</task>

<task type="auto">
  <name>Tâche 3 : page.tsx — remplacement template, metadata page, assemblage</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Remplacer entièrement le contenu de page.tsx (per FOND-01, FOND-02) :
```typescript
import type { Metadata } from 'next'
import { Header } from '@/components/public/Header/Header'
import styles from './page.module.css'

export const metadata: Metadata = {
  title: 'Accueil',
  description: 'Visualisez votre canapé personnalisé dans le tissu de votre choix grâce à notre configurateur IA. Livraison Paris et Île-de-France.',
}

export default function HomePage() {
  return (
    <div className={styles.page}>
      <Header />
      <main id="main-content" className={styles.main}>
        {/* Phase 2 : <Hero /> */}
        {/* Phase 3 : <HowItWorks /> */}
      </main>
    </div>
  )
}
```

Points critiques :
- PAS de `'use client'` sur page.tsx (Server Component)
- `<main id="main-content">` est la cible du skip link Header
- Import nommé `{ Header }` (pas default)
- Les commentaires de phases futures restent pour guider les prochaines implémentations
- Avec title.template dans layout.tsx, l'onglet affichera "Accueil | Möbel Unique"
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - page.tsx ne contient plus aucune référence au template Next.js (next.svg, vercel.svg, "To get started")
    - page.tsx n'a pas de 'use client'
    - main a id="main-content"
    - Header est importé depuis @/components/public/Header/Header
    - npx tsc --noEmit retourne 0 erreur
    - npm run build réussit sans erreur
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tâche 4 : Vérification visuelle — header sticky et fondation CSS</name>
  <action>Vérification manuelle par l'utilisateur du résultat visuel et comportemental du header.</action>
  <what-built>
    Fondation CSS et Header sticky Möbel Unique :
    - globals.css mis à jour (scroll-behavior smooth, scroll-padding-top 64px)
    - layout.tsx mis à jour (title.template 'Accueil | Möbel Unique')
    - page.tsx remplacé (plus de template Next.js, Header + main)
    - page.module.css remplacé (propre, sans template)
    - Header.tsx créé ('use client', scroll listener, skip link)
    - Header.module.css créé (fixed, transition, glassmorphism)
  </what-built>
  <how-to-verify>
    1. Lancer `npm run dev` et ouvrir http://localhost:3000
    2. Vérifier l'onglet navigateur : doit afficher "Accueil | Möbel Unique" (pas "Back-office")
    3. Vérifier la page : fond blanc, pas de logo Next.js, pas de template "To get started"
    4. Vérifier le header : visible en haut, logo "MU" ambré + "Möbel Unique" + lien "Retour à la boutique"
    5. Scroller lentement la page (créer du contenu fictif si besoin) — au-delà de 80px le header doit devenir blanc avec ombre
    6. Appuyer sur Tab : le lien "Aller au contenu" doit apparaître en ambré en haut à gauche
    7. Vérifier sur mobile (DevTools responsive) : header visible sur mobile, tablette et desktop
  </how-to-verify>
  <resume-signal>Taper "approuvé" ou décrire les problèmes observés</resume-signal>
</task>

</tasks>

<verification>
Vérifications automatisées après chaque tâche TypeScript :

```bash
# Après Tâche 1, 2 et 3 :
npx tsc --noEmit

# Validation finale de phase (après Tâche 3) :
npm run build
```

Vérifications manuelles (checkpoint Tâche 4) :
- Onglet navigateur : "Accueil | Möbel Unique"
- Page blanche sans template Next.js
- Header sticky ambré avec glassmorphism au scroll
- Skip link visible au focus Tab
- Responsive header 3 breakpoints
</verification>

<success_criteria>
Phase 1 est complète quand :
1. `npm run build` réussit sans erreur TypeScript ni d'import manquant
2. L'onglet navigateur affiche "Accueil | Möbel Unique" sur http://localhost:3000
3. Le header est visible et fixe, avec logo MU et lien retour boutique
4. Au scroll de 80px, le header passe de transparent à blanc avec ombre (transition 300ms visible)
5. Le skip link "Aller au contenu" apparaît au focus Tab
6. globals.css contient scroll-behavior et scroll-padding-top dans le bloc html{}
7. page.module.css ne contient plus aucun artifact du template Next.js (dark mode, geist-sans)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-fondation-header/01-01-SUMMARY.md` en suivant le template summary.md.
</output>
