---
phase: 03-howitworks-assemblage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/public/HowItWorks/HowItWorks.tsx
  - src/components/public/HowItWorks/HowItWorks.module.css
  - src/components/public/HowItWorks/__tests__/HowItWorks.test.tsx
  - src/app/page.tsx
autonomous: true
requirements: [STEP-01, STEP-02, STEP-03]

must_haves:
  truths:
    - "3 cartes etapes (01 Choisissez votre modele, 02 Personnalisez avec votre tissu, 03 Visualisez chez vous) sont visibles sur fond beige #F8F4EE"
    - "Chaque carte affiche un numero en primary #E49400, une icone Lucide, un titre H3 et une description"
    - "Sur mobile (<640px) les cartes sont en colonne unique, sur desktop (>=640px) en grille 3 colonnes"
    - "Les cartes apparaissent avec fade-in + translateY au scroll via useInView (stagger 100ms)"
    - "Le template Next.js est entierement remplace — page.tsx ne contient aucun commentaire placeholder Phase 3"
  artifacts:
    - path: "src/components/public/HowItWorks/HowItWorks.tsx"
      provides: "Composant client HowItWorks avec 3 cartes animees"
      exports: ["HowItWorks"]
    - path: "src/components/public/HowItWorks/HowItWorks.module.css"
      provides: "Styles CSS Modules — section, grid, card, responsive"
      contains: "grid-template-columns"
    - path: "src/components/public/HowItWorks/__tests__/HowItWorks.test.tsx"
      provides: "Tests unitaires couvrant STEP-01, STEP-02, STEP-03"
      contains: "describe"
    - path: "src/app/page.tsx"
      provides: "Page accueil avec Header + Hero + HowItWorks"
      contains: "<HowItWorks"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/public/HowItWorks/HowItWorks.tsx"
      via: "import { HowItWorks } from '@/components/public/HowItWorks/HowItWorks'"
      pattern: "import.*HowItWorks.*from.*@/components/public/HowItWorks"
    - from: "src/components/public/HowItWorks/HowItWorks.tsx"
      to: "lucide-react"
      via: "import { Sofa, Palette, Home } from 'lucide-react'"
      pattern: "import.*Sofa.*Palette.*Home.*from.*lucide-react"
    - from: "src/components/public/HowItWorks/HowItWorks.tsx"
      to: "motion/react"
      via: "import { motion, useInView, useReducedMotion } from 'motion/react'"
      pattern: "import.*useInView.*from.*motion/react"
---

<objective>
Creer la section "Comment ca marche" avec 3 cartes etapes animees et l'integrer dans la page publique.

Purpose: Completer la page publique avec la section HowItWorks — 3 cartes (choisir, personnaliser, visualiser) sur fond beige, animation fade-in au scroll, responsive 1 col mobile / 3 col desktop. Le template Next.js par defaut est entierement remplace apres cette phase.

Output: Composant HowItWorks.tsx + HowItWorks.module.css + tests unitaires + integration page.tsx
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-howitworks-assemblage/03-CONTEXT.md
@.planning/phases/03-howitworks-assemblage/03-RESEARCH.md
@.planning/phases/03-howitworks-assemblage/03-UI-SPEC.md
@.planning/phases/03-howitworks-assemblage/03-VALIDATION.md

<interfaces>
<!-- Patterns existants a reproduire exactement -->

From src/components/public/Hero/Hero.tsx (pattern Framer Motion):
```typescript
'use client'
import { motion, useReducedMotion } from 'motion/react'
// useInView a ajouter pour cette phase

export function Hero() {
  const prefersReducedMotion = useReducedMotion()
  // motion.div avec initial/animate/transition
}
```

From src/app/page.tsx (point d'integration):
```typescript
import { Hero } from '@/components/public/Hero/Hero'
// Ajouter: import { HowItWorks } from '@/components/public/HowItWorks/HowItWorks'

export default function HomePage() {
  return (
    <div className={styles.page}>
      <Header />
      <main id="main-content" className={styles.main}>
        <Hero />
        {/* Phase 3 : <HowItWorks /> */}  // Remplacer par <HowItWorks />
      </main>
    </div>
  )
}
```

From src/app/globals.css (tokens CSS utilises):
```css
--color-primary: #E49400;
--color-text: #1D1D1B;
--color-muted: #888888;
--color-background-alt: #F8F4EE;
--surface-container-lowest: #FFFFFF;
--font-size-xs: 0.75rem;
--font-size-base: 1rem;
--font-size-2xl: 1.5rem;
--font-size-3xl: 2rem;
--spacing-xs: 0.25rem;
--spacing-sm: 0.5rem;
--spacing-md: 1rem;
--spacing-lg: 1.5rem;
--spacing-xl: 2rem;
--spacing-2xl: 3rem;
--spacing-section: 7rem;
--radius-lg: 12px;
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.07);
--shadow-lg: 0 10px 40px rgba(28, 28, 26, 0.04);
--container-max: 1280px;
--container-padding-mobile: 24px;
--container-padding-large: 64px;
--transition-smooth: 400ms ease-in-out;
```

From src/components/public/Hero/__tests__/Hero.test.tsx (pattern test):
```typescript
vi.mock('motion/react', () => ({
  motion: {
    div: ({ children, initial, animate, transition, ...props }) => <div {...props}>{children}</div>,
  },
  useReducedMotion: () => false,
}))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer lucide-react et creer le scaffold de tests HowItWorks</name>
  <files>
    package.json,
    package-lock.json,
    src/components/public/HowItWorks/__tests__/HowItWorks.test.tsx
  </files>
  <read_first>
    src/components/public/Hero/__tests__/Hero.test.tsx,
    src/__tests__/setup.ts,
    package.json,
    vitest.config.ts
  </read_first>
  <action>
1. Installer lucide-react :
   ```bash
   npm install lucide-react@1.7.0
   ```

2. Creer `src/components/public/HowItWorks/__tests__/HowItWorks.test.tsx` avec les tests suivants.
   Mock `motion/react` et `lucide-react` comme dans Hero.test.tsx. Les mocks :

   ```typescript
   vi.mock('lucide-react', () => ({
     Sofa: (props: Record<string, unknown>) => <svg data-testid="icon-sofa" aria-hidden="true" {...props} />,
     Palette: (props: Record<string, unknown>) => <svg data-testid="icon-palette" aria-hidden="true" {...props} />,
     Home: (props: Record<string, unknown>) => <svg data-testid="icon-home" aria-hidden="true" {...props} />,
   }))

   vi.mock('motion/react', () => ({
     motion: {
       div: ({ children, initial, animate, transition, ...props }: {
         children: React.ReactNode
         initial?: Record<string, unknown>
         animate?: Record<string, unknown>
         transition?: Record<string, unknown>
         [key: string]: unknown
       }) => <div {...props}>{children}</div>,
     },
     useInView: () => true,
     useReducedMotion: () => false,
   }))
   ```

   Tests a ecrire (chaque test dans un `it(...)`) :

   **STEP-01 — contenu cards :**
   - `rend le titre H2 "Comment ca marche"` : `screen.getByRole('heading', { level: 2 })` contient "Comment ca marche" (per D-01)
   - `rend 3 titres H3 des etapes` : `screen.getAllByRole('heading', { level: 3 })` a `.length === 3`, textes exacts "Choisissez votre modele", "Personnalisez avec votre tissu", "Visualisez chez vous" (per D-03)
   - `rend les numeros 01, 02, 03` : `screen.getByText('01')`, `screen.getByText('02')`, `screen.getByText('03')` tous `.toBeInTheDocument()` (per D-07)
   - `rend les 3 icones Lucide` : `screen.getByTestId('icon-sofa')`, `screen.getByTestId('icon-palette')`, `screen.getByTestId('icon-home')` tous `.toBeInTheDocument()` (per D-08)
   - `rend le sous-titre de section` : `screen.getByText(/En quelques clics/)` `.toBeInTheDocument()` (per D-02)
   - `rend les descriptions sous chaque etape` : `screen.getByText(/Parcourez notre collection/)`, `screen.getByText(/Choisissez parmi nos tissus/)`, `screen.getByText(/Uploadez une photo/)` tous `.toBeInTheDocument()` (per D-04)

   **STEP-02 — structure section :**
   - `la section a l'id "comment-ca-marche"` : `document.getElementById('comment-ca-marche')` n'est pas null
   - `les icones ont aria-hidden="true"` : `screen.getByTestId('icon-sofa')` `.toHaveAttribute('aria-hidden', 'true')` (accessibilite)

   **STEP-03 — animation (mock) :**
   - `avec useInView mocke a true, les cards sont rendues` : `screen.getAllByRole('heading', { level: 3 }).length === 3` (confirme que le composant se rend correctement avec le mock isInView=true)

   Import du composant : `import { HowItWorks } from '../HowItWorks'`
   Ce fichier de test echouera tant que le composant n'existe pas (RED normal — Task 2 le cree).
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && node -e "require('lucide-react')" 2>&1 | head -1 && test -f src/components/public/HowItWorks/__tests__/HowItWorks.test.tsx && echo "PASS: lucide-react installed + test file exists"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contient `"lucide-react": "1.7.0"` (ou `^1.7.0`)
    - `node_modules/lucide-react/package.json` existe
    - `src/components/public/HowItWorks/__tests__/HowItWorks.test.tsx` existe et contient `vi.mock('lucide-react'`
    - Le fichier test contient `describe('HowItWorks'`
    - Le fichier test contient au moins 9 `it(` blocks
    - Le fichier test contient les textes `Comment ca marche`, `Choisissez votre modele`, `Personnalisez avec votre tissu`, `Visualisez chez vous`
    - Le fichier test contient `getByTestId('icon-sofa')`, `getByTestId('icon-palette')`, `getByTestId('icon-home')`
    - Le fichier test contient `useInView: () => true` dans le mock motion/react
  </acceptance_criteria>
  <done>lucide-react@1.7.0 installe et fichier de test cree avec 9+ tests couvrant STEP-01, STEP-02, STEP-03. Les tests sont en echec (RED) car le composant n'existe pas encore.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Creer le composant HowItWorks.tsx et HowItWorks.module.css</name>
  <files>
    src/components/public/HowItWorks/HowItWorks.tsx,
    src/components/public/HowItWorks/HowItWorks.module.css
  </files>
  <read_first>
    src/components/public/HowItWorks/__tests__/HowItWorks.test.tsx,
    src/components/public/Hero/Hero.tsx,
    src/components/public/Hero/Hero.module.css,
    src/app/globals.css,
    .planning/phases/03-howitworks-assemblage/03-UI-SPEC.md,
    .planning/phases/03-howitworks-assemblage/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Le composant exporte `HowItWorks` comme named export
    - Test 2: Rend un H2 "Comment ca marche"
    - Test 3: Rend 3 H3 avec les titres des etapes
    - Test 4: Rend les numeros 01, 02, 03
    - Test 5: Rend les 3 icones Lucide (Sofa, Palette, Home)
    - Test 6: Rend le sous-titre contenant "En quelques clics"
    - Test 7: Rend les 3 descriptions des etapes
    - Test 8: La section a id="comment-ca-marche"
    - Test 9: Les icones ont aria-hidden="true"
  </behavior>
  <action>
Creer `src/components/public/HowItWorks/HowItWorks.tsx` — composant 'use client' :

```typescript
'use client'

import { useRef } from 'react'
import { motion, useInView, useReducedMotion } from 'motion/react'
import { Sofa, Palette, Home } from 'lucide-react'
import styles from './HowItWorks.module.css'
```

**Constante steps** (tableau de 3 objets) — per D-03, D-04, D-09 :
```typescript
const steps = [
  {
    id: 1,
    number: '01',
    icon: Sofa,
    title: 'Choisissez votre modele',  // avec accent e sur modèle dans le JSX
    description: 'Parcourez notre collection et selectionnez le modele qui correspond a votre espace et votre style de vie.',
  },
  {
    id: 2,
    number: '02',
    icon: Palette,
    title: 'Personnalisez avec votre tissu',
    description: 'Choisissez parmi nos tissus \u2014 couleur, texture, matiere \u2014 et voyez le resultat instantanement sur votre modele.',
  },
  {
    id: 3,
    number: '03',
    icon: Home,
    title: 'Visualisez chez vous',
    description: 'Uploadez une photo de votre salon et decouvrez votre nouveau canape exactement la ou il sera pose.',
  },
]
```

IMPORTANT: Les textes dans le JSX doivent utiliser les caracteres accentues corrects (e avec accent, a avec accent, etc.) conformement a la UI-SPEC. Les accents sont obligatoires dans le rendu HTML.

**Textes exacts de la UI-SPEC (avec accents) :**
- H2 : `Comment ca marche` (sans accent cedille sur le c dans le code, mais le navigateur peut recevoir `Comment ça marche` — utiliser `\u00e7` ou le caractere direct `ça`)

CORRECTION: Utiliser les caracteres Unicode corrects dans le JSX :
- Titre H2 : "Comment ça marche" (per D-01)
- Sous-titre : "En quelques clics, choisissez votre modèle, personnalisez le tissu et visualisez le résultat dans votre salon avant de commander." (per D-02 — texte exact de la UI-SPEC Copywriting Contract)
- Titres etapes : "Choisissez votre modèle", "Personnalisez avec votre tissu", "Visualisez chez vous" (per D-03)
- Descriptions (textes exacts UI-SPEC) :
  - "Parcourez notre collection et sélectionnez le modèle qui correspond à votre espace et votre style de vie."
  - "Choisissez parmi nos tissus — couleur, texture, matière — et voyez le résultat instantanément sur votre modèle."
  - "Uploadez une photo de votre salon et découvrez votre nouveau canapé exactement là où il sera posé."

**Fonction composant** :
```typescript
export function HowItWorks() {
  const ref = useRef<HTMLDivElement>(null)
  const isInView = useInView(ref, { once: true, margin: '-100px' })
  const prefersReducedMotion = useReducedMotion()

  return (
    <section id="comment-ca-marche" className={styles.section}>
      <div className={styles.container}>
        <div className={styles.header}>
          <h2 className={styles.sectionTitle}>Comment ça marche</h2>
          <p className={styles.sectionSubtitle}>
            En quelques clics, choisissez votre modèle, personnalisez le tissu
            et visualisez le résultat dans votre salon avant de commander.
          </p>
        </div>
        <div ref={ref} className={styles.grid}>
          {steps.map((step, index) => {
            const Icon = step.icon
            return (
              <motion.div
                key={step.id}
                className={styles.card}
                initial={{ opacity: 0, y: prefersReducedMotion ? 0 : 20 }}
                animate={isInView ? { opacity: 1, y: 0 } : { opacity: 0, y: prefersReducedMotion ? 0 : 20 }}
                transition={{
                  duration: prefersReducedMotion ? 0 : 0.4,
                  delay: prefersReducedMotion ? 0 : index * 0.1,
                  ease: 'easeInOut',
                }}
              >
                <span className={styles.number}>{step.number}</span>
                <Icon size={32} strokeWidth={1.5} aria-hidden="true" />
                <h3 className={styles.cardTitle}>{step.title}</h3>
                <p className={styles.cardDescription}>{step.description}</p>
              </motion.div>
            )
          })}
        </div>
      </div>
    </section>
  )
}
```

Animation : ref sur `.grid`, `useInView` avec `once: true` et `margin: '-100px'`. Chaque card recoit `delay: index * 0.1` pour le stagger 100ms (per D-11). `useReducedMotion()` desactive les animations si prefers-reduced-motion (per D-12).

Creer `src/components/public/HowItWorks/HowItWorks.module.css` avec TOUS les styles suivants (per UI-SPEC Layout Contract) :

```css
/* === Section Comment ca marche — HowItWorks.module.css === */
/* Phase 03 — howitworks-assemblage */

/* Section fond beige per D-05 */
.section {
  background-color: var(--color-background-alt); /* #F8F4EE */
  padding: var(--spacing-section) var(--container-padding-mobile); /* 112px 24px mobile */
}

/* Conteneur centre */
.container {
  max-width: var(--container-max); /* 1280px */
  margin: 0 auto;
}

/* En-tete section centre */
.header {
  text-align: center;
  margin-bottom: var(--spacing-2xl); /* 48px avant la grille */
}

/* Titre H2 */
.sectionTitle {
  font-size: var(--font-size-3xl); /* 32px */
  font-weight: 700;
  line-height: 1.2;
  color: var(--color-text); /* #1D1D1B */
}

/* Sous-titre muted */
.sectionSubtitle {
  font-size: var(--font-size-base); /* 16px */
  font-weight: 400;
  line-height: 1.6;
  color: var(--color-muted); /* #888888 */
  margin-top: var(--spacing-md); /* 16px sous le H2 */
  max-width: 640px;
  margin-left: auto;
  margin-right: auto;
}

/* Grille cards — mobile first 1 col */
.grid {
  display: grid;
  grid-template-columns: 1fr; /* 1 colonne mobile */
  gap: var(--spacing-xl); /* 32px entre cards */
}

/* Card blanche per D-05, D-06 */
.card {
  background-color: var(--surface-container-lowest); /* #FFFFFF */
  border-radius: var(--radius-lg); /* 12px per D-06 */
  box-shadow: var(--shadow-md); /* 0 4px 6px rgba(0,0,0,0.07) */
  padding: var(--spacing-2xl); /* 48px — espace galerie */
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm); /* 8px entre elements internes */
  transition: transform var(--transition-smooth), box-shadow var(--transition-smooth);
}

/* Hover subtil — luxe = delibere */
.card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg); /* 0 10px 40px rgba(28,28,26,0.04) */
}

/* Numero d'etape per D-07 */
.number {
  font-size: var(--font-size-xs); /* 12px */
  font-weight: 700;
  color: var(--color-primary); /* #E49400 per D-07 */
  letter-spacing: 0.1em;
  text-transform: uppercase;
  margin-bottom: var(--spacing-xs); /* 4px sous le numero */
}

/* Titre H3 card */
.cardTitle {
  font-size: var(--font-size-2xl); /* 24px */
  font-weight: 700;
  line-height: 1.2;
  color: var(--color-text); /* #1D1D1B */
}

/* Description card */
.cardDescription {
  font-size: var(--font-size-base); /* 16px */
  font-weight: 400;
  line-height: 1.6;
  color: var(--color-text); /* #1D1D1B */
}

/* === Responsive === */

/* Tablet/Desktop >= 640px : 3 colonnes per STEP-02 */
@media (min-width: 640px) {
  .grid {
    grid-template-columns: repeat(3, 1fr); /* 3 colonnes fixes */
  }
}

/* Large >= 1280px : padding lateral plus genereux */
@media (min-width: 1280px) {
  .section {
    padding: var(--spacing-section) var(--container-padding-large); /* 112px 64px */
  }
}
```

Couleur des icones : les icones Lucide heritent `color` du parent. Le conteneur `.card` a `color: var(--color-text)` via `.cardDescription`. L'icone aura la couleur texte sombre `#1D1D1B` naturellement — pas besoin de prop `color` explicite (per UI-SPEC : icones en `--color-text`).

Apres creation des deux fichiers, executer les tests pour verifier GREEN :
```bash
npm test -- --reporter=verbose src/components/public/HowItWorks
```
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npm test -- --reporter=verbose src/components/public/HowItWorks 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/public/HowItWorks/HowItWorks.tsx` existe et contient `'use client'`
    - Le fichier contient `export function HowItWorks()`
    - Le fichier contient `import { Sofa, Palette, Home } from 'lucide-react'`
    - Le fichier contient `import { motion, useInView, useReducedMotion } from 'motion/react'`
    - Le fichier contient `useInView(ref, { once: true, margin: '-100px' })`
    - Le fichier contient `delay: prefersReducedMotion ? 0 : index * 0.1`
    - Le fichier contient `id="comment-ca-marche"` sur la section
    - Le fichier contient `aria-hidden="true"` sur les icones
    - Le fichier contient les 3 titres exacts : "Choisissez votre modèle", "Personnalisez avec votre tissu", "Visualisez chez vous"
    - `src/components/public/HowItWorks/HowItWorks.module.css` existe et contient `var(--color-background-alt)`
    - Le CSS contient `grid-template-columns: 1fr` (mobile) et `grid-template-columns: repeat(3, 1fr)` (desktop)
    - Le CSS contient `var(--radius-lg)` et `var(--shadow-md)` pour les cards
    - Le CSS contient `var(--color-primary)` pour `.number`
    - Le CSS contient `@media (min-width: 640px)`
    - `npm test -- src/components/public/HowItWorks` passe avec 0 echecs
  </acceptance_criteria>
  <done>Composant HowItWorks.tsx + HowItWorks.module.css crees avec 3 cartes etapes, animation useInView + stagger 100ms, layout CSS Grid responsive. Tous les tests passent (GREEN).</done>
</task>

<task type="auto">
  <name>Task 3: Integrer HowItWorks dans page.tsx et verifier le build</name>
  <files>
    src/app/page.tsx
  </files>
  <read_first>
    src/app/page.tsx,
    src/components/public/HowItWorks/HowItWorks.tsx
  </read_first>
  <action>
Modifier `src/app/page.tsx` :

1. Ajouter l'import en haut du fichier, apres l'import de Hero :
   ```typescript
   import { HowItWorks } from '@/components/public/HowItWorks/HowItWorks'
   ```

2. Remplacer le commentaire `{/* Phase 3 : <HowItWorks /> */}` par le composant :
   ```typescript
   <HowItWorks />
   ```

Le fichier final doit etre :
```typescript
import type { Metadata } from 'next'
import { Header } from '@/components/public/Header/Header'
import { Hero } from '@/components/public/Hero/Hero'
import { HowItWorks } from '@/components/public/HowItWorks/HowItWorks'
import styles from './page.module.css'

export const metadata: Metadata = {
  title: 'Accueil | Mobel Unique',
  description: 'Visualisez votre canape personnalise dans le tissu de votre choix grace a notre configurateur IA. Livraison Paris et Ile-de-France.',
}

export default function HomePage() {
  return (
    <div className={styles.page}>
      <Header />
      <main id="main-content" className={styles.main}>
        <Hero />
        <HowItWorks />
      </main>
    </div>
  )
}
```

IMPORTANT: Conserver les accents et caracteres speciaux exactement tels qu'ils sont dans le fichier actuel (Mobel avec trema, canape avec accent, etc.). Ne PAS modifier le contenu existant de metadata ou d'autres elements — uniquement ajouter l'import et remplacer le commentaire.

3. Verifier que le build production passe sans erreur :
   ```bash
   npx tsc --noEmit
   npm run build
   ```

4. Verifier que la suite de tests complete passe :
   ```bash
   npm test
   ```
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -10 && npm test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` contient `import { HowItWorks } from '@/components/public/HowItWorks/HowItWorks'`
    - `src/app/page.tsx` contient `<HowItWorks />`
    - `src/app/page.tsx` ne contient PAS `{/* Phase 3`
    - `npx tsc --noEmit` se termine sans erreur (exit code 0)
    - `npm run build` se termine sans erreur (exit code 0)
    - `npm test` passe avec 0 echecs — incluant les tests Header, Hero et HowItWorks
  </acceptance_criteria>
  <done>HowItWorks integre dans page.tsx, build production sans erreur, zero erreur TypeScript, suite de tests complete verte. Le template Next.js est entierement remplace.</done>
</task>

</tasks>

<verification>
Verifications globales de la phase :

1. **STEP-01** : 3 cartes avec numeros (01, 02, 03), icones (Sofa, Palette, Home), titres H3 et descriptions sont visibles
   - `npm test -- src/components/public/HowItWorks` passe
   - Grepper "Choisissez votre modele" + "Personnalisez avec votre tissu" + "Visualisez chez vous" dans HowItWorks.tsx

2. **STEP-02** : Layout responsive 1 col mobile / 3 col desktop, fond beige
   - `grep "grid-template-columns: repeat(3, 1fr)" src/components/public/HowItWorks/HowItWorks.module.css`
   - `grep "color-background-alt" src/components/public/HowItWorks/HowItWorks.module.css`

3. **STEP-03** : Animation useInView avec stagger 100ms
   - `grep "useInView" src/components/public/HowItWorks/HowItWorks.tsx`
   - `grep "index \* 0.1" src/components/public/HowItWorks/HowItWorks.tsx`
   - `grep "useReducedMotion" src/components/public/HowItWorks/HowItWorks.tsx`

4. **Build** : `npm run build` sans erreur + `npx tsc --noEmit` sans erreur
5. **Tests** : `npm test` — suite complete verte
</verification>

<success_criteria>
- La section "Comment ca marche" est rendue dans page.tsx avec 3 cartes sur fond beige #F8F4EE
- Les cartes sont en colonne unique sur mobile et en grille 3 colonnes a partir de 640px
- Les cartes s'animent au scroll (fade-in + translateY, stagger 100ms) via useInView
- prefers-reduced-motion est respecte (duration: 0, delay: 0, y: 0)
- Le build production passe sans erreur
- Tous les tests passent (Header + Hero + HowItWorks)
- Aucun commentaire placeholder "Phase 3" ne reste dans page.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/03-howitworks-assemblage/03-01-SUMMARY.md`
</output>
