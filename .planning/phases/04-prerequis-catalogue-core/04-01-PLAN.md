---
phase: 04-prerequis-catalogue-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/components/public/Catalogue/ProductCard.tsx
  - src/components/public/Catalogue/ProductCard.module.css
  - src/components/public/Catalogue/ProductCardSkeleton.tsx
  - src/components/public/Catalogue/ProductCardSkeleton.module.css
  - src/__tests__/ProductCard.test.tsx
  - src/__tests__/ProductCardSkeleton.test.tsx
  - src/__tests__/nextconfig.test.ts
autonomous: true
requirements:
  - TECH-01
  - CAT-01
  - CAT-03

must_haves:
  truths:
    - "next/image peut charger des URLs Supabase Storage sans erreur 400"
    - "Une card produit affiche l'image, le nom et le prix formate d'un canape"
    - "Un skeleton loading a les memes dimensions qu'une vraie card avec animation shimmer"
  artifacts:
    - path: "next.config.ts"
      provides: "remotePatterns Supabase Storage"
      contains: "**.supabase.co"
    - path: "src/components/public/Catalogue/ProductCard.tsx"
      provides: "Card produit presentationnelle"
      exports: ["ProductCard"]
    - path: "src/components/public/Catalogue/ProductCard.module.css"
      provides: "Styles card tonal layering"
      contains: ".card"
    - path: "src/components/public/Catalogue/ProductCardSkeleton.tsx"
      provides: "Skeleton placeholder loading"
      exports: ["ProductCardSkeleton", "CatalogueSkeletonGrid"]
    - path: "src/components/public/Catalogue/ProductCardSkeleton.module.css"
      provides: "Animation shimmer CSS"
      contains: "@keyframes shimmer"
  key_links:
    - from: "ProductCard.tsx"
      to: "src/types/database.ts"
      via: "import ModelWithImages, ModelImage"
      pattern: "import.*ModelWithImages.*from.*@/types/database"
    - from: "ProductCard.tsx"
      to: "next/image"
      via: "Image component avec fill + sizes"
      pattern: "import Image from.*next/image"
    - from: "next.config.ts"
      to: "Supabase Storage"
      via: "remotePatterns hostname"
      pattern: "hostname.*supabase\\.co"
---

<objective>
Configurer le prerequis bloquant next/image pour les images Supabase, puis creer les composants presentationnels ProductCard et ProductCardSkeleton qui seront consommes par le Plan 02.

Purpose: TECH-01 est un prerequis absolu — aucune image produit ne s'affiche sans remotePatterns. ProductCard et ProductCardSkeleton sont les briques de base du catalogue.
Output: next.config.ts configure, ProductCard.tsx + ProductCardSkeleton.tsx prets a etre importes par CatalogueClient.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-prerequis-catalogue-core/04-CONTEXT.md
@.planning/phases/04-prerequis-catalogue-core/04-UI-SPEC.md
@.planning/phases/04-prerequis-catalogue-core/04-RESEARCH.md

<interfaces>
<!-- Types existants dans le projet que les taches doivent utiliser -->

From src/types/database.ts (lignes 103-109):
```typescript
// model_images Row
{
  id: string
  image_url: string
  model_id: string
  sort_order: number
  view_type: string
}
```

From src/types/database.ts (lignes 135-146):
```typescript
// models Row
{
  created_at: string
  description: string | null
  dimensions: string | null
  id: string
  is_active: boolean
  name: string
  price: number
  shopify_url: string | null
  slug: string
}
```

From src/types/database.ts (lignes 204-207):
```typescript
export type Model = Database['public']['Tables']['models']['Row']
export type ModelImage = Database['public']['Tables']['model_images']['Row']
export type ModelWithImages = Model & {
  model_images: ModelImage[]
}
```

From src/app/globals.css (tokens utilises):
```css
--color-primary: #E49400;
--color-text: #1D1D1B;
--color-muted: #888888;
--surface-container-low: #F6F3EF;
--surface-container: #F0EDEA;
--surface-container-high: #EBE8E4;
--surface-container-highest: #E5E2DE;
--font-size-xl: 1.25rem;
--font-size-sm: 0.875rem;
--spacing-md: 1rem;
--spacing-sm: 0.5rem;
--spacing-xl: 2rem;
--transition-fast: 300ms ease;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config next.config.ts remotePatterns + tests fondation</name>
  <files>next.config.ts, src/__tests__/nextconfig.test.ts, src/__tests__/ProductCard.test.tsx, src/__tests__/ProductCardSkeleton.test.tsx</files>
  <read_first>
    - next.config.ts (etat actuel — objet vide)
    - vitest.config.ts (config test existante)
    - src/__tests__/setup.ts (setup existant)
    - src/types/database.ts (types Model, ModelImage, ModelWithImages — lignes 103-207)
  </read_first>
  <action>
**1. Configurer next.config.ts (per D-01, TECH-01):**

Remplacer le contenu de `next.config.ts` par :

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https' as const,
        hostname: '**.supabase.co',
        port: '',
        pathname: '/storage/v1/object/public/**',
      },
    ],
  },
};

export default nextConfig;
```

- `hostname: '**.supabase.co'` (double wildcard) couvre tout sous-domaine Supabase
- `pathname: '/storage/v1/object/public/**'` restreint aux buckets publics (securite)

**2. Creer le test next.config (TECH-01):**

Creer `src/__tests__/nextconfig.test.ts` :

```typescript
import { describe, it, expect } from 'vitest'
import nextConfig from '../../next.config'

describe('next.config.ts', () => {
  it('contient remotePatterns pour Supabase Storage', () => {
    const patterns = nextConfig.images?.remotePatterns
    expect(patterns).toBeDefined()
    expect(patterns).toHaveLength(1)

    const supabasePattern = patterns![0]
    expect(supabasePattern.protocol).toBe('https')
    expect(supabasePattern.hostname).toBe('**.supabase.co')
    expect(supabasePattern.pathname).toBe('/storage/v1/object/public/**')
  })
})
```

**3. Creer les test stubs ProductCard (CAT-01):**

Creer `src/__tests__/ProductCard.test.tsx` avec les tests suivants. Utiliser une fixture inline :

```typescript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { ProductCard } from '@/components/public/Catalogue/ProductCard'
import type { ModelWithImages } from '@/types/database'

// Mock next/image pour eviter erreur dans happy-dom
vi.mock('next/image', () => ({
  default: (props: Record<string, unknown>) => {
    const { fill, ...rest } = props
    return <img {...rest} data-fill={fill ? 'true' : undefined} />
  },
}))

const mockModel: ModelWithImages = {
  id: 'test-uuid-001',
  name: 'Milano',
  slug: 'milano',
  price: 1290,
  description: 'Canape 3 places design italien',
  dimensions: '220x90x85',
  is_active: true,
  shopify_url: null,
  created_at: '2026-01-01T00:00:00Z',
  model_images: [
    {
      id: 'img-001',
      model_id: 'test-uuid-001',
      image_url: 'https://test.supabase.co/storage/v1/object/public/model-photos/milano-3-4.jpg',
      view_type: '3/4',
      sort_order: 0,
    },
  ],
}

const mockModelNoImage: ModelWithImages = {
  ...mockModel,
  id: 'test-uuid-002',
  name: 'Oslo',
  slug: 'oslo',
  model_images: [],
}

describe('ProductCard', () => {
  it('affiche le nom du modele en uppercase', () => {
    render(<ProductCard model={mockModel} />)
    expect(screen.getByRole('heading', { level: 3 })).toHaveTextContent('Milano')
  })

  it('affiche le prix formate avec prefixe', () => {
    render(<ProductCard model={mockModel} />)
    expect(screen.getByText(/1\s?290\s?€/)).toBeInTheDocument()
    expect(screen.getByText(/partir de/i)).toBeInTheDocument()
  })

  it('affiche l image avec alt correct quand model_images non vide', () => {
    render(<ProductCard model={mockModel} />)
    const img = screen.getByAltText('Canape Milano')
    expect(img).toBeInTheDocument()
  })

  it('affiche le placeholder Sofa quand aucune image', () => {
    render(<ProductCard model={mockModelNoImage} />)
    expect(screen.queryByRole('img')).not.toBeInTheDocument()
  })

  it('affiche le CTA Configurer ce modele', () => {
    render(<ProductCard model={mockModel} />)
    const btn = screen.getByRole('button', { name: /configurer le modele milano/i })
    expect(btn).toBeInTheDocument()
  })

  it('affiche la description si presente', () => {
    render(<ProductCard model={mockModel} />)
    expect(screen.getByText('Canape 3 places design italien')).toBeInTheDocument()
  })
})
```

**4. Creer les test stubs ProductCardSkeleton (CAT-03):**

Creer `src/__tests__/ProductCardSkeleton.test.tsx` :

```typescript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'
import { CatalogueSkeletonGrid } from '@/components/public/Catalogue/ProductCardSkeleton'

describe('CatalogueSkeletonGrid', () => {
  it('rend 3 skeletons', () => {
    const { container } = render(<CatalogueSkeletonGrid />)
    const skeletons = container.querySelectorAll('[aria-hidden="true"]')
    expect(skeletons.length).toBe(3)
  })

  it('a aria-busy sur le container', () => {
    render(<CatalogueSkeletonGrid />)
    const section = screen.getByLabelText('Chargement du catalogue')
    expect(section).toHaveAttribute('aria-busy', 'true')
  })

  it('a id catalogue sur la section', () => {
    const { container } = render(<CatalogueSkeletonGrid />)
    expect(container.querySelector('#catalogue')).toBeInTheDocument()
  })
})
```

**Note:** Les tests echoueront (RED) car les composants n'existent pas encore. C'est voulu — Task 2 les fera passer au GREEN.
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts contient `hostname: '**.supabase.co'`
    - next.config.ts contient `pathname: '/storage/v1/object/public/**'`
    - src/__tests__/nextconfig.test.ts existe et importe nextConfig depuis `../../next.config`
    - src/__tests__/ProductCard.test.tsx existe et contient `mockModel` avec price: 1290
    - src/__tests__/ProductCardSkeleton.test.tsx existe et contient `CatalogueSkeletonGrid`
  </acceptance_criteria>
  <done>next.config.ts configure avec remotePatterns Supabase. 3 fichiers de test crees (nextconfig, ProductCard, ProductCardSkeleton) prets pour le cycle RED-GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Composants ProductCard et ProductCardSkeleton</name>
  <files>
    src/components/public/Catalogue/ProductCard.tsx,
    src/components/public/Catalogue/ProductCard.module.css,
    src/components/public/Catalogue/ProductCardSkeleton.tsx,
    src/components/public/Catalogue/ProductCardSkeleton.module.css
  </files>
  <read_first>
    - src/__tests__/ProductCard.test.tsx (tests a faire passer — source de verite)
    - src/__tests__/ProductCardSkeleton.test.tsx (tests a faire passer)
    - src/types/database.ts (types Model, ModelImage, ModelWithImages — lignes 188-207)
    - src/app/globals.css (tokens CSS — toutes les variables custom)
    - src/components/public/HowItWorks/HowItWorks.module.css (pattern CSS existant a suivre)
    - .planning/phases/04-prerequis-catalogue-core/04-UI-SPEC.md (contrat visuel)
  </read_first>
  <behavior>
    - ProductCard avec image: affiche img avec alt "Canape {name}", nom en h3, prix "a partir de 1 290 EUR", description, bouton CTA "Configurer ce modele" avec aria-label
    - ProductCard sans image: affiche placeholder avec icone Sofa (aria-hidden), pas d'element img
    - ProductCard prix: Intl.NumberFormat('fr-FR') + prefixe "a partir de " + suffixe " EUR"
    - ProductCard CTA: button type="button", aria-label="Configurer le modele {name}", callback onConfigure optionnel
    - ProductCard image selection: priorite view_type === '3/4', fallback model_images[0] (deja trie par sort_order)
    - CatalogueSkeletonGrid: rend 3 ProductCardSkeleton avec aria-hidden="true" chacun
    - CatalogueSkeletonGrid: section avec id="catalogue", aria-label="Chargement du catalogue", aria-busy="true"
  </behavior>
  <action>
**1. Creer le dossier `src/components/public/Catalogue/`**

**2. Creer `src/components/public/Catalogue/ProductCard.tsx` (per D-02, D-03, D-04, D-05, D-09, D-10):**

Composant presentationnel pur. Structure :

```typescript
import Image from 'next/image'
import { Sofa } from 'lucide-react'
import type { ModelWithImages, ModelImage } from '@/types/database'
import styles from './ProductCard.module.css'
```

Fonctions utilitaires internes (pas exportees) :
- `getPrimaryImage(model_images: ModelImage[]): string | null` — retourne `null` si array vide, cherche `view_type === '3/4'`, fallback `model_images[0].image_url`
- `formatPrice(price: number): string` — retourne `'a partir de ' + new Intl.NumberFormat('fr-FR').format(price) + ' \u20ac'`

Interface props :
```typescript
interface ProductCardProps {
  model: ModelWithImages
  onConfigure?: (model: ModelWithImages) => void
}
```

JSX structure :
- `<article className={styles.card}>` (conteneur principal)
  - `<div className={styles.imageWrapper}>` (position relative, aspect-ratio 4/5, overflow hidden)
    - Si image: `<Image src={imageUrl} alt={`Canape ${model.name}`} fill style={{ objectFit: 'cover' }} sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw" className={styles.image} />`
    - Si pas d'image: `<div className={styles.imagePlaceholder}><Sofa size={48} strokeWidth={1.5} aria-hidden="true" className={styles.placeholderIcon} /></div>`
  - `<div className={styles.body}>` (padding 16px)
    - `<div className={styles.metaRow}>` (flex row, justify-between, align-start)
      - `<div className={styles.nameGroup}>`
        - `<h3 className={styles.name}>{model.name}</h3>`
        - Si description: `<p className={styles.description}>{model.description}</p>`
      - `<span className={styles.price}>{formatPrice(model.price)}</span>`
    - `<button type="button" className={styles.cta} onClick={() => onConfigure?.(model)} aria-label={`Configurer le modele ${model.name}`}>Configurer ce modele</button>`

Export nomme : `export function ProductCard(...)`

**3. Creer `src/components/public/Catalogue/ProductCard.module.css` (per D-02, D-03, D-04, D-05):**

```css
/* Card — tonal layering, pas de bordures per D-04 */
.card {
  background-color: var(--surface-container-low); /* #F6F3EF */
  border-radius: 0; /* design editorial bord-a-bord per D-02 */
  overflow: hidden;
  cursor: pointer;
  transition: background-color var(--transition-fast); /* 300ms */
}

.card:hover {
  background-color: var(--surface-container-high); /* #EBE8E4 per D-04 */
}

/* Image wrapper — aspect 4/5 portrait per D-02 */
.imageWrapper {
  position: relative;
  aspect-ratio: 4 / 5;
  overflow: hidden;
}

.image {
  transition: transform 700ms ease; /* per D-05 */
}

.card:hover .image {
  transform: scale(1.05); /* per D-05 */
}

/* Placeholder quand aucune image per D-10 */
.imagePlaceholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: var(--surface-container); /* #F0EDEA */
}

.placeholderIcon {
  color: var(--color-muted); /* #888888 */
}

/* Corps card — padding 16px per UI-SPEC */
.body {
  padding: var(--spacing-md); /* 16px */
}

/* Row nom/prix — flex per D-02 */
.metaRow {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: var(--spacing-sm); /* 8px */
}

.nameGroup {
  flex: 1;
  min-width: 0; /* pour text-overflow */
}

/* Nom produit — Montserrat 700, 20px, uppercase per D-02 */
.name {
  font-size: var(--font-size-xl); /* 1.25rem = 20px */
  font-weight: 700;
  line-height: 1.2;
  color: var(--color-text); /* #1D1D1B */
  text-transform: uppercase;
  letter-spacing: -0.01em;
}

/* Description courte — 14px, muted, ellipsis per UI-SPEC */
.description {
  font-size: var(--font-size-sm); /* 0.875rem = 14px */
  font-weight: 400;
  line-height: 1.5;
  color: var(--color-muted); /* #888888 */
  margin-top: var(--spacing-xs); /* 4px */
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

/* Prix — 20px, primary bold, aligne droite per D-02 */
.price {
  font-size: var(--font-size-xl); /* 1.25rem = 20px */
  font-weight: 700;
  color: var(--color-primary); /* #E49400 */
  white-space: nowrap;
  flex-shrink: 0;
}

/* CTA pleine largeur per D-03 */
.cta {
  display: block;
  width: 100%;
  margin-top: var(--spacing-md); /* 16px */
  padding: 12px var(--spacing-md);
  min-height: 44px; /* touch target accessibilite */
  background: transparent;
  border: 1px solid rgba(228, 148, 0, 0.2); /* primary 20% opacity per D-03 */
  color: var(--color-text);
  font-family: inherit;
  font-size: var(--font-size-sm); /* 14px */
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  cursor: pointer;
  transition: background-color var(--transition-fast), color var(--transition-fast), border-color var(--transition-fast);
}

.cta:hover {
  background-color: var(--color-primary); /* #E49400 per D-03 */
  color: #FFFFFF;
  border-color: var(--color-primary);
}
```

**4. Creer `src/components/public/Catalogue/ProductCardSkeleton.tsx` (per D-13, CAT-03):**

```typescript
import styles from './ProductCardSkeleton.module.css'

function ProductCardSkeleton() {
  return (
    <div className={styles.card} aria-hidden="true">
      <div className={`${styles.imageZone} ${styles.shimmer}`} />
      <div className={styles.body}>
        <div className={styles.row}>
          <div className={`${styles.lineName} ${styles.shimmer}`} />
          <div className={`${styles.linePrice} ${styles.shimmer}`} />
        </div>
        <div className={`${styles.lineDesc} ${styles.shimmer}`} />
        <div className={`${styles.lineCta} ${styles.shimmer}`} />
      </div>
    </div>
  )
}

export function CatalogueSkeletonGrid() {
  return (
    <section id="catalogue" aria-label="Chargement du catalogue" aria-busy="true" className={styles.section}>
      <div className={styles.container}>
        <div className={styles.grid}>
          <ProductCardSkeleton />
          <ProductCardSkeleton />
          <ProductCardSkeleton />
        </div>
      </div>
    </section>
  )
}
```

Export nomme : `CatalogueSkeletonGrid` (et `ProductCardSkeleton` si besoin d'import individuel).

**5. Creer `src/components/public/Catalogue/ProductCardSkeleton.module.css`:**

```css
@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}

.shimmer {
  background: linear-gradient(
    90deg,
    var(--surface-container) 0%,
    var(--surface-container-highest) 50%,
    var(--surface-container) 100%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s ease-in-out infinite;
}

.card {
  background-color: var(--surface-container-low);
  overflow: hidden;
}

.imageZone {
  aspect-ratio: 4 / 5;
  width: 100%;
}

.body {
  padding: var(--spacing-md);
}

.row {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: var(--spacing-sm);
}

.lineName {
  width: 60%;
  height: 20px;
  border-radius: var(--radius-sm);
}

.linePrice {
  width: 40%;
  height: 20px;
  border-radius: var(--radius-sm);
}

.lineDesc {
  width: 80%;
  height: 14px;
  border-radius: var(--radius-sm);
  margin-top: var(--spacing-sm);
}

.lineCta {
  width: 100%;
  height: 44px;
  margin-top: var(--spacing-md);
}

/* Section skeleton — memes styles que la section catalogue */
.section {
  background-color: var(--color-background);
  padding: var(--spacing-section) var(--container-padding-mobile);
}

.container {
  max-width: var(--container-max);
  margin: 0 auto;
}

.grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--spacing-xl);
}

@media (min-width: 640px) {
  .grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1024px) {
  .grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (min-width: 1280px) {
  .section {
    padding: var(--spacing-section) var(--container-padding-large);
  }
}
```

**6. Lancer les tests et verifier qu'ils passent au GREEN.**
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/public/Catalogue/ProductCard.tsx exporte `ProductCard`
    - src/components/public/Catalogue/ProductCard.tsx contient `import Image from 'next/image'`
    - src/components/public/Catalogue/ProductCard.tsx contient `import { Sofa } from 'lucide-react'`
    - src/components/public/Catalogue/ProductCard.tsx contient `import type { ModelWithImages, ModelImage } from '@/types/database'`
    - src/components/public/Catalogue/ProductCard.tsx contient `Intl.NumberFormat('fr-FR')`
    - src/components/public/Catalogue/ProductCard.tsx contient `view_type === '3/4'`
    - src/components/public/Catalogue/ProductCard.tsx contient `aria-label` avec "Configurer le modele"
    - src/components/public/Catalogue/ProductCard.module.css contient `.card` avec `background-color: var(--surface-container-low)`
    - src/components/public/Catalogue/ProductCard.module.css contient `.card:hover .image` avec `transform: scale(1.05)`
    - src/components/public/Catalogue/ProductCard.module.css contient `.cta:hover` avec `background-color: var(--color-primary)`
    - src/components/public/Catalogue/ProductCardSkeleton.tsx exporte `CatalogueSkeletonGrid`
    - src/components/public/Catalogue/ProductCardSkeleton.tsx contient `aria-busy="true"`
    - src/components/public/Catalogue/ProductCardSkeleton.tsx contient `id="catalogue"`
    - src/components/public/Catalogue/ProductCardSkeleton.module.css contient `@keyframes shimmer`
    - `npx vitest run` passe sans erreur (tous tests GREEN)
    - `npx tsc --noEmit` passe sans erreur TypeScript
  </acceptance_criteria>
  <done>ProductCard affiche image (priorite 3/4), nom uppercase, prix "a partir de X EUR", description, CTA avec aria-label. ProductCardSkeleton affiche 3 skeletons avec animation shimmer CSS. Tous les tests passent.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — tous les tests passent (nextconfig, ProductCard, ProductCardSkeleton)
- `npx tsc --noEmit` — zero erreur TypeScript
- `npm run build` — build reussit sans erreur liee aux remotePatterns
</verification>

<success_criteria>
- next.config.ts configure avec remotePatterns `**.supabase.co` (TECH-01)
- ProductCard affiche image, nom, prix et CTA pour un ModelWithImages (CAT-01)
- CatalogueSkeletonGrid rend 3 skeletons avec shimmer et aria-busy (CAT-03)
- Tous les tests vitest passent
- Zero erreur TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/04-prerequis-catalogue-core/04-01-SUMMARY.md`
</output>
