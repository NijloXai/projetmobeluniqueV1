---
phase: 04-prerequis-catalogue-core
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/public/Catalogue/CatalogueSection.tsx
  - src/components/public/Catalogue/CatalogueSection.module.css
  - src/components/public/Catalogue/CatalogueClient.tsx
  - src/app/page.tsx
  - src/__tests__/CatalogueClient.test.tsx
autonomous: true
requirements:
  - CAT-02

must_haves:
  truths:
    - "Les canapes actifs depuis Supabase sont visibles en grille responsive sur la page publique"
    - "La grille affiche 1 colonne sur mobile, 2 colonnes sur tablette, 3 colonnes sur desktop"
    - "Un skeleton loading s'affiche pendant le chargement du catalogue via Suspense"
    - "Si aucun canape actif, le message 'Nos canapes arrivent bientot.' s'affiche"
    - "Le CTA Hero #catalogue scrolle vers la section catalogue"
  artifacts:
    - path: "src/components/public/Catalogue/CatalogueSection.tsx"
      provides: "Server Component async qui fetch Supabase"
      exports: ["CatalogueSection"]
    - path: "src/components/public/Catalogue/CatalogueSection.module.css"
      provides: "Styles section + grille responsive"
      contains: ".grid"
    - path: "src/components/public/Catalogue/CatalogueClient.tsx"
      provides: "Client Component wrapper avec etat vide"
      exports: ["CatalogueClient"]
    - path: "src/app/page.tsx"
      provides: "Integration Suspense + CatalogueSection"
      contains: "Suspense"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/public/Catalogue/CatalogueSection.tsx"
      via: "import + Suspense fallback"
      pattern: "Suspense.*fallback.*CatalogueSkeletonGrid"
    - from: "CatalogueSection.tsx"
      to: "src/lib/supabase/server.ts"
      via: "createClient() fetch direct"
      pattern: "createClient.*from.*@/lib/supabase/server"
    - from: "CatalogueSection.tsx"
      to: "CatalogueClient.tsx"
      via: "passe ModelWithImages[] en props"
      pattern: "CatalogueClient.*models="
    - from: "CatalogueClient.tsx"
      to: "src/components/public/Catalogue/ProductCard.tsx"
      via: "import ProductCard, rendu dans .map()"
      pattern: "ProductCard.*key=.*model="
    - from: "Hero CTA #catalogue"
      to: "CatalogueClient section"
      via: "id='catalogue' sur la section"
      pattern: "id=\"catalogue\""
---

<objective>
Creer CatalogueSection (async Server Component qui fetch Supabase), CatalogueClient (Client Component qui rend la grille de ProductCards), et integrer le tout dans page.tsx avec Suspense/skeleton.

Purpose: Connecter les composants presentationnels du Plan 01 aux donnees reelles Supabase et les afficher sur la page publique. Le catalogue est la piece centrale du milestone v8.0.
Output: Section catalogue fonctionnelle visible sur la page publique avec donnees reelles Supabase.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-prerequis-catalogue-core/04-CONTEXT.md
@.planning/phases/04-prerequis-catalogue-core/04-UI-SPEC.md
@.planning/phases/04-prerequis-catalogue-core/04-RESEARCH.md
@.planning/phases/04-prerequis-catalogue-core/04-01-SUMMARY.md

<interfaces>
<!-- Composants crees par Plan 01 que ce plan importe -->

From src/components/public/Catalogue/ProductCard.tsx:
```typescript
import type { ModelWithImages } from '@/types/database'

interface ProductCardProps {
  model: ModelWithImages
  onConfigure?: (model: ModelWithImages) => void
}

export function ProductCard({ model, onConfigure }: ProductCardProps): JSX.Element
```

From src/components/public/Catalogue/ProductCardSkeleton.tsx:
```typescript
export function CatalogueSkeletonGrid(): JSX.Element
// Rend 3 skeletons dans une section id="catalogue" aria-busy="true"
```

From src/types/database.ts:
```typescript
export type Model = Database['public']['Tables']['models']['Row']
// { id, name, slug, price, description, dimensions, is_active, shopify_url, created_at }

export type ModelImage = Database['public']['Tables']['model_images']['Row']
// { id, image_url, model_id, sort_order, view_type }

export type ModelWithImages = Model & { model_images: ModelImage[] }
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient<Database>>
```

From src/app/page.tsx (etat actuel):
```typescript
import { Header } from '@/components/public/Header/Header'
import { Hero } from '@/components/public/Hero/Hero'
import { HowItWorks } from '@/components/public/HowItWorks/HowItWorks'
import styles from './page.module.css'

export default function HomePage() {
  return (
    <div className={styles.page}>
      <Header />
      <main id="main-content" className={styles.main}>
        <Hero />
        <HowItWorks />
      </main>
    </div>
  )
}
```

From src/components/public/HowItWorks/HowItWorks.module.css (pattern section a suivre):
```css
.section {
  background-color: var(--color-background-alt);
  padding: var(--spacing-section) var(--container-padding-mobile);
}
.container {
  max-width: var(--container-max);
  margin: 0 auto;
}
.grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--spacing-xl);
}
@media (min-width: 640px) {
  .grid { grid-template-columns: repeat(3, 1fr); }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CatalogueSection + CatalogueClient + styles grille</name>
  <files>
    src/components/public/Catalogue/CatalogueSection.tsx,
    src/components/public/Catalogue/CatalogueSection.module.css,
    src/components/public/Catalogue/CatalogueClient.tsx,
    src/__tests__/CatalogueClient.test.tsx
  </files>
  <read_first>
    - src/components/public/Catalogue/ProductCard.tsx (composant a importer dans CatalogueClient)
    - src/components/public/Catalogue/ProductCardSkeleton.tsx (CatalogueSkeletonGrid pour Suspense fallback)
    - src/lib/supabase/server.ts (createClient — signature exacte)
    - src/types/database.ts (types Model, ModelImage, ModelWithImages — lignes 103-207)
    - src/app/globals.css (tokens CSS utilises)
    - src/components/public/HowItWorks/HowItWorks.module.css (pattern section a reproduire)
    - .planning/phases/04-prerequis-catalogue-core/04-UI-SPEC.md (contrat visuel)
  </read_first>
  <action>
**1. Creer le test CatalogueClient (CAT-02) — RED d'abord :**

Creer `src/__tests__/CatalogueClient.test.tsx` :

```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { CatalogueClient } from '@/components/public/Catalogue/CatalogueClient'
import type { ModelWithImages } from '@/types/database'

// Mock next/image
vi.mock('next/image', () => ({
  default: (props: Record<string, unknown>) => {
    const { fill, ...rest } = props
    return <img {...rest} data-fill={fill ? 'true' : undefined} />
  },
}))

const mockModels: ModelWithImages[] = [
  {
    id: 'test-uuid-001',
    name: 'Milano',
    slug: 'milano',
    price: 1290,
    description: 'Canape 3 places design italien',
    dimensions: '220x90x85',
    is_active: true,
    shopify_url: null,
    created_at: '2026-01-01T00:00:00Z',
    model_images: [
      {
        id: 'img-001',
        model_id: 'test-uuid-001',
        image_url: 'https://test.supabase.co/storage/v1/object/public/model-photos/milano.jpg',
        view_type: '3/4',
        sort_order: 0,
      },
    ],
  },
  {
    id: 'test-uuid-002',
    name: 'Oslo',
    slug: 'oslo',
    price: 1490,
    description: 'Canape scandinave',
    dimensions: '200x85x80',
    is_active: true,
    shopify_url: null,
    created_at: '2026-01-02T00:00:00Z',
    model_images: [],
  },
]

describe('CatalogueClient', () => {
  it('affiche la grille de ProductCards quand models non vide', () => {
    render(<CatalogueClient models={mockModels} />)
    expect(screen.getByRole('heading', { level: 2 })).toHaveTextContent('Nos Canapes')
    expect(screen.getAllByRole('article')).toHaveLength(2)
  })

  it('affiche le message etat vide quand models est vide', () => {
    render(<CatalogueClient models={[]} />)
    expect(screen.getByText(/canapes arrivent bientot/i)).toBeInTheDocument()
  })

  it('a id catalogue sur la section', () => {
    const { container } = render(<CatalogueClient models={mockModels} />)
    expect(container.querySelector('#catalogue')).toBeInTheDocument()
  })

  it('a aria-labelledby pointant vers le titre', () => {
    render(<CatalogueClient models={mockModels} />)
    const section = screen.getByRole('region', { name: /nos canapes/i })
      || document.getElementById('catalogue')
    expect(section).toBeInTheDocument()
  })

  it('affiche le sous-titre de section', () => {
    render(<CatalogueClient models={mockModels} />)
    expect(screen.getByText(/commencer la configuration/i)).toBeInTheDocument()
  })
})
```

**2. Creer `src/components/public/Catalogue/CatalogueClient.tsx` (per D-06, D-07, D-08, D-11, D-12) :**

```typescript
'use client'

import type { ModelWithImages } from '@/types/database'
import { ProductCard } from './ProductCard'
import styles from './CatalogueSection.module.css'

interface CatalogueClientProps {
  models: ModelWithImages[]
}

export function CatalogueClient({ models }: CatalogueClientProps) {
  if (models.length === 0) {
    return (
      <section id="catalogue" className={styles.section}>
        <div className={styles.container}>
          <p className={styles.emptyMessage}>Nos canapes arrivent bientot.</p>
        </div>
      </section>
    )
  }

  return (
    <section
      id="catalogue"
      className={styles.section}
      aria-labelledby="catalogue-title"
    >
      <div className={styles.container}>
        <div className={styles.sectionHeader}>
          <h2 id="catalogue-title" className={styles.sectionTitle}>
            Nos Canapes
          </h2>
          <p className={styles.sectionSubtitle}>
            Selectionnez une base pour commencer la configuration.
          </p>
        </div>
        <div className={styles.grid}>
          {models.map((model) => (
            <ProductCard
              key={model.id}
              model={model}
              onConfigure={undefined}
            />
          ))}
        </div>
      </div>
    </section>
  )
}
```

**Notes importantes :**
- `'use client'` obligatoire — ce composant sera interactif en Phase 5 (recherche) et Phase 6 (modal)
- Utilise les styles de `CatalogueSection.module.css` (partage entre section server et client) — un seul fichier CSS pour eviter la duplication
- `onConfigure={undefined}` — le callback sera connecte au modal en Phase 6
- Le message etat vide utilise le texte exact de D-11 avec accents : "Nos canapes arrivent bientot."

**3. Creer `src/components/public/Catalogue/CatalogueSection.tsx` (per D-12, pattern async Server Component) :**

```typescript
import { createClient } from '@/lib/supabase/server'
import { CatalogueClient } from './CatalogueClient'
import type { ModelWithImages } from '@/types/database'
import styles from './CatalogueSection.module.css'

export async function CatalogueSection() {
  const supabase = await createClient()

  const { data, error } = await supabase
    .from('models')
    .select('*, model_images(*)')
    .eq('is_active', true)
    .order('created_at', { ascending: false })

  if (error) {
    return (
      <section id="catalogue" className={styles.section}>
        <div className={styles.container}>
          <p className={styles.errorMessage}>
            Impossible de charger les produits. Veuillez rafraichir la page.
          </p>
        </div>
      </section>
    )
  }

  const models: ModelWithImages[] = (data ?? []).map((model) => ({
    ...model,
    model_images: (model.model_images ?? []).sort(
      (a: { sort_order: number }, b: { sort_order: number }) =>
        a.sort_order - b.sort_order
    ),
  }))

  return <CatalogueClient models={models} />
}
```

**Notes :**
- Fetch Supabase directement (pas via `/api/models`) — meme processus Node, aller-retour HTTP inutile
- Tri `model_images` par `sort_order` ascendant cote serveur
- En cas d'erreur : message francais centre (per UI-SPEC copywriting)
- `select('*, model_images(*)')` — join implicite Supabase, retourne `ModelWithImages`

**4. Creer `src/components/public/Catalogue/CatalogueSection.module.css` (per D-06, D-07, CAT-02) :**

Ce fichier est partage entre CatalogueSection et CatalogueClient (meme section HTML).

```css
/* === Section Catalogue — CatalogueSection.module.css === */
/* Phase 04 — prerequis-catalogue-core */

/* Section fond blanc per D-07 — alternance avec HowItWorks beige */
.section {
  background-color: var(--color-background); /* #FFFFFF */
  padding: var(--spacing-section) var(--container-padding-mobile); /* 112px 24px mobile */
}

/* Conteneur centre per pattern HowItWorks */
.container {
  max-width: var(--container-max); /* 1280px */
  margin: 0 auto;
}

/* En-tete section centre per D-06 */
.sectionHeader {
  text-align: center;
  margin-bottom: var(--spacing-2xl); /* 48px avant la grille */
}

/* Titre H2 per D-06 */
.sectionTitle {
  font-size: var(--font-size-3xl); /* 32px */
  font-weight: 700;
  line-height: 1.2;
  color: var(--color-text); /* #1D1D1B */
}

/* Sous-titre muted */
.sectionSubtitle {
  font-size: var(--font-size-base); /* 16px */
  font-weight: 400;
  line-height: 1.6;
  color: var(--color-muted); /* #888888 */
  margin-top: var(--spacing-md); /* 16px sous le H2 */
  max-width: 480px;
  margin-left: auto;
  margin-right: auto;
}

/* Grille responsive per CAT-02 — mobile first 1 col */
.grid {
  display: grid;
  grid-template-columns: 1fr; /* 1 colonne mobile */
  gap: var(--spacing-xl); /* 32px entre cards */
}

/* Etat vide per D-11 */
.emptyMessage {
  text-align: center;
  color: var(--color-muted); /* #888888 */
  font-size: var(--font-size-base);
  padding: var(--spacing-section) 0;
}

/* Erreur fetch */
.errorMessage {
  text-align: center;
  color: var(--color-muted);
  font-size: var(--font-size-base);
  padding: var(--spacing-section) 0;
}

/* === Responsive per CAT-02 === */

/* Tablet >= 640px : 2 colonnes */
@media (min-width: 640px) {
  .grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Desktop >= 1024px : 3 colonnes, gap 32px */
@media (min-width: 1024px) {
  .grid {
    grid-template-columns: repeat(3, 1fr);
  }

  .section {
    padding: var(--spacing-section) var(--container-padding-desktop); /* 112px 48px */
  }
}

/* Large >= 1280px : padding lateral plus genereux */
@media (min-width: 1280px) {
  .section {
    padding: var(--spacing-section) var(--container-padding-large); /* 112px 64px */
  }
}
```

**5. Lancer les tests CatalogueClient pour confirmer le GREEN.**
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/public/Catalogue/CatalogueClient.tsx contient `'use client'` en premiere ligne
    - src/components/public/Catalogue/CatalogueClient.tsx contient `import { ProductCard } from './ProductCard'`
    - src/components/public/Catalogue/CatalogueClient.tsx contient `id="catalogue"`
    - src/components/public/Catalogue/CatalogueClient.tsx contient `aria-labelledby="catalogue-title"`
    - src/components/public/Catalogue/CatalogueClient.tsx contient `Nos canapes arrivent bientot.` (etat vide D-11)
    - src/components/public/Catalogue/CatalogueSection.tsx contient `import { createClient } from '@/lib/supabase/server'`
    - src/components/public/Catalogue/CatalogueSection.tsx contient `.from('models')` et `.select('*, model_images(*)')`
    - src/components/public/Catalogue/CatalogueSection.tsx contient `.eq('is_active', true)`
    - src/components/public/Catalogue/CatalogueSection.tsx contient `Impossible de charger les produits`
    - src/components/public/Catalogue/CatalogueSection.module.css contient `grid-template-columns: 1fr` (mobile)
    - src/components/public/Catalogue/CatalogueSection.module.css contient `grid-template-columns: repeat(2, 1fr)` (tablet 640px)
    - src/components/public/Catalogue/CatalogueSection.module.css contient `grid-template-columns: repeat(3, 1fr)` (desktop 1024px)
    - src/__tests__/CatalogueClient.test.tsx existe et contient `CatalogueClient`
    - `npx vitest run` passe sans erreur (tous tests GREEN)
  </acceptance_criteria>
  <done>CatalogueSection fetch Supabase directement, CatalogueClient rend la grille responsive 1/2/3 colonnes avec ProductCards, etat vide et erreur geres. Tests passent.</done>
</task>

<task type="auto">
  <name>Task 2: Integration page.tsx avec Suspense et skeleton</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (etat actuel — Header + Hero + HowItWorks)
    - src/components/public/Catalogue/CatalogueSection.tsx (composant async a importer)
    - src/components/public/Catalogue/ProductCardSkeleton.tsx (CatalogueSkeletonGrid pour Suspense fallback)
    - src/app/page.module.css (styles page existants)
  </read_first>
  <action>
**Modifier `src/app/page.tsx` pour integrer le catalogue avec Suspense.**

Le fichier actuel :
```typescript
import type { Metadata } from 'next'
import { Header } from '@/components/public/Header/Header'
import { Hero } from '@/components/public/Hero/Hero'
import { HowItWorks } from '@/components/public/HowItWorks/HowItWorks'
import styles from './page.module.css'

export const metadata: Metadata = { ... }

export default function HomePage() {
  return (
    <div className={styles.page}>
      <Header />
      <main id="main-content" className={styles.main}>
        <Hero />
        <HowItWorks />
      </main>
    </div>
  )
}
```

Le modifier en :
```typescript
import { Suspense } from 'react'
import type { Metadata } from 'next'
import { Header } from '@/components/public/Header/Header'
import { Hero } from '@/components/public/Hero/Hero'
import { HowItWorks } from '@/components/public/HowItWorks/HowItWorks'
import { CatalogueSection } from '@/components/public/Catalogue/CatalogueSection'
import { CatalogueSkeletonGrid } from '@/components/public/Catalogue/ProductCardSkeleton'
import styles from './page.module.css'

export const metadata: Metadata = {
  title: 'Accueil | Mobel Unique',
  description: 'Visualisez votre canape personnalise dans le tissu de votre choix grace a notre configurateur IA. Livraison Paris et Ile-de-France.',
}

export default function HomePage() {
  return (
    <div className={styles.page}>
      <Header />
      <main id="main-content" className={styles.main}>
        <Hero />
        <HowItWorks />
        <Suspense fallback={<CatalogueSkeletonGrid />}>
          <CatalogueSection />
        </Suspense>
      </main>
    </div>
  )
}
```

**Points critiques :**
- `import { Suspense } from 'react'` — pas de `React.Suspense`
- `<Suspense fallback={<CatalogueSkeletonGrid />}>` enveloppe `<CatalogueSection />` — le skeleton s'affiche pendant le fetch async
- `page.tsx` reste un **composant synchrone** (pas `async`) — c'est `CatalogueSection` qui est async et suspend
- L'ordre est : Header > Hero > HowItWorks > Catalogue (apres la section beige)
- Le Hero CTA `#catalogue` pointe vers `id="catalogue"` sur la section du catalogue — le scroll smooth est deja configure dans globals.css
- Ne PAS mettre `async` sur `HomePage` — cela bloquerait le rendu de tout le reste pendant le fetch
- Conserver les metadata existantes avec les accents (le fichier existant a des accents dans la description)
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit --pretty 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contient `import { Suspense } from 'react'`
    - src/app/page.tsx contient `import { CatalogueSection } from '@/components/public/Catalogue/CatalogueSection'`
    - src/app/page.tsx contient `import { CatalogueSkeletonGrid } from '@/components/public/Catalogue/ProductCardSkeleton'`
    - src/app/page.tsx contient `<Suspense fallback={<CatalogueSkeletonGrid />}>`
    - src/app/page.tsx contient `<CatalogueSection />`
    - src/app/page.tsx ne contient PAS `async function HomePage` (doit rester synchrone)
    - `<CatalogueSection />` est place apres `<HowItWorks />`
    - `npx tsc --noEmit` passe sans erreur
    - `npx vitest run` passe sans erreur
    - `npm run build` reussit (si Supabase disponible)
  </acceptance_criteria>
  <done>page.tsx integre CatalogueSection dans un Suspense avec CatalogueSkeletonGrid en fallback. Le catalogue s'affiche apres HowItWorks. Le skeleton s'affiche pendant le fetch Supabase.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — tous les tests passent (4 fichiers : nextconfig, ProductCard, ProductCardSkeleton, CatalogueClient)
- `npx tsc --noEmit` — zero erreur TypeScript
- `npm run build` — build reussit sans erreur
- Verification manuelle : `npm run dev` → ouvrir http://localhost:3000 → la section catalogue est visible apres HowItWorks
- Verification manuelle : cliquer le CTA Hero → scroll smooth vers la section #catalogue
</verification>

<success_criteria>
- CatalogueSection fetch les modeles actifs depuis Supabase directement (pas via /api/models)
- CatalogueClient rend la grille responsive 1/2/3 colonnes avec les ProductCards (CAT-02)
- Le skeleton loading s'affiche via Suspense pendant le fetch async
- Le message etat vide s'affiche si aucun canape actif
- Le Hero CTA #catalogue scrolle vers la section catalogue
- Tous les tests passent, zero erreur TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/04-prerequis-catalogue-core/04-02-SUMMARY.md`
</output>
