---
phase: 13-nanobananaservice
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/app/api/admin/generate/route.ts
  - src/app/api/admin/generate-all/route.ts
  - src/app/api/simulate/route.ts
autonomous: true
requirements:
  - IA-04
  - IA-06

must_haves:
  truths:
    - "generate/route.ts exporte maxDuration = 60"
    - "generate-all/route.ts exporte maxDuration = 300"
    - "simulate/route.ts exporte maxDuration = 60"
    - "Une image simulate est redimensionnee a max 1024px avant envoi a Gemini"
    - "generate-all retourne un champ errors dans sa reponse JSON"
    - "simulate retourne 429 avec Retry-After apres 5 appels/minute de la meme IP"
    - "simulate retourne 422 pour IMAGE_SAFETY avec message francais"
    - "generate retourne 422 pour IMAGE_SAFETY et 504 pour timeout"
  artifacts:
    - path: "src/app/api/admin/generate/route.ts"
      provides: "Route generation single avec maxDuration + error handling IA"
      contains: "export const maxDuration = 60"
    - path: "src/app/api/admin/generate-all/route.ts"
      provides: "Route generation batch avec maxDuration + errors array"
      contains: "export const maxDuration = 300"
    - path: "src/app/api/simulate/route.ts"
      provides: "Route simulation avec maxDuration + rate-limit + resize + error handling IA"
      contains: "export const maxDuration = 60"
  key_links:
    - from: "src/app/api/simulate/route.ts"
      to: "sharp"
      via: "resize image avant envoi"
      pattern: "sharp.*resize.*1024"
    - from: "src/app/api/simulate/route.ts"
      to: "rateMap"
      via: "rate-limit IP en memoire"
      pattern: "rateMap\\.get"
    - from: "src/app/api/admin/generate-all/route.ts"
      to: "errors array"
      via: "erreur par angle dans la reponse"
      pattern: "errors.*push"
    - from: "src/app/api/admin/generate/route.ts"
      to: "ImageSafetyError"
      via: "import depuis nano-banana"
      pattern: "ImageSafetyError"
---

<objective>
Adapter les 3 routes consommatrices du service IA pour supporter le provider Gemini reel : maxDuration Vercel, rate-limit IP, resize image, tableaux d'erreurs, et gestion erreurs specifiques (IMAGE_SAFETY, timeout).

Purpose: Les routes doivent gerer la latence reelle de Gemini (5-30s vs ~5ms mock) et proteger la route publique contre les abus.
Output: 3 fichiers route.ts modifies avec maxDuration, rate-limit, resize, error handling.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-nanobananaservice/13-CONTEXT.md
@.planning/phases/13-nanobananaservice/13-RESEARCH.md
@.planning/phases/13-nanobananaservice/13-01-SUMMARY.md

<interfaces>
<!-- Key types and exports from Plan 01 that this plan consumes -->

From src/lib/ai/nano-banana.ts (created in Plan 01):
```typescript
export class ImageSafetyError extends Error {
  constructor() {
    super("Cette image n'a pas pu etre traitee (contenu non autorise). Essayez une autre photo.")
    this.name = 'ImageSafetyError'
  }
}

export class NanoBananaService implements IAService {
  async generate(request: GenerateRequest): Promise<GenerateResult>
  async addWatermark(imageBuffer: Buffer, text?: string): Promise<Buffer>
}
```

From src/lib/ai/index.ts (unchanged):
```typescript
export function getIAService(): IAService
export type { IAService, GenerateRequest, GenerateResult } from './types'
export { buildBackOfficePrompt, buildSimulatePrompt } from './prompts'
```

From src/lib/ai/types.ts (unchanged):
```typescript
export interface GenerateResult {
  imageBuffer: Buffer
  mimeType: 'image/jpeg' | 'image/png'
  extension: 'jpg' | 'png'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapter generate et generate-all (maxDuration + error handling + errors array)</name>
  <files>src/app/api/admin/generate/route.ts, src/app/api/admin/generate-all/route.ts</files>
  <read_first>
    - /Users/salah/Desktop/projetmobelunique/src/app/api/admin/generate/route.ts
    - /Users/salah/Desktop/projetmobelunique/src/app/api/admin/generate-all/route.ts
    - /Users/salah/Desktop/projetmobelunique/src/lib/ai/nano-banana.ts
  </read_first>
  <action>
**Modifications de `src/app/api/admin/generate/route.ts` :**

1. Ajouter en TOUTE PREMIERE LIGNE du fichier (avant les imports) :
```typescript
export const maxDuration = 60
```

2. Ajouter un import pour `ImageSafetyError` :
```typescript
import { ImageSafetyError } from '@/lib/ai/nano-banana'
```

3. Dans le bloc `catch (err)` existant (ligne ~164), REMPLACER le catch generique par un handling specifique :
```typescript
  } catch (err) {
    // Erreurs specifiques IA
    if (err instanceof ImageSafetyError) {
      return NextResponse.json(
        { error: err.message },
        { status: 422 }
      )
    }

    const message = err instanceof Error ? err.message : 'Erreur inconnue'

    // Timeout (AbortError ou message explicite) (per D-05)
    if (
      (err instanceof Error && err.name === 'AbortError') ||
      message.includes('trop de temps') ||
      message.includes('aborted')
    ) {
      return NextResponse.json(
        { error: 'La generation a pris trop de temps. Veuillez reessayer.' },
        { status: 504 }
      )
    }

    console.error('[POST /api/admin/generate] Erreur:', message)
    return NextResponse.json(
      { error: `Erreur lors de la generation : ${message}` },
      { status: 500 }
    )
  }
```

**Modifications de `src/app/api/admin/generate-all/route.ts` :**

1. Ajouter en TOUTE PREMIERE LIGNE du fichier (avant les imports) :
```typescript
export const maxDuration = 300
```

2. Ajouter un import pour `ImageSafetyError` :
```typescript
import { ImageSafetyError } from '@/lib/ai/nano-banana'
```

3. Ajouter une variable `errors` typee avant la boucle `for...of` (apres `const results = []`) :
```typescript
const errors: Array<{ view_type: string; reason: string }> = []
```

4. Dans la boucle `for (const modelImage of modelImages)`, envelopper l'appel `iaService.generate()` + upload + insert dans un try/catch interne. Quand une erreur survient pour un angle :
```typescript
try {
  // ... code existant de generation + upload + insert pour cet angle ...
} catch (angleErr) {
  const reason = angleErr instanceof Error ? angleErr.message : 'Erreur inconnue'
  console.error(
    `[POST /api/admin/generate-all] Echec pour ${modelImage.view_type}:`,
    reason
  )
  errors.push({ view_type: modelImage.view_type, reason })
  continue
}
```

Note: les `continue` existants apres `uploadError` et `insertError` doivent aussi pousser dans `errors` au lieu de juste `continue` :
- Apres uploadError : `errors.push({ view_type: modelImage.view_type, reason: uploadError.message }); continue`
- Apres insertError : `errors.push({ view_type: modelImage.view_type, reason: insertError.message }); continue`

5. Modifier la reponse finale pour inclure le champ `errors` (per D-09) :
```typescript
return NextResponse.json({
  generated: results,
  total: modelImages.length,
  success: results.length,
  errors,
})
```

6. Dans le catch externe, ajouter le meme handling IMAGE_SAFETY et timeout que generate :
```typescript
  } catch (err) {
    if (err instanceof ImageSafetyError) {
      return NextResponse.json(
        { error: err.message },
        { status: 422 }
      )
    }

    const message = err instanceof Error ? err.message : 'Erreur inconnue'
    console.error('[POST /api/admin/generate-all] Erreur:', message)
    return NextResponse.json(
      { error: `Erreur lors de la generation : ${message}` },
      { status: 500 }
    )
  }
```
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `generate/route.ts` ligne 1 contient `export const maxDuration = 60`
    - `generate/route.ts` contient `import { ImageSafetyError } from '@/lib/ai/nano-banana'`
    - `generate/route.ts` contient `err instanceof ImageSafetyError`
    - `generate/route.ts` contient `status: 422` et `status: 504`
    - `generate-all/route.ts` ligne 1 contient `export const maxDuration = 300`
    - `generate-all/route.ts` contient `const errors: Array<{ view_type: string; reason: string }>`
    - `generate-all/route.ts` contient `errors.push(`
    - `generate-all/route.ts` la reponse JSON contient `errors` dans l'objet retourne
    - `npx tsc --noEmit` passe sans erreur
  </acceptance_criteria>
  <done>
    generate/route.ts a maxDuration=60 et gere IMAGE_SAFETY (422) + timeout (504).
    generate-all/route.ts a maxDuration=300 et retourne errors array avec detail par angle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adapter simulate (maxDuration + rate-limit IP + resize image + error handling)</name>
  <files>src/app/api/simulate/route.ts</files>
  <read_first>
    - /Users/salah/Desktop/projetmobelunique/src/app/api/simulate/route.ts
    - /Users/salah/Desktop/projetmobelunique/src/lib/ai/nano-banana.ts
  </read_first>
  <action>
**Modifications de `src/app/api/simulate/route.ts` :**

1. Ajouter en TOUTE PREMIERE LIGNE du fichier (avant les imports) :
```typescript
export const maxDuration = 60
```

2. Ajouter les imports necessaires :
```typescript
import sharp from 'sharp'
import { ImageSafetyError } from '@/lib/ai/nano-banana'
```

3. Ajouter le systeme de rate-limit par IP en module-level (apres MAX_FILE_SIZE, AVANT la fonction POST). Code exact (per D-04) :
```typescript
// Rate-limit en memoire par IP — 5 appels/minute (D-04)
// Limitation connue : reset au cold start Vercel (Redis differe v12+)
const rateMap = new Map<string, { count: number; resetAt: number }>()
const RATE_LIMIT_MAX = 5
const RATE_WINDOW_MS = 60_000

function checkRateLimit(ip: string): { allowed: boolean; retryAfter: number } {
  const now = Date.now()
  const entry = rateMap.get(ip)
  if (!entry || now > entry.resetAt) {
    rateMap.set(ip, { count: 1, resetAt: now + RATE_WINDOW_MS })
    return { allowed: true, retryAfter: 0 }
  }
  if (entry.count >= RATE_LIMIT_MAX) {
    const retryAfter = Math.ceil((entry.resetAt - now) / 1000)
    return { allowed: false, retryAfter }
  }
  entry.count++
  return { allowed: true, retryAfter: 0 }
}
```

4. Au tout debut de la fonction `POST`, AVANT le parsing FormData, ajouter le check rate-limit :
```typescript
// Rate-limit par IP (per D-04, D-18 : pas de rate-limit sur admin)
const ip =
  request.headers.get('x-forwarded-for')?.split(',')[0]?.trim() ??
  request.headers.get('x-real-ip') ??
  '127.0.0.1'
const { allowed, retryAfter } = checkRateLimit(ip)
if (!allowed) {
  return NextResponse.json(
    { error: `Trop de demandes. Reessayez dans ${retryAfter} secondes.` },
    { status: 429, headers: { 'Retry-After': String(retryAfter) } }
  )
}
```

5. Modifier la construction du `sourceImageUrl` pour inclure le resize (per IA-04). REMPLACER les lignes :
```typescript
const imageBuffer = Buffer.from(await image.arrayBuffer())
const sourceImageUrl = `data:${image.type};base64,${imageBuffer.toString('base64')}`
```
PAR :
```typescript
// Resize systematique avant envoi Gemini — evite payload > 20 Mo (per IA-04)
const rawImageBuffer = Buffer.from(await image.arrayBuffer())
const resizedBuffer = await sharp(rawImageBuffer)
  .resize(1024, 1024, { fit: 'inside', withoutEnlargement: true })
  .jpeg({ quality: 80 })
  .toBuffer()
const sourceImageUrl = `data:image/jpeg;base64,${resizedBuffer.toString('base64')}`
```

6. Modifier le catch interne de `iaService.generate()` pour gerer IMAGE_SAFETY et timeout en plus du HEIC existant :
```typescript
    } catch (genErr) {
      const genMessage = genErr instanceof Error ? genErr.message : ''

      // Format HEIC non supporte
      if (
        genMessage.includes('compression format') ||
        genMessage.includes('unsupported image format')
      ) {
        return NextResponse.json(
          {
            error:
              'Ce format HEIC ne peut pas etre traite. Convertissez votre photo en JPEG et reessayez.',
          },
          { status: 422 }
        )
      }

      // IMAGE_SAFETY (per D-01)
      if (genErr instanceof ImageSafetyError) {
        return NextResponse.json(
          { error: genErr.message },
          { status: 422 }
        )
      }

      // Timeout (per D-05)
      if (
        (genErr instanceof Error && genErr.name === 'AbortError') ||
        genMessage.includes('trop de temps') ||
        genMessage.includes('aborted')
      ) {
        return NextResponse.json(
          { error: 'La generation a pris trop de temps. Veuillez reessayer.' },
          { status: 504 }
        )
      }

      throw genErr // Re-throw pour le catch externe
    }
```

**Points critiques :**
- Le `import sharp from 'sharp'` est nouveau dans simulate/route.ts (sharp est deja installe)
- La variable `imageBuffer` existante est renommee en `rawImageBuffer` pour eviter le conflit avec la variable `imageBuffer` utilisee plus loin dans le code (si necessaire, verifier le scope)
- Le rate-limit Map est module-level, pas dans la fonction POST
- Le rate-limit N'EST PAS applique sur les routes admin (per D-18) — seulement sur simulate
- Le header `Retry-After` est ajoute dans la reponse 429 (per D-04)
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `simulate/route.ts` ligne 1 contient `export const maxDuration = 60`
    - `simulate/route.ts` contient `import sharp from 'sharp'`
    - `simulate/route.ts` contient `import { ImageSafetyError } from '@/lib/ai/nano-banana'`
    - `simulate/route.ts` contient `const rateMap = new Map`
    - `simulate/route.ts` contient `RATE_LIMIT_MAX = 5`
    - `simulate/route.ts` contient `RATE_WINDOW_MS = 60_000`
    - `simulate/route.ts` contient `checkRateLimit(ip)`
    - `simulate/route.ts` contient `status: 429` et `Retry-After`
    - `simulate/route.ts` contient `sharp(rawImageBuffer).resize(1024, 1024`
    - `simulate/route.ts` contient `withoutEnlargement: true`
    - `simulate/route.ts` contient `err instanceof ImageSafetyError` ou `genErr instanceof ImageSafetyError`
    - `simulate/route.ts` contient `status: 504`
    - `npx tsc --noEmit` passe sans erreur
    - `npm run build` passe sans erreur
  </acceptance_criteria>
  <done>
    simulate/route.ts a maxDuration=60, rate-limit 5/min par IP avec Retry-After, resize image a 1024px, et gere IMAGE_SAFETY (422), timeout (504), HEIC (422). Build complet passe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client public -> /api/simulate | Input non-fiable (image utilisateur, IP spoofable) |
| Admin auth -> /api/admin/* | Input trusted apres requireAdmin() |
| Routes -> NanoBananaService | Delegation de confiance au service IA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Denial of Service | /api/simulate | mitigate | Rate-limit 5/min par IP via Map en memoire + resize image a 1024px pour reduire la charge Gemini |
| T-13-07 | Spoofing | x-forwarded-for header | accept | IP spoofable via header mais impact limite (route publique, taux 5/min deja faible). Redis en v12+ si necessaire |
| T-13-08 | Denial of Service | generate-all | mitigate | maxDuration=300 empeche les fonctions serverless de tourner indefiniment. Sequential for...of empeche le flood Gemini |
| T-13-09 | Tampering | Image upload simulate | mitigate | Validation taille 15 Mo existante + resize a 1024px + jpeg quality 80 = payload Gemini toujours < 2 Mo |
| T-13-10 | Information Disclosure | Error messages routes | mitigate | Messages d'erreur generiques en francais, pas de stack trace, pas de details Gemini internes exposes |
</threat_model>

<verification>
1. `npx tsc --noEmit` passe sans erreur
2. `npm run build` passe sans erreur
3. `grep 'maxDuration' src/app/api/admin/generate/route.ts` retourne `export const maxDuration = 60`
4. `grep 'maxDuration' src/app/api/admin/generate-all/route.ts` retourne `export const maxDuration = 300`
5. `grep 'maxDuration' src/app/api/simulate/route.ts` retourne `export const maxDuration = 60`
6. `grep 'rateMap' src/app/api/simulate/route.ts` confirme le rate-limiter
7. `grep 'resize' src/app/api/simulate/route.ts` confirme le resize 1024px
8. `grep 'errors' src/app/api/admin/generate-all/route.ts` confirme le tableau d'erreurs
</verification>

<success_criteria>
- Les 3 routes exportent maxDuration (60, 300, 60)
- simulate a un rate-limit 5/min par IP qui retourne 429 + Retry-After
- simulate resize les images a max 1024px avant envoi
- generate-all retourne un champ errors avec detail par angle
- generate et simulate gerent IMAGE_SAFETY (422) et timeout (504)
- TypeScript compile, build passe
</success_criteria>

<output>
After completion, create `.planning/phases/13-nanobananaservice/13-02-SUMMARY.md`
</output>
