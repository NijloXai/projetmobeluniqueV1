---
phase: 11-simulation-ia-upload-et-traitement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/simulate/route.ts
  - src/lib/ai/prompts.ts
autonomous: true
requirements:
  - SIM-01

must_haves:
  truths:
    - "L'API /api/simulate accepte des fichiers jusqu'a 15 Mo sans erreur 400"
    - "L'API fonctionne sans fabric_id (tissu optionnel) et retourne un JPEG"
    - "L'API retourne un message HEIC specifique si le codec n'est pas supporte"
    - "Le message d'erreur taille mentionne 15 Mo (pas 10 Mo)"
  artifacts:
    - path: "src/app/api/simulate/route.ts"
      provides: "Route POST avec MAX_FILE_SIZE=15Mo, fabric_id optionnel, HEIC error handling"
      contains: "15 * 1024 * 1024"
    - path: "src/lib/ai/prompts.ts"
      provides: "buildSimulatePrompt inchange — fonctionne avec fabricName='tissu original'"
  key_links:
    - from: "src/app/api/simulate/route.ts"
      to: "src/lib/ai/prompts.ts"
      via: "buildSimulatePrompt(model.name, fabricName)"
      pattern: "buildSimulatePrompt"
---

<objective>
Adapter la route API /api/simulate pour supporter le tissu optionnel (D-16) et les fichiers jusqu'a 15 Mo (D-10), avec gestion HEIC specifique.

Purpose: Preparer le backend pour l'upload utilisateur Phase 11 — la route existe mais requiert fabric_id obligatoire et limite a 10 Mo.
Output: Route /api/simulate modifiee, prete pour le client.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-simulation-ia-upload-et-traitement/11-CONTEXT.md
@.planning/phases/11-simulation-ia-upload-et-traitement/11-RESEARCH.md

<interfaces>
<!-- Route API existante a modifier -->
From src/app/api/simulate/route.ts:
```typescript
const MAX_FILE_SIZE = 10 * 1024 * 1024 // 10 Mo
// POST accepte FormData: image (File), model_id (string), fabric_id (string)
// fabric_id est actuellement REQUIS (retourne 400 si absent)
```

From src/lib/ai/prompts.ts:
```typescript
export function buildSimulatePrompt(modelName: string, fabricName: string): string
// Signature inchangee — accepte deja fabricName comme string
```

From src/lib/ai/index.ts:
```typescript
export function getIAService(): IAService
```

From src/lib/ai/types.ts:
```typescript
interface GenerateRequest {
  modelName: string
  fabricName: string
  viewType: string
  sourceImageUrl: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modifier route /api/simulate — fabric_id optionnel + MAX_FILE_SIZE 15 Mo</name>
  <files>src/app/api/simulate/route.ts</files>
  <read_first>
    - src/app/api/simulate/route.ts (etat actuel complet — 124 lignes)
    - src/lib/ai/prompts.ts (signature buildSimulatePrompt)
    - src/lib/ai/types.ts (interface GenerateRequest)
  </read_first>
  <action>
Modifier src/app/api/simulate/route.ts avec 3 changements precis :

1. **MAX_FILE_SIZE** — Ligne 5 : remplacer `10 * 1024 * 1024` par `15 * 1024 * 1024` et le commentaire `// 10 Mo` par `// 15 Mo (D-10)`. Mettre a jour le message d'erreur ligne 39 de `"L'image ne doit pas depasser 10 Mo."` a `"L'image ne doit pas depasser 15 Mo."`.

2. **fabric_id optionnel** — Supprimer le bloc de validation qui retourne 400 quand fabric_id est absent (lignes 50-55 actuelles : `if (!fabricId) { return NextResponse.json(...) }`). Remplacer la section de recuperation tissu (lignes 74-86) par un conditionnel :
```typescript
// Recuperer le tissu conditionnellement (D-16 : tissu optionnel)
let fabricName = 'tissu original'
if (fabricId) {
  const { data: fabric, error: fabricError } = await supabase
    .from('fabrics')
    .select('id, name')
    .eq('id', fabricId)
    .single()

  if (fabricError || !fabric) {
    return NextResponse.json(
      { error: 'Tissu introuvable.' },
      { status: 404 }
    )
  }
  fabricName = fabric.name
}
```
Et remplacer `fabric.name` par `fabricName` dans l'appel iaService.generate() ligne 96.

3. **HEIC error handling** — Entourer l'appel `iaService.generate()` + `iaService.addWatermark()` (lignes 94-105) d'un try/catch interne qui intercepte les erreurs contenant "compression format" ou "unsupported image format" :
```typescript
let resultBuffer: Buffer
try {
  const result = await iaService.generate({
    modelName: model.name,
    fabricName,
    viewType: 'simulation',
    sourceImageUrl,
  })
  resultBuffer = await iaService.addWatermark(
    result.imageBuffer,
    'MOBEL UNIQUE -- Apercu'
  )
} catch (genErr) {
  const genMessage = genErr instanceof Error ? genErr.message : ''
  if (genMessage.includes('compression format') || genMessage.includes('unsupported image format')) {
    return NextResponse.json(
      { error: 'Ce format HEIC ne peut pas etre traite. Convertissez votre photo en JPEG et reessayez.' },
      { status: 422 }
    )
  }
  throw genErr // Re-throw pour le catch externe
}
```
Puis utiliser `resultBuffer` dans le `new NextResponse(new Uint8Array(resultBuffer), ...)`.

Mettre a jour le JSDoc en haut du fichier : `fabric_id (string, optionnel)`.

Ne PAS modifier la signature de buildSimulatePrompt dans prompts.ts — elle fonctionne deja avec "tissu original" comme fabricName.
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/simulate/route.ts contient `const MAX_FILE_SIZE = 15 * 1024 * 1024`
    - src/app/api/simulate/route.ts contient `"L'image ne doit pas depasser 15 Mo."`
    - src/app/api/simulate/route.ts NE contient PAS le bloc `if (!fabricId) { return NextResponse.json`
    - src/app/api/simulate/route.ts contient `let fabricName = 'tissu original'`
    - src/app/api/simulate/route.ts contient `if (fabricId) {` (conditionnel, pas obligatoire)
    - src/app/api/simulate/route.ts contient `compression format` dans un catch
    - src/app/api/simulate/route.ts contient `status: 422`
    - Le JSDoc mentionne `optionnel` pour fabric_id
    - `npx tsc --noEmit` sort sans erreur
  </acceptance_criteria>
  <done>Route /api/simulate accepte fabric_id optionnel, limite a 15 Mo, retourne 422 pour HEIC non supporte. TypeScript compile sans erreur.</done>
</task>

<task type="auto">
  <name>Task 2: Verifier que buildSimulatePrompt fonctionne avec "tissu original"</name>
  <files>src/lib/ai/prompts.ts</files>
  <read_first>
    - src/lib/ai/prompts.ts (contenu actuel — 39 lignes)
  </read_first>
  <action>
Relire buildSimulatePrompt pour confirmer que la signature `(modelName: string, fabricName: string)` accepte `"tissu original"` comme fabricName sans modification. Le prompt genere sera : `Place the "Milano" sofa upholstered in "tissu original" fabric into the provided room photograph.`

Cela est correct et ne necessite aucune modification. Si le contenu est deja conforme, ne rien changer.

Si pour une raison quelconque le code a change depuis la lecture initiale, s'assurer que la fonction accepte bien une string libre comme fabricName (pas de validation enum interne).
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/ai/prompts.ts exporte `buildSimulatePrompt` avec signature `(modelName: string, fabricName: string): string`
    - La fonction ne contient PAS de validation/assertion sur fabricName (pas de throw, pas de if fabricName ===)
    - `npx tsc --noEmit` sort sans erreur
  </acceptance_criteria>
  <done>buildSimulatePrompt confirme compatible avec "tissu original" — aucune modification necessaire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/simulate | Upload fichier non fiable depuis le navigateur |
| /api/simulate -> Sharp | Fichier image potentiellement malicieux traite par Sharp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | /api/simulate FormData | mitigate | Validation server-side : file.size <= 15Mo, modelId non vide. fabric_id optionnel mais valide en BDD si present |
| T-11-02 | DoS | /api/simulate | mitigate | MAX_FILE_SIZE 15Mo cote serveur (pas seulement client). Buffer.from(arrayBuffer) limite par la taille du fichier valide |
| T-11-03 | Spoofing | MIME type HEIC | mitigate | Try/catch sur iaService.generate() — intercepte les erreurs Sharp pour HEIC non supporte, retourne 422 avec message explicite |
| T-11-04 | Info Disclosure | Error messages | mitigate | Les erreurs serveur IA sont wrappees dans un message generique, le message detaille est log cote serveur (console.error) mais pas expose au client |
</threat_model>

<verification>
```bash
cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit && npm run build
```
</verification>

<success_criteria>
- Route /api/simulate compile sans erreur TypeScript
- fabric_id est optionnel dans le FormData (pas de 400 si absent)
- MAX_FILE_SIZE = 15 * 1024 * 1024
- Erreur HEIC retourne 422 avec message francais specifique
- buildSimulatePrompt inchange et compatible avec "tissu original"
</success_criteria>

<output>
After completion, create `.planning/phases/11-simulation-ia-upload-et-traitement/11-01-SUMMARY.md`
</output>
