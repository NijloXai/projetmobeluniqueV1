---
phase: 12-simulation-ia-affichage-resultat-et-partage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/public/Catalogue/ConfiguratorModal.module.css
  - src/components/public/Catalogue/ConfiguratorModal.tsx
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "L'image resultat IA s'affiche dans le modal quand simulationState === 'done'"
    - "Un disclaimer 'Apercu genere par IA' est visible sous l'image"
    - "Le bouton Telecharger sauvegarde un fichier mobel-unique-simulation.jpg"
    - "Le bouton Partager ouvre Web Share API sur mobile ou WhatsApp sur desktop"
    - "Le bouton Commander est masque si shopify_url est null"
    - "Le bouton Essayer une autre photo reset a idle en preservant la config tissu"
    - "Le layout est responsive : 1 colonne mobile, 2 colonnes desktop"
  artifacts:
    - path: "src/components/public/Catalogue/ConfiguratorModal.module.css"
      provides: "Classes CSS resultat + 4 boutons + responsive mobile/desktop"
      contains: ".resultContainer"
    - path: "src/components/public/Catalogue/ConfiguratorModal.tsx"
      provides: "JSX etat done + handlers download/partage/reset"
      contains: "handleDownload"
  key_links:
    - from: "ConfiguratorModal.tsx"
      to: "resultBlobUrl"
      via: "img src={resultBlobUrl} dans etat done"
      pattern: "simulationState === 'done' && resultBlobUrl"
    - from: "handleDownload"
      to: "resultBlobUrl"
      via: "createElement('a') + download attribute"
      pattern: "a\\.download = 'mobel-unique-simulation\\.jpg'"
    - from: "handlePartager"
      to: "navigator.share / wa.me"
      via: "canShare check + fallback WhatsApp"
      pattern: "navigator\\.canShare"
    - from: "handleEssayerAutrePhoto"
      to: "setSimulationState('idle')"
      via: "reset state sans toucher selectedFabricId"
      pattern: "revokeObjectURL.*setResultBlobUrl\\(null\\)"
---

<objective>
Afficher le resultat de la simulation IA dans le modal configurateur et implementer les 4 boutons d'action post-resultat (telecharger, partager, commander, relancer).

Purpose: Completer le flux simulation IA de bout en bout — le stub vide `simulationState === 'done'` (Phase 11) devient une experience utilisateur complete avec actions concretes.
Output: ConfiguratorModal etendu avec rendu resultat, disclaimer, 4 boutons d'action, responsive mobile/desktop.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-simulation-ia-affichage-resultat-et-partage/12-CONTEXT.md
@.planning/phases/12-simulation-ia-affichage-resultat-et-partage/12-RESEARCH.md
@.planning/phases/12-simulation-ia-affichage-resultat-et-partage/12-UI-SPEC.md
@.planning/phases/11-simulation-ia-upload-et-traitement/11-02-SUMMARY.md

<interfaces>
<!-- Etat existant dans ConfiguratorModal.tsx — l'executeur doit comprendre ces interfaces avant de modifier -->

State machine (ligne 72-78):
```typescript
const [simulationState, setSimulationState] = useState<SimulationState>('idle')
const [selectedFile, setSelectedFile] = useState<File | null>(null)
const [previewUrl, setPreviewUrl] = useState<string | null>(null)
const [progress, setProgress] = useState(0)
const [progressStage, setProgressStage] = useState(0)
const [errorMessage, setErrorMessage] = useState<string | null>(null)
const [resultBlobUrl, setResultBlobUrl] = useState<string | null>(null)
```

Computed values (ligne 333-335):
```typescript
const selectedFabric = selectedFabricId
  ? eligibleFabrics.find(f => f.id === selectedFabricId) ?? null
  : null
```

Existing handlers (lignes 284-291):
```typescript
const handleBackToConfigurator = useCallback(() => {
  if (abortControllerRef.current) abortControllerRef.current.abort()
  stopProgressTimer()
  setModalStep('configurator')
  setSimulationState(selectedFile ? 'preview' : 'idle')
  setProgress(0)
  setProgressStage(0)
  setErrorMessage(null)
}, [stopProgressTimer, selectedFile])
```

Stub actuel dans JSX (ligne 680 — entre fin de `generating` et fin de `leftColumn`):
```typescript
// Etat done : VIDE — Phase 12 ajoute ici
```

Colonne droite actuelle (lignes 684-708) — titre fixe "Simulation" a remplacer conditionnellement pour etat done.

Tokens CSS disponibles dans globals.css:
```css
--color-primary: #E49400;
--color-muted: #888888;
--color-whatsapp: #25D366;
--color-whatsapp-hover: #1FB855;
--color-text: #1D1D1B;
--color-background: #FFFFFF;
--color-background-alt: #F8F4EE;
--font-size-sm: 0.875rem;
--font-size-base: 1rem;
--font-size-xl: 1.25rem;
--spacing-xs: 0.25rem;
--spacing-sm: 0.5rem;
--spacing-md: 1rem;
--spacing-xl: 2rem;
--radius-md: 8px;
--radius-lg: 12px;
```

Keyframe existant dans ConfiguratorModal.module.css (ligne 282):
```css
@keyframes imageFadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
```
NE PAS redeclarer ce keyframe — utiliser uniquement `animation: imageFadeIn 400ms ease` dans .resultImage.

Imports lucide-react existants (ligne 4):
```typescript
import { X } from 'lucide-react'
```
Ajouter Download, Share2, ExternalLink, RefreshCw a cet import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS — Classes resultat, disclaimer et 4 boutons d'action</name>
  <files>src/components/public/Catalogue/ConfiguratorModal.module.css</files>

  <read_first>
    - src/components/public/Catalogue/ConfiguratorModal.module.css (etat complet actuel, verifier keyframe imageFadeIn existant, classes existantes)
    - src/app/globals.css (tokens CSS disponibles)
    - .planning/phases/12-simulation-ia-affichage-resultat-et-partage/12-UI-SPEC.md (contrat visuel complet)
    - .planning/phases/12-simulation-ia-affichage-resultat-et-partage/12-RESEARCH.md (CSS code examples section)
  </read_first>

  <action>
Ajouter les classes CSS suivantes a la FIN du fichier `ConfiguratorModal.module.css`, AVANT la derniere media query `@media (min-width: 640px)` existante (ou apres toutes les classes existantes si la media query est inline). L'objectif est de grouper les classes Phase 12 ensemble.

**IMPORTANT :** Ne PAS redeclarer `@keyframes imageFadeIn` — il existe deja a la ligne 282 du CSS. La classe `.resultImage` doit utiliser `animation: imageFadeIn 400ms ease` (pas 200ms).

Classes a ajouter :

1. `.resultContainer` — container image resultat :
   - `position: relative; width: 100%; aspect-ratio: 4 / 3; border-radius: var(--radius-lg); overflow: hidden; background: var(--color-background-alt);`

2. `.resultImage` — image resultat avec fade-in 400ms (D-04) :
   - `width: 100%; height: 100%; object-fit: cover; animation: imageFadeIn 400ms ease;`

3. `.resultDisclaimer` — texte disclaimer IA (D-02) :
   - `font-size: var(--font-size-sm); font-weight: 400; color: var(--color-muted); font-style: italic; margin-top: var(--spacing-sm);`

4. `.resultSubtitle` — sous-titre dynamique dans colonne droite (D-03) :
   - `font-size: var(--font-size-base); font-weight: 400; color: var(--color-muted); margin-top: var(--spacing-xs);`

5. `.actionButtons` — conteneur flex pour les 4 boutons :
   - `display: flex; flex-direction: column; gap: var(--spacing-sm); margin-top: var(--spacing-xl);`

6. `.actionButtonsMobile` — boutons visibles uniquement sur mobile :
   - `display: flex; flex-direction: column; gap: var(--spacing-sm); margin-top: var(--spacing-xl);`

7. `.actionButtonsDesktop` — boutons visibles uniquement sur desktop :
   - `display: none;`

8. `.downloadButton` — CTA primaire "Telecharger" :
   - `display: flex; align-items: center; justify-content: center; gap: var(--spacing-xs); width: 100%; height: 48px; font-size: var(--font-size-sm); font-weight: 600; color: var(--color-text); background: var(--color-primary); border: none; border-radius: var(--radius-md); cursor: pointer; transition: opacity 150ms ease;`
   - `:hover` — `opacity: 0.9;`
   - `:focus-visible` — `outline: 2px solid var(--color-primary); outline-offset: 2px;`

9. `.shareButton` — bouton "Partager" vert WhatsApp :
   - `display: flex; align-items: center; justify-content: center; gap: var(--spacing-xs); width: 100%; height: 48px; font-size: var(--font-size-sm); font-weight: 600; color: #FFFFFF; background: var(--color-whatsapp); border: none; border-radius: var(--radius-md); cursor: pointer; transition: background-color 150ms ease;`
   - `:hover` — `background: var(--color-whatsapp-hover);`
   - `:focus-visible` — `outline: 2px solid var(--color-primary); outline-offset: 2px;`

10. `.orderButton` — bouton outline "Commander sur Shopify" :
    - `display: flex; align-items: center; justify-content: center; gap: var(--spacing-xs); width: 100%; height: 48px; font-size: var(--font-size-sm); font-weight: 600; color: var(--color-text); background: transparent; border: 1px solid var(--color-text); border-radius: var(--radius-md); cursor: pointer; text-decoration: none; transition: background-color 150ms ease, color 150ms ease;`
    - `:hover` — `background: var(--color-text); color: var(--color-background);`
    - `:focus-visible` — `outline: 2px solid var(--color-primary); outline-offset: 2px;`

11. `.retryPhotoButton` — bouton tertiaire "Essayer une autre photo" :
    - `display: flex; align-items: center; justify-content: center; gap: var(--spacing-xs); width: 100%; height: 44px; font-size: var(--font-size-sm); font-weight: 600; color: var(--color-muted); background: transparent; border: none; border-radius: var(--radius-md); cursor: pointer; transition: color 150ms ease;`
    - `:hover` — `color: var(--color-text);`
    - `:focus-visible` — `outline: 2px solid var(--color-primary); outline-offset: 2px;`

12. Media query responsive `@media (min-width: 640px)` — ajouter DANS le bloc media query existant (ou creer un nouveau bloc si necessaire, apres les classes Phase 12) :
    - `.actionButtonsMobile { display: none; }`
    - `.actionButtonsDesktop { display: flex; flex-direction: column; gap: var(--spacing-sm); margin-top: var(--spacing-xl); }`
    - `.resultContainer { aspect-ratio: auto; min-height: 300px; }`

Verifier qu'aucun `@keyframes imageFadeIn` n'est ajoute. Le keyframe existant a la ligne 282 suffit — seule la duree change via la classe `.resultImage`.
  </action>

  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - ConfiguratorModal.module.css contient la classe `.resultContainer` avec `aspect-ratio: 4 / 3`
    - ConfiguratorModal.module.css contient la classe `.resultImage` avec `animation: imageFadeIn 400ms ease`
    - ConfiguratorModal.module.css contient la classe `.resultDisclaimer` avec `font-style: italic`
    - ConfiguratorModal.module.css contient la classe `.downloadButton` avec `background: var(--color-primary)`
    - ConfiguratorModal.module.css contient la classe `.shareButton` avec `background: var(--color-whatsapp)`
    - ConfiguratorModal.module.css contient la classe `.orderButton` avec `border: 1px solid var(--color-text)`
    - ConfiguratorModal.module.css contient la classe `.retryPhotoButton` avec `color: var(--color-muted)`
    - ConfiguratorModal.module.css contient la classe `.actionButtonsMobile`
    - ConfiguratorModal.module.css contient la classe `.actionButtonsDesktop`
    - ConfiguratorModal.module.css contient `.actionButtonsMobile` avec `display: none` dans le media query `min-width: 640px`
    - ConfiguratorModal.module.css NE contient PAS de deuxieme declaration `@keyframes imageFadeIn` (une seule doit exister, ligne 282)
    - `npx tsc --noEmit` passe sans erreur
  </acceptance_criteria>

  <done>12 nouvelles classes CSS ajoutees, responsive mobile/desktop fonctionne, keyframe imageFadeIn non duplique, tsc passe.</done>
</task>

<task type="auto">
  <name>Task 2: TSX — Affichage resultat + handlers download/partage/reset</name>
  <files>src/components/public/Catalogue/ConfiguratorModal.tsx</files>

  <read_first>
    - src/components/public/Catalogue/ConfiguratorModal.tsx (etat complet actuel — lire TOUT le fichier)
    - src/components/public/Catalogue/ConfiguratorModal.module.css (verifier les nouvelles classes de Task 1 sont presentes)
    - .planning/phases/12-simulation-ia-affichage-resultat-et-partage/12-CONTEXT.md (decisions D-01 a D-10)
    - .planning/phases/12-simulation-ia-affichage-resultat-et-partage/12-RESEARCH.md (code examples complets pour download, partage, reset)
    - .planning/phases/12-simulation-ia-affichage-resultat-et-partage/12-UI-SPEC.md (copywriting exact, accessibilite, boutons)
  </read_first>

  <action>
Modifier `ConfiguratorModal.tsx` en 4 blocs distincts. NE PAS modifier les etats existants (idle, preview, error, generating) — uniquement ajouter l'etat `done` et ajuster la colonne droite.

### Bloc 1 : Import — ligne 4

Remplacer :
```typescript
import { X } from 'lucide-react'
```
Par :
```typescript
import { X, Download, Share2, ExternalLink, RefreshCw } from 'lucide-react'
```

### Bloc 2 : 3 nouveaux handlers — apres `handleReessayer` (ligne 275) et avant `handleGoToSimulation` (ligne 278)

Ajouter ces 3 fonctions `useCallback` :

```typescript
// Phase 12 — Download resultat JPEG (D-06)
const handleDownload = useCallback(() => {
  if (!resultBlobUrl) return
  const a = document.createElement('a')
  a.href = resultBlobUrl
  a.download = 'mobel-unique-simulation.jpg'
  document.body.appendChild(a)
  a.click()
  document.body.removeChild(a)
}, [resultBlobUrl])

// Phase 12 — Partage Web Share API avec fallback WhatsApp (D-07)
const handlePartager = useCallback(async () => {
  if (!resultBlobUrl || !model) return

  const shopifyUrl = model.shopify_url ?? 'https://mobelunique.fr'
  const message = `Regardez comment ce canape s'integre dans mon salon ! Visualise avec Mobel Unique \u2014 ${shopifyUrl}`

  // Tenter Web Share API avec fichier (mobile)
  if (typeof navigator?.canShare === 'function') {
    try {
      const response = await fetch(resultBlobUrl)
      const blob = await response.blob()
      const file = new File([blob], 'mobel-unique-simulation.jpg', { type: 'image/jpeg' })
      if (navigator.canShare({ files: [file] })) {
        await navigator.share({ files: [file], title: 'Ma simulation M\u00f6bel Unique' })
        return
      }
    } catch (err) {
      if (err instanceof Error && err.name === 'AbortError') return
      // Fall through to WhatsApp
    }
  }

  // Fallback WhatsApp (desktop ou navigateur sans support fichiers)
  window.open(
    `https://wa.me/?text=${encodeURIComponent(message)}`,
    '_blank',
    'noopener,noreferrer'
  )
}, [resultBlobUrl, model])

// Phase 12 — Reset vers idle pour essayer une autre photo (D-09)
const handleEssayerAutrePhoto = useCallback(() => {
  if (resultBlobUrl) URL.revokeObjectURL(resultBlobUrl)
  setResultBlobUrl(null)
  setSelectedFile(null)
  if (previewUrl) URL.revokeObjectURL(previewUrl)
  setPreviewUrl(null)
  setSimulationState('idle')
  setProgress(0)
  setProgressStage(0)
  setErrorMessage(null)
  // selectedFabricId et selectedAngle intentionnellement preserves (D-09)
}, [resultBlobUrl, previewUrl])
```

### Bloc 3 : JSX etat `done` dans colonne gauche — inserer apres le bloc `{simulationState === 'generating' && previewUrl && (...)}` (apres ligne 679), AVANT la fermeture de `</div>` de `leftColumn` (ligne 681)

```tsx
{/* Phase 12 — Etat done : resultat simulation (D-01) */}
{simulationState === 'done' && resultBlobUrl && (
  <>
    <div
      className={styles.resultContainer}
      aria-live="polite"
      aria-label="Simulation generee"
    >
      <img
        src={resultBlobUrl}
        alt={`Simulation IA de votre salon avec le canape ${model.name}`}
        className={styles.resultImage}
      />
    </div>
    <p className={styles.resultDisclaimer}>
      Apercu genere par IA &mdash; le rendu reel peut varier
    </p>
    {/* Boutons d'action mobile uniquement (< 640px) — D-08 */}
    <div className={styles.actionButtonsMobile} aria-hidden="false">
      <button type="button" className={styles.downloadButton} onClick={handleDownload} aria-label="Telecharger la simulation en JPEG">
        <Download size={16} aria-hidden="true" />
        Telecharger
      </button>
      <button type="button" className={styles.shareButton} onClick={handlePartager} aria-label="Partager l'image">
        <Share2 size={16} aria-hidden="true" />
        Partager
      </button>
      {model.shopify_url && (
        <a href={model.shopify_url} target="_blank" rel="noopener noreferrer" className={styles.orderButton} aria-label={`Commander le canape ${model.name} sur Shopify`}>
          <ExternalLink size={16} aria-hidden="true" />
          Commander sur Shopify
        </a>
      )}
      <button type="button" className={styles.retryPhotoButton} onClick={handleEssayerAutrePhoto} aria-label="Essayer avec une autre photo de salon">
        <RefreshCw size={14} aria-hidden="true" />
        Essayer une autre photo
      </button>
    </div>
  </>
)}
```

### Bloc 4 : Colonne droite — modifier le contenu EXISTANT de `<div className={styles.body}>` dans l'etape simulation (lignes 684-708)

Remplacer le contenu entre `<div className={styles.body}>` et `</div>` par un rendu conditionnel selon `simulationState === 'done'` :

```tsx
<div className={styles.body}>
  <button type="button" className={styles.backButton} onClick={handleBackToConfigurator}>
    &larr; Modifier la configuration
  </button>

  {simulationState === 'done' ? (
    <>
      <h2 id="modal-title" className={styles.simulationTitle}>Votre simulation</h2>
      <p className={styles.resultSubtitle}>
        {selectedFabric
          ? `${model.name} \u00d7 ${selectedFabric.name} dans votre salon`
          : 'Canape original dans votre salon'}
      </p>

      {/* Boutons d'action desktop uniquement (>= 640px) */}
      <div className={styles.actionButtonsDesktop} aria-hidden="false">
        <button type="button" className={styles.downloadButton} onClick={handleDownload} aria-label="Telecharger la simulation en JPEG">
          <Download size={16} aria-hidden="true" />
          Telecharger
        </button>
        <button type="button" className={styles.shareButton} onClick={handlePartager} aria-label="Partager l'image">
          <Share2 size={16} aria-hidden="true" />
          Partager
        </button>
        {model.shopify_url && (
          <a href={model.shopify_url} target="_blank" rel="noopener noreferrer" className={styles.orderButton} aria-label={`Commander le canape ${model.name} sur Shopify`}>
            <ExternalLink size={16} aria-hidden="true" />
            Commander sur Shopify
          </a>
        )}
        <button type="button" className={styles.retryPhotoButton} onClick={handleEssayerAutrePhoto} aria-label="Essayer avec une autre photo de salon">
          <RefreshCw size={14} aria-hidden="true" />
          Essayer une autre photo
        </button>
      </div>
    </>
  ) : (
    <>
      <h2 id="modal-title" className={styles.simulationTitle}>Simulation</h2>

      <p className={styles.uploadExplainer}>
        Prenez votre salon en photo, on y place votre canap&eacute;
      </p>

      {/* Bandeau rappel config (D-05) */}
      <div className={styles.configRecap}>
        {selectedFabric?.swatch_url ? (
          <img src={selectedFabric.swatch_url} alt="" className={styles.configRecapSwatch} />
        ) : (
          <div className={styles.configRecapSwatch} style={{ background: 'var(--surface-container)' }} />
        )}
        <span className={styles.configRecapText}>
          {selectedFabric ? `${model.name} \u00d7 ${selectedFabric.name}` : `Canap\u00e9 original \u00b7 Aucun tissu s\u00e9lectionn\u00e9`}
        </span>
        <button type="button" className={styles.configRecapLink} onClick={handleBackToConfigurator}>
          Modifier
        </button>
      </div>
    </>
  )}
</div>
```

**Note importante sur TypeScript :** `model.shopify_url` est de type `string | null` dans les types database. Le conditionnel `{model.shopify_url && (...)}` gere correctement le cas null. NE PAS utiliser `as string` ni `!` — le narrowing natif suffit.

**Note sur `aria-hidden` des groupes de boutons dupliques :** Les classes CSS `.actionButtonsMobile` et `.actionButtonsDesktop` masquent visuellement un groupe par breakpoint. Les deux groupes restent dans le DOM. L'attribut `aria-hidden="false"` est explicite sur les deux pour que les lecteurs d'ecran voient les deux (le groupe CSS-masque est toujours accessible en DOM). C'est acceptable car les boutons sont identiques — pas de confusion pour l'utilisateur.
  </action>

  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - ConfiguratorModal.tsx contient `import { X, Download, Share2, ExternalLink, RefreshCw } from 'lucide-react'`
    - ConfiguratorModal.tsx contient `const handleDownload = useCallback(`
    - ConfiguratorModal.tsx contient `a.download = 'mobel-unique-simulation.jpg'`
    - ConfiguratorModal.tsx contient `const handlePartager = useCallback(async (`
    - ConfiguratorModal.tsx contient `navigator.canShare({ files: [file] })`
    - ConfiguratorModal.tsx contient `window.open(\`https://wa.me/?text=`
    - ConfiguratorModal.tsx contient `'noopener,noreferrer'`
    - ConfiguratorModal.tsx contient `const handleEssayerAutrePhoto = useCallback(`
    - ConfiguratorModal.tsx contient `URL.revokeObjectURL(resultBlobUrl)` dans handleEssayerAutrePhoto (avant setResultBlobUrl(null))
    - ConfiguratorModal.tsx contient `setSimulationState('idle')` dans handleEssayerAutrePhoto
    - ConfiguratorModal.tsx contient `simulationState === 'done' && resultBlobUrl &&`
    - ConfiguratorModal.tsx contient `alt={\`Simulation IA de votre salon avec le canape ${model.name}\`}`
    - ConfiguratorModal.tsx contient `Apercu genere par IA` (disclaimer D-02)
    - ConfiguratorModal.tsx contient `Votre simulation` (titre D-03)
    - ConfiguratorModal.tsx contient `dans votre salon` (sous-titre D-03)
    - ConfiguratorModal.tsx contient `styles.actionButtonsMobile`
    - ConfiguratorModal.tsx contient `styles.actionButtonsDesktop`
    - ConfiguratorModal.tsx contient `model.shopify_url &&` avant le rendu du bouton Commander (pas de href null)
    - ConfiguratorModal.tsx NE contient PAS de `any` (TypeScript strict)
    - `npx tsc --noEmit` passe sans erreur
    - `npm run build` passe sans erreur
  </acceptance_criteria>

  <done>Etat done complet dans ConfiguratorModal : image resultat avec fade 400ms, disclaimer, 4 boutons d'action (telecharger/partager/commander/relancer), responsive mobile/desktop, reset preservant la config, aucune regression sur les etats existants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client blob URL | resultBlobUrl est un blob local — pas de donnee sensible reseau |
| window.open vers wa.me | Ouverture d'URL tierce depuis le client |
| navigator.share | API native du navigateur — interaction utilisateur requise |
| model.shopify_url | URL stockee en BDD Supabase — rendue comme href |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-12-01 | Spoofing | window.open('https://wa.me/...') | mitigate | `noopener,noreferrer` dans le 3e argument de window.open — empeche reverse tabnapping |
| T-12-02 | Tampering | model.shopify_url rendu comme href | mitigate | Conditionner le rendu : `model.shopify_url && <a href={model.shopify_url}>` — empeche href="null". Ajouter `rel="noopener noreferrer"` |
| T-12-03 | Info Disclosure | resultBlobUrl dans le DOM | accept | Blob URL locale, pas de donnee sensible. L'image est generee par le service IA qui applique deja un watermark |
| T-12-04 | Denial of Service | Fuite memoire blob URL sur reset | mitigate | `URL.revokeObjectURL(resultBlobUrl)` appele dans handleEssayerAutrePhoto AVANT setResultBlobUrl(null). Pattern identique aux 3 chemins existants Phase 11 |
</threat_model>

<verification>
1. `npx tsc --noEmit` — zero erreur TypeScript
2. `npm run build` — build production complet sans erreur
3. Verification manuelle : ouvrir le modal, lancer une simulation, verifier que l'image apparait avec fondu, que les 4 boutons sont visibles, que le download fonctionne, que "Essayer une autre photo" reset correctement
</verification>

<success_criteria>
- Build production vert (`npm run build` sans erreur)
- TypeScript strict (`npx tsc --noEmit` sans erreur)
- Etat `done` affiche l'image resultat avec animation 400ms
- Disclaimer "Apercu genere par IA" visible sous l'image
- Bouton Telecharger declenche le download d'un fichier `mobel-unique-simulation.jpg`
- Bouton Partager utilise Web Share API sur mobile, WhatsApp sur desktop
- Bouton Commander masque si `shopify_url === null`
- Bouton Essayer une autre photo reset a idle, config tissu preservee
- Layout responsive : 1 colonne mobile (boutons sous disclaimer), 2 colonnes desktop (boutons colonne droite)
- Zero `any` dans le code modifie
- Zero regression sur les etats idle/preview/generating/error
</success_criteria>

<output>
After completion, create `.planning/phases/12-simulation-ia-affichage-resultat-et-partage/12-01-SUMMARY.md`
</output>
