---
phase: 16-tests-e2e-corrections-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/app/api/simulate/route.ts
  - src/app/api/admin/models/[id]/route.ts
  - src/app/api/admin/fabrics/[id]/route.ts
  - src/app/api/admin/visuals/[id]/validate/route.ts
  - src/app/api/admin/visuals/[id]/publish/route.ts
  - src/app/api/admin/generate/route.ts
  - src/app/api/admin/generate-all/route.ts
  - src/app/api/admin/visuals/bulk-validate/route.ts
  - src/app/api/admin/visuals/bulk-publish/route.ts
  - src/app/api/admin/models/[id]/images/route.ts
  - src/app/api/admin/models/[id]/visuals/route.ts
  - src/lib/schemas.ts
  - src/lib/ai/index.ts
  - src/lib/ai/nano-banana.ts
  - src/app/admin/(protected)/produits/IAGenerationSection.tsx
  - src/components/public/Catalogue/ConfiguratorModal.tsx
  - src/components/public/Catalogue/ConfiguratorModal.module.css
  - src/components/public/Hero/__tests__/Hero.test.tsx
  - src/components/public/HowItWorks/__tests__/HowItWorks.test.tsx
  - src/__tests__/ConfiguratorModal.test.tsx
  - src/types/database.ts
  - package.json
  - knip.json
  - .gitignore
autonomous: true
requirements:
  - FIX-01

must_haves:
  truths:
    - "npm run build se termine sans erreur"
    - "npx tsc --noEmit se termine sans erreur (0 erreurs)"
    - "npx eslint src/ ne retourne aucune error (0 errors, warnings acceptables)"
    - "Les security headers HTTP sont configurees dans next.config.ts"
    - "La route simulate valide le type MIME avant traitement"
    - "Les routes admin avec :id valident le format UUID"
    - "Les routes POST admin utilisent des schemas Zod pour validation"
    - "Les dependances inutilisees (8 Radix + zustand + immer) sont supprimees de package.json"
  artifacts:
    - path: "next.config.ts"
      provides: "Security headers (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, CSP)"
      contains: "async headers()"
    - path: "src/lib/schemas.ts"
      provides: "Schemas Zod pour routes POST admin (generateSchema, generateAllSchema, bulkSchema)"
      exports: ["generateSchema", "generateAllSchema", "bulkSchema"]
  key_links:
    - from: "src/app/api/admin/generate/route.ts"
      to: "src/lib/schemas.ts"
      via: "import generateSchema"
      pattern: "generateSchema.safeParse"
    - from: "src/app/api/simulate/route.ts"
      to: "ALLOWED_MIME array"
      via: "image.type validation"
      pattern: "ALLOWED_MIME.includes"
---

<objective>
Corriger les findings de l'audit Phase 14 (AUDIT.md) : security headers critiques, validation inputs, dead code, ESLint errors, console.log, img vers Image, schemas Zod, et nettoyage dependances.

Purpose: Assurer un codebase propre et securise avant la mise en place des tests E2E. Les corrections sont prerequis pour un build et lint propres (SC-4).
Output: Codebase avec 0 erreur tsc, 0 error ESLint, security headers configures, dead code elimine, et dependances nettoyees.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-audit-code/AUDIT.md
@.planning/phases/16-tests-e2e-corrections-audit/16-CONTEXT.md
@.planning/phases/16-tests-e2e-corrections-audit/16-RESEARCH.md

<interfaces>
<!-- Schemas Zod existants dans src/lib/schemas.ts -->
<!-- L'executeur doit AJOUTER les nouveaux schemas apres les existants -->
<!-- NE PAS modifier les schemas existants (modelSchema, fabricSchema, etc.) -->

From src/lib/schemas.ts (existing exports):
```typescript
export const modelSchema = z.object({ ... })
export const modelImageSchema = z.object({ ... })
export const fabricSchema = z.object({ ... })
export const generatedVisualSchema = z.object({ ... })
export const modelWithImagesSchema = modelSchema.extend({ model_images: z.array(modelImageSchema) })
export const visualWithFabricSchema = generatedVisualSchema.extend({ fabric: fabricSchema })
export const createModelSchema = z.object({ ... })
export const updateModelSchema = createModelSchema.partial()
export const createFabricSchema = z.object({ ... })
export const updateFabricSchema = createFabricSchema.partial()
export type ModelInput = z.infer<typeof createModelSchema>
export type ModelUpdateInput = z.infer<typeof updateModelSchema>
export type FabricInput = z.infer<typeof createFabricSchema>
export type FabricUpdateInput = z.infer<typeof updateFabricSchema>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security headers + corrections securite routes (SEC-01/02/04/05/08/10/11)</name>
  <read_first>
    - next.config.ts
    - src/app/api/simulate/route.ts
    - src/app/api/admin/models/[id]/route.ts
    - src/app/api/admin/fabrics/[id]/route.ts
    - src/app/api/admin/visuals/[id]/validate/route.ts
    - src/app/api/admin/visuals/[id]/publish/route.ts
    - src/lib/ai/nano-banana.ts
    - .planning/phases/16-tests-e2e-corrections-audit/16-RESEARCH.md (Pattern 5: security headers, Code Examples section)
  </read_first>
  <files>
    next.config.ts,
    src/app/api/simulate/route.ts,
    src/app/api/admin/models/[id]/route.ts,
    src/app/api/admin/fabrics/[id]/route.ts,
    src/app/api/admin/visuals/[id]/validate/route.ts,
    src/app/api/admin/visuals/[id]/publish/route.ts,
    src/lib/ai/nano-banana.ts
  </files>
  <action>
**SEC-01 + SEC-10 + SEC-11 — Security headers dans next.config.ts (per D-01):**

Ajouter une fonction `async headers()` dans la config Next.js qui retourne les headers suivants pour `source: '/(.*)'` :

```typescript
async headers() {
  return [
    {
      source: '/(.*)',
      headers: [
        { key: 'X-Frame-Options', value: 'SAMEORIGIN' },
        { key: 'X-Content-Type-Options', value: 'nosniff' },
        { key: 'Referrer-Policy', value: 'strict-origin-when-cross-origin' },
        {
          key: 'Permissions-Policy',
          value: 'camera=(), microphone=(), geolocation=()',
        },
        {
          key: 'Content-Security-Policy',
          value: [
            "default-src 'self'",
            "script-src 'self' 'unsafe-eval' 'unsafe-inline'",
            "style-src 'self' 'unsafe-inline' https://fonts.googleapis.com",
            "font-src 'self' https://fonts.gstatic.com",
            "img-src 'self' https://*.supabase.co data: blob:",
            "connect-src 'self' https://*.supabase.co wss://*.supabase.co",
          ].join('; '),
        },
      ],
    },
  ]
},
```

Conserver le bloc `images.remotePatterns` existant intact.

**SEC-02 — Validation MIME dans simulate/route.ts (per D-01):**

Ajouter apres la verification de taille (`if (image.size > MAX_FILE_SIZE)`) et avant le rate-limit :

```typescript
const ALLOWED_MIME = ['image/jpeg', 'image/png', 'image/webp', 'image/gif']
if (!ALLOWED_MIME.includes(image.type)) {
  return NextResponse.json(
    { error: 'Format non supporte. Formats acceptes : JPEG, PNG, WebP, GIF.' },
    { status: 422 }
  )
}
```

**SEC-04 — Reduire seuil eviction rateMap (per D-01):**

Dans simulate/route.ts, changer `if (rateMap.size > 1000)` en `if (rateMap.size > 100)`.

**SEC-08 — Correction extraction IP dans simulate/route.ts (per D-01):**

Remplacer `.split(',')[0]?.trim()` par `.split(',').at(-1)?.trim()` pour prendre la derniere IP (ajoutee par le proxy Vercel, non falsifiable).

**SEC-05 — Validation UUID dans les routes admin avec :id (per D-01):**

Dans chaque route admin qui recoit un parametre `id` (models/[id], fabrics/[id], visuals/[id]/validate, visuals/[id]/publish), ajouter en debut de handler (apres `requireAdmin()`) :

```typescript
const UUID_REGEX = /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i
// ...
const { id } = await params
if (!UUID_REGEX.test(id)) {
  return NextResponse.json({ error: 'ID invalide' }, { status: 400 })
}
```

Definir `UUID_REGEX` comme constante en haut de chaque fichier ou bien creer un helper dans `src/lib/utils.ts` et l'importer. Le regex doit etre identique dans chaque fichier.

**TS-13 — Correction non-null assertion dans nano-banana.ts (per D-01):**

Remplacer `throw lastError!` (ligne ~193) par :
```typescript
throw lastError ?? new Error('Echec de la generation apres tous les essais')
```

**SEC-07 — Validation taille buffer dans nano-banana.ts (per D-01):**

Dans `resolveImagePart()`, apres `const buffer = await res.arrayBuffer()`, ajouter :
```typescript
if (buffer.byteLength > 20 * 1024 * 1024) {
  throw new Error('Image source trop volumineuse (max 20 Mo)')
}
```
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit && echo "TSC OK"</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts contient `async headers()` avec `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`, `Content-Security-Policy`
    - src/app/api/simulate/route.ts contient `ALLOWED_MIME` et `image.type` check retournant 422
    - src/app/api/simulate/route.ts contient `rateMap.size > 100` (pas 1000)
    - src/app/api/simulate/route.ts contient `.split(',').at(-1)?.trim()` (pas `[0]`)
    - src/app/api/admin/models/[id]/route.ts contient `UUID_REGEX.test(id)` retournant 400
    - src/app/api/admin/fabrics/[id]/route.ts contient `UUID_REGEX.test(id)` retournant 400
    - src/app/api/admin/visuals/[id]/validate/route.ts contient `UUID_REGEX.test(id)` retournant 400
    - src/app/api/admin/visuals/[id]/publish/route.ts contient `UUID_REGEX.test(id)` retournant 400
    - src/lib/ai/nano-banana.ts contient `throw lastError ?? new Error` (pas `throw lastError!`)
    - src/lib/ai/nano-banana.ts contient `buffer.byteLength > 20 * 1024 * 1024`
    - npx tsc --noEmit retourne exit code 0
  </acceptance_criteria>
  <done>Security headers configures dans next.config.ts, validation MIME dans simulate, eviction rateMap reduite, IP extraction corrigee, UUID valide dans toutes les routes admin :id, non-null assertion eliminee dans nano-banana, taille buffer validee dans resolveImagePart</done>
</task>

<task type="auto">
  <name>Task 2: Dead code cleanup + ESLint fixes + Zod schemas + console.info + img/Image + deps (per D-01 a D-05)</name>
  <read_first>
    - src/lib/schemas.ts
    - src/lib/ai/index.ts
    - src/lib/ai/nano-banana.ts
    - src/app/admin/(protected)/produits/IAGenerationSection.tsx
    - src/components/public/Catalogue/ConfiguratorModal.tsx
    - src/components/public/Catalogue/ConfiguratorModal.module.css
    - src/components/public/Hero/__tests__/Hero.test.tsx
    - src/components/public/HowItWorks/__tests__/HowItWorks.test.tsx
    - src/__tests__/ConfiguratorModal.test.tsx
    - src/types/database.ts
    - src/app/api/admin/generate/route.ts
    - src/app/api/admin/generate-all/route.ts
    - src/app/api/admin/visuals/bulk-validate/route.ts
    - src/app/api/admin/visuals/bulk-publish/route.ts
    - src/app/api/admin/models/[id]/images/route.ts
    - src/app/api/admin/models/[id]/visuals/route.ts
    - package.json
    - knip.json
    - .gitignore
    - .planning/phases/14-audit-code/AUDIT.md (sections DEAD-*, PERF-*, SEC-09)
    - .planning/phases/16-tests-e2e-corrections-audit/16-RESEARCH.md (Code Examples section)
  </read_first>
  <files>
    src/lib/schemas.ts,
    src/lib/ai/index.ts,
    src/lib/ai/nano-banana.ts,
    src/app/admin/(protected)/produits/IAGenerationSection.tsx,
    src/components/public/Catalogue/ConfiguratorModal.tsx,
    src/components/public/Catalogue/ConfiguratorModal.module.css,
    src/components/public/Hero/__tests__/Hero.test.tsx,
    src/components/public/HowItWorks/__tests__/HowItWorks.test.tsx,
    src/__tests__/ConfiguratorModal.test.tsx,
    src/types/database.ts,
    src/app/api/admin/generate/route.ts,
    src/app/api/admin/generate-all/route.ts,
    src/app/api/admin/visuals/bulk-validate/route.ts,
    src/app/api/admin/visuals/bulk-publish/route.ts,
    src/app/api/admin/models/[id]/images/route.ts,
    src/app/api/admin/models/[id]/visuals/route.ts,
    package.json,
    knip.json,
    .gitignore
  </files>
  <action>
**SEC-09 / D-04 — Ajouter schemas Zod pour routes POST admin sans validation formelle:**

Dans `src/lib/schemas.ts`, ajouter APRES les types inferes existants (apres ligne 87) :

```typescript
// === Schemas admin POST (SEC-09) ===

export const generateSchema = z.object({
  model_id: z.string().uuid('model_id doit etre un UUID valide'),
  fabric_id: z.string().uuid('fabric_id doit etre un UUID valide'),
})

export const generateAllSchema = z.object({
  model_id: z.string().uuid('model_id doit etre un UUID valide'),
})

export const bulkSchema = z.object({
  ids: z.array(z.string().uuid()).min(1, 'Au moins un ID requis'),
})

export const imagesUploadBodySchema = z.object({
  view_type: z.enum(['face', '3/4', 'profil', 'dos']),
  sort_order: z.number().int().min(0).optional(),
})
```

Puis dans chaque route POST admin, remplacer la validation manuelle par `schema.safeParse(body)` :
- `admin/generate/route.ts` : utiliser `generateSchema.safeParse(body)` au lieu du `if (!model_id || !fabric_id)`
- `admin/generate-all/route.ts` : utiliser `generateAllSchema.safeParse(body)` au lieu du `if (!model_id)`
- `admin/visuals/bulk-validate/route.ts` : utiliser `bulkSchema.safeParse(body)` au lieu du `if (!ids || !Array.isArray(ids))`
- `admin/visuals/bulk-publish/route.ts` : utiliser `bulkSchema.safeParse(body)` au lieu du `if (!ids || !Array.isArray(ids))`
- `admin/models/[id]/images/route.ts` : utiliser `imagesUploadBodySchema` pour valider le view_type du FormData
- `admin/models/[id]/visuals/route.ts` : utiliser `generateSchema.safeParse(body)` pour model_image_id + fabric_id

Pattern de remplacement pour chaque route :
```typescript
import { generateSchema } from '@/lib/schemas'
// ...
const parseResult = generateSchema.safeParse(body)
if (!parseResult.success) {
  return NextResponse.json(
    { error: parseResult.error.errors[0]?.message ?? 'Donnees invalides' },
    { status: 400 }
  )
}
const { model_id, fabric_id } = parseResult.data
```

**DEAD-05/06 — Supprimer schemas non utilises dans schemas.ts (per D-01):**

Supprimer `modelWithImagesSchema` et `visualWithFabricSchema` (lignes 50-56 actuelles). Ces exports ne sont importes nulle part dans le codebase (confirme par knip).

**DEAD-07/08 — Supprimer types non utilises dans schemas.ts (per D-01):**

Verifier avec grep si `ModelInput`, `ModelUpdateInput`, `FabricInput`, `FabricUpdateInput` sont importes quelque part. Si non : les supprimer (lignes 84-87). Si oui : les conserver.

**DEAD-09 — Supprimer types non utilises dans database.ts (per D-01):**

Supprimer `GeneratedVisualUpdate` et `ModelWithImagesAndVisuals` de `src/types/database.ts` s'ils ne sont pas importes (verifier avec grep).

**DEAD-10 — Supprimer countUngenerated dans IAGenerationSection.tsx (per D-01):**

Supprimer la variable `countUngenerated` (ligne ~193) qui est declaree mais jamais utilisee. C'est un ESLint error bloquant.

**DEAD-11 — Corriger les variables framer-motion non utilisees dans les tests (per D-01):**

Dans les 3 fichiers de test qui mockent framer-motion, remplacer le pattern de mock pour ne plus destructurer les props inutilisees. Utiliser le pattern suivant :

```typescript
vi.mock('framer-motion', () => ({
  motion: {
    div: (props: Record<string, unknown>) => {
      const { initial: _, animate: _a, transition: _t, ...rest } = props
      return <div {...(rest as React.HTMLAttributes<HTMLDivElement>)} />
    },
  },
  AnimatePresence: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  useReducedMotion: () => false,
  useInView: () => true,
}))
```

Adapter selon ce que chaque fichier de test exporte deja dans son mock (certains ont AnimatePresence, d'autres non). L'objectif est que ESLint `@typescript-eslint/no-unused-vars` ne signale plus de error.

**DEAD-12 — Supprimer eslint-disable inutiles dans ConfiguratorModal.tsx (per D-01):**

Examiner les commentaires `// eslint-disable-line` dans ConfiguratorModal.tsx. Supprimer ceux qui ne sont plus necessaires (la ligne n'a plus de violation ESLint). Si certains eslint-disable sont encore necessaires (ex: dependencies exhaustive-deps intentionnellement ignorees), les conserver avec un commentaire expliquant pourquoi.

**PERF-03/04/05/08 / D-05 — Remplacer console.log par console.info:**

Fichiers concernes et lignes exactes :
- `src/lib/ai/index.ts` : ligne ~14 `console.log('[IA] Using NanaBanana provider')` -> `console.info(...)`
- `src/lib/ai/index.ts` : ligne ~18 `console.log('[IA] Using mock provider')` -> `console.info(...)`
- `src/lib/ai/nano-banana.ts` : ligne ~82 `console.log('[IA] NanoBananaService initialise...')` -> `console.info(...)`
- `src/lib/ai/nano-banana.ts` : ligne ~153 `console.log('[IA] generate OK...')` -> `console.info(...)`
- `src/app/api/admin/generate/route.ts` : tout `console.log` -> `console.info`
- `src/app/api/admin/generate-all/route.ts` : tout `console.log` -> `console.info`
- `src/app/api/admin/visuals/bulk-publish/route.ts` : tout `console.log` -> `console.info`
- `src/app/api/admin/visuals/bulk-validate/route.ts` : tout `console.log` -> `console.info`

**PERF-07 / D-01 — Deplacer inline styles dans CSS modules:**

Dans ConfiguratorModal.tsx, remplacer toutes les occurrences de `style={{ objectFit: 'cover' }}` (valeurs statiques) par une classe CSS `.coverImage { object-fit: cover; }` dans ConfiguratorModal.module.css. Conserver les inline styles dynamiques (ex: `style={{ width: \`${progress}%\` }}`).

**PERF-06 / D-03 — Remplacer img par Image pour URLs Supabase definitives:**

Dans ConfiguratorModal.tsx et IAGenerationSection.tsx, remplacer les `<img>` qui affichent des URLs Supabase (pattern `https://*.supabase.co/storage/...`) par `<Image>` de `next/image` avec `width`, `height`, et `alt`. Ajouter `import Image from 'next/image'` si pas deja present.

IMPORTANT (per D-03) : Garder `<img>` pour les blob URLs de preview (upload en cours). Seules les images avec URL Supabase definitive passent a `<Image>`.

**DEAD-02 / D-02 — Supprimer dependances Radix UI inutilisees:**

Executer :
```bash
npm uninstall @radix-ui/react-dialog @radix-ui/react-dropdown-menu @radix-ui/react-label @radix-ui/react-select @radix-ui/react-slot @radix-ui/react-switch @radix-ui/react-toast @radix-ui/react-toggle
```

**DEAD-03 / D-02 — Supprimer zustand et immer:**

Executer :
```bash
npm uninstall zustand immer
```

**DEAD-01 — Corriger faux positifs knip:**

Dans `knip.json`, ajouter `"entry": ["scripts/**/*.ts"]` pour que knip reconnaisse les scripts CLI comme entry points. Cela corrige aussi DEAD-04 (tinyglobby).

**Gitignore — Ajouter les repertoires Playwright:**

Ajouter a `.gitignore` :
```
# Playwright
playwright/.auth/
playwright-report/
test-results/
```
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit && npx eslint src/ 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0 && npm run build 2>&1 | tail -5 && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/schemas.ts contient `export const generateSchema` et `export const bulkSchema` et `export const imagesUploadBodySchema`
    - src/lib/schemas.ts ne contient PAS `modelWithImagesSchema` ni `visualWithFabricSchema`
    - src/app/api/admin/generate/route.ts contient `generateSchema.safeParse`
    - src/app/api/admin/generate-all/route.ts contient `generateAllSchema.safeParse`
    - src/app/api/admin/visuals/bulk-validate/route.ts contient `bulkSchema.safeParse`
    - src/app/api/admin/visuals/bulk-publish/route.ts contient `bulkSchema.safeParse`
    - src/lib/ai/index.ts ne contient PAS `console.log` (uniquement `console.info`)
    - src/lib/ai/nano-banana.ts ne contient PAS `console.log` (uniquement `console.info`, `console.error`, `console.warn`)
    - package.json ne contient PAS `@radix-ui/react-dialog` ni `zustand` ni `immer`
    - .gitignore contient `playwright/.auth/` et `playwright-report/` et `test-results/`
    - knip.json contient `"entry"` avec `"scripts/**/*.ts"`
    - npx tsc --noEmit retourne exit code 0
    - npx eslint src/ retourne 0 errors (warnings acceptables)
    - npm run build se termine avec exit code 0
  </acceptance_criteria>
  <done>Tous les findings de l'audit sont corriges : 0 error ESLint, 0 erreur tsc, security headers, schemas Zod, dead code supprime, dependances nettoyees, console.info, img/Image selon D-03, inline styles deplace en CSS modules</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /api/simulate | Image upload non authentifiee, rate-limitee, validation MIME ajoutee |
| client -> /api/admin/* | Routes admin protegees par requireAdmin() + validation UUID ajoutee |
| admin routes -> Supabase | Requetes parametrisees (protection injection SQL native) |
| simulate -> Gemini API | Payload image limitee a 20 Mo (SEC-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Spoofing | /api/simulate IP extraction | mitigate | SEC-08: prendre derniere IP x-forwarded-for (ajoutee par Vercel, non falsifiable) |
| T-16-02 | Tampering | /api/simulate MIME type | mitigate | SEC-02: validation ALLOWED_MIME avant traitement (retourne 422 si non-image) |
| T-16-03 | Information Disclosure | next.config.ts headers | mitigate | SEC-01: X-Frame-Options SAMEORIGIN, X-Content-Type-Options nosniff, CSP restrictive |
| T-16-04 | Tampering | /api/admin/:id UUID | mitigate | SEC-05: regex UUID v4 validation avant requete DB (retourne 400) |
| T-16-05 | Tampering | /api/admin POST bodies | mitigate | SEC-09/D-04: schemas Zod safeParse sur toutes les routes POST admin |
| T-16-06 | Denial of Service | rateMap memory | accept | SEC-04: seuil reduit a 100 (ameliore). Migration Redis en v12+ |
| T-16-07 | Denial of Service | resolveImagePart large image | mitigate | SEC-07: rejet buffer > 20 Mo avant conversion base64 |
</threat_model>

<verification>
```bash
# Build propre
npm run build

# TypeScript strict
npx tsc --noEmit

# ESLint 0 errors
npx eslint src/ 2>&1 | grep -E "^\s*\d+ error" && exit 1 || echo "ESLint errors: 0"

# Tests unitaires existants passent toujours
npm test

# Security headers presents
grep -c "X-Frame-Options" next.config.ts
grep -c "Content-Security-Policy" next.config.ts

# Dependances nettoyees
! grep "radix-ui" package.json || echo "FAIL: Radix UI still present"
! grep '"zustand"' package.json || echo "FAIL: zustand still present"
! grep '"immer"' package.json || echo "FAIL: immer still present"
```
</verification>

<success_criteria>
- npm run build exit 0
- npx tsc --noEmit exit 0
- npx eslint src/ retourne 0 errors
- npm test passe (tests unitaires et integration existants)
- next.config.ts contient les 5 security headers
- package.json ne contient plus les 10 dependances inutilisees
- Schemas Zod utilises dans les 6+ routes POST admin
</success_criteria>

<output>
After completion, create `.planning/phases/16-tests-e2e-corrections-audit/16-01-SUMMARY.md`
</output>
