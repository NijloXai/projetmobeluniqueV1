---
phase: 05-recherche-et-tats-interactifs
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/__tests__/CatalogueClient.test.tsx
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - CAT-04

must_haves:
  truths:
    - "Les tests pour SRCH-01 echouent avant implementation (RED)"
    - "Les tests pour SRCH-02 echouent avant implementation (RED)"
    - "Les tests pour CAT-04 echouent avant implementation (RED)"
    - "Chaque comportement attendu est couvert par un test distinct"
  artifacts:
    - path: "src/__tests__/CatalogueClient.test.tsx"
      provides: "Tests RED pour recherche, etat vide, compteur"
      contains: "userEvent.type"
  key_links:
    - from: "src/__tests__/CatalogueClient.test.tsx"
      to: "src/components/public/Catalogue/CatalogueClient.tsx"
      via: "render(<CatalogueClient models={mockModels} />)"
      pattern: "CatalogueClient"
---

<objective>
Ecrire les tests (RED) pour la fonctionnalite de recherche du catalogue avant toute implementation.

Purpose: Etablir le contrat comportemental des 3 requirements (SRCH-01, SRCH-02, CAT-04) via des tests qui echouent, garantissant que l'implementation Plan 02 sera guidee par des specs executables.
Output: `src/__tests__/CatalogueClient.test.tsx` etendu avec 6 nouveaux cas RED, suite complete toujours verte sur les cas existants.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-recherche-et-tats-interactifs/05-VALIDATION.md
</context>

<feature>
  <name>Filtre recherche catalogue (SRCH-01, SRCH-02, CAT-04)</name>
  <files>src/__tests__/CatalogueClient.test.tsx</files>
  <behavior>
    - SRCH-01 cas 1: Taper "mil" dans le champ de recherche filtre et affiche seulement "Milano" (pas "Oslo")
    - SRCH-01 cas 2: Taper "canape" trouve "Canapé Milano" (normalisation accents — "canape" matche "Canapé")
    - SRCH-02 cas 1: Taper "zzz" affiche un message contenant 'Aucun canapé ne correspond' et le terme "zzz"
    - SRCH-02 cas 2: Dans l'etat vide, cliquer "Effacer la recherche" remet toutes les cards (Milano et Oslo)
    - CAT-04 cas 1: Avec 2 modeles, le compteur affiche "2 canapés"
    - CAT-04 cas 2: Avec 1 seul modele (mockModels[0] uniquement), le compteur affiche "1 canapé" (singulier)
  </behavior>
  <implementation>
    Ajouter un nouveau describe bloc "recherche et filtrage" dans le fichier de test existant.
    Utiliser @testing-library/user-event pour simuler la saisie dans le champ.
    Importer userEvent depuis '@testing-library/user-event'.
    Le champ de recherche sera trouve via son aria-label "Rechercher un canapé par nom" (getByRole('searchbox') ou getByLabelText).
    Le compteur sera trouve via getByText avec un matcher regex.
    Le bouton reset sera trouve via getByRole('button', { name: /effacer la recherche/i }).
    IMPORTANT: Les 7 tests EXISTANTS ne doivent PAS etre modifies — seulement ajouter de nouveaux.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ecrire les tests RED (SRCH-01, SRCH-02, CAT-04)</name>
  <files>src/__tests__/CatalogueClient.test.tsx</files>

  <read_first>
    - src/__tests__/CatalogueClient.test.tsx — fichier existant a etendre (7 tests passants, mockModels definis)
    - src/components/public/Catalogue/CatalogueClient.tsx — composant actuel (pas de useState, pas de recherche)
    - .planning/phases/05-recherche-et-tats-interactifs/05-VALIDATION.md — map requirements -> tests
  </read_first>

  <behavior>
    - SRCH-01 cas 1: Taper "mil" filtre sur "Milano" — Oslo disparait
    - SRCH-01 cas 2: Taper "canape" trouve "Canapé Milano" (normalisation NFD)
    - SRCH-02 cas 1: Taper "zzz" affiche "Aucun canapé ne correspond" + "zzz"
    - SRCH-02 cas 2: Clic "Effacer la recherche" depuis etat vide reaffiche toutes les cards
    - CAT-04 cas 1: 2 modeles → compteur "2 canapés"
    - CAT-04 cas 2: 1 modele → compteur "1 canapé" (singulier)
  </behavior>

  <action>
Ajouter en fin du fichier `src/__tests__/CatalogueClient.test.tsx`, APRES le describe bloc existant, un nouveau describe bloc :

```typescript
import userEvent from '@testing-library/user-event'
```
(Ajouter cet import en haut du fichier avec les autres imports existants.)

Puis ajouter apres le describe bloc existant :

```typescript
describe('CatalogueClient — recherche et filtrage', () => {
  // Donnees mockModels deja definies en dehors — les reutiliser directement

  it('[SRCH-01] saisir "mil" filtre et affiche seulement Milano', async () => {
    const user = userEvent.setup()
    render(<CatalogueClient models={mockModels} />)
    const input = screen.getByLabelText(/rechercher un canapé par nom/i)
    await user.type(input, 'mil')
    expect(screen.getByText(/milano/i)).toBeInTheDocument()
    expect(screen.queryByText(/oslo/i)).not.toBeInTheDocument()
  })

  it('[SRCH-01] saisir "canape" sans accent trouve Canapé Milano (normalisation)', async () => {
    const user = userEvent.setup()
    const modelsWithAccent: ModelWithImages[] = [
      { ...mockModels[0], name: 'Canapé Milano' },
    ]
    render(<CatalogueClient models={modelsWithAccent} />)
    const input = screen.getByLabelText(/rechercher un canapé par nom/i)
    await user.type(input, 'canape')
    expect(screen.getByText(/canapé milano/i)).toBeInTheDocument()
  })

  it('[SRCH-02] saisir "zzz" affiche letat vide avec le terme', async () => {
    const user = userEvent.setup()
    render(<CatalogueClient models={mockModels} />)
    const input = screen.getByLabelText(/rechercher un canapé par nom/i)
    await user.type(input, 'zzz')
    expect(screen.getByText(/aucun canapé ne correspond/i)).toBeInTheDocument()
    expect(screen.getByText(/zzz/i)).toBeInTheDocument()
  })

  it('[SRCH-02] cliquer Effacer la recherche remet toutes les cards', async () => {
    const user = userEvent.setup()
    render(<CatalogueClient models={mockModels} />)
    const input = screen.getByLabelText(/rechercher un canapé par nom/i)
    await user.type(input, 'zzz')
    const resetBtn = screen.getByRole('button', { name: /effacer la recherche/i })
    await user.click(resetBtn)
    expect(screen.getAllByRole('article')).toHaveLength(2)
  })

  it('[CAT-04] compteur affiche "2 canapés" avec 2 modeles', () => {
    render(<CatalogueClient models={mockModels} />)
    expect(screen.getByText('2 canapés')).toBeInTheDocument()
  })

  it('[CAT-04] compteur affiche "1 canapé" (singulier) avec 1 modele', () => {
    render(<CatalogueClient models={[mockModels[0]]} />)
    expect(screen.getByText('1 canapé')).toBeInTheDocument()
  })
})
```

IMPORTANT : Les tests doivent ECHOUER apres cette tache (RED). Ne pas modifier CatalogueClient.tsx dans ce plan.

NOTE sur mockModels : Le modele mockModels[0] a name: 'Milano'. Pour le test SRCH-01 cas 1, 'mil' matchera 'Milano' (normalize('milano').includes(normalize('mil')) = true). Pour le test SRCH-01 cas 2, on utilise un modele local avec name: 'Canapé Milano' pour tester la normalisation — s'assurer que l'import de ModelWithImages est deja present (il l'est).
  </action>

  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx vitest run src/__tests__/CatalogueClient.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/__tests__/CatalogueClient.test.tsx contient "import userEvent from '@testing-library/user-event'"
    - src/__tests__/CatalogueClient.test.tsx contient "SRCH-01"
    - src/__tests__/CatalogueClient.test.tsx contient "SRCH-02"
    - src/__tests__/CatalogueClient.test.tsx contient "CAT-04"
    - src/__tests__/CatalogueClient.test.tsx contient "recherche et filtrage" (nouveau describe bloc)
    - La suite a exactement 13 tests (7 existants + 6 nouveaux)
    - Les 7 tests existants passent, les 6 nouveaux echouent (RED confirmé)
    - `npx vitest run src/__tests__/CatalogueClient.test.tsx` montre 7 passed + 6 failed
  </acceptance_criteria>

  <done>6 tests RED ajoutés couvrant SRCH-01 (2 cas), SRCH-02 (2 cas), CAT-04 (2 cas). Les 7 tests existants passent toujours.</done>
</task>

</tasks>

<verification>
`npx vitest run src/__tests__/CatalogueClient.test.tsx` : 7 passed + 6 failed — la suite existante reste integre, les nouveaux tests sont RED et attendent l'implementation Plan 02.
</verification>

<success_criteria>
- 6 nouveaux tests présents dans le describe "recherche et filtrage"
- Les 7 tests existants passent (aucune regression)
- Les 6 nouveaux tests echouent (RED) — preuve que l'implementation n'existe pas encore
- Import userEvent présent
</success_criteria>

<output>
Apres completion, creer `.planning/phases/05-recherche-et-tats-interactifs/05-01-SUMMARY.md`
</output>
