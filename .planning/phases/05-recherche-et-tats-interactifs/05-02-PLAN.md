---
phase: 05-recherche-et-tats-interactifs
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/public/Catalogue/CatalogueClient.tsx
  - src/components/public/Catalogue/CatalogueSection.module.css
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - CAT-04

must_haves:
  truths:
    - "L'utilisateur peut saisir un nom dans la barre de recherche et les cards se filtrent instantanément"
    - "La recherche fonctionne avec ou sans accents (canape trouve Canapé Milano)"
    - "Un message 'Aucun canapé ne correspond à...' s'affiche quand la recherche ne correspond à aucun produit"
    - "Le nombre de canapés affichés est visible en permanence (singulier/pluriel correct)"
    - "Le bouton 'Effacer la recherche' remet la liste complète"
  artifacts:
    - path: "src/components/public/Catalogue/CatalogueClient.tsx"
      provides: "useState + filtre derive + barre de recherche + compteur + etat vide"
      contains: "normalize('NFD')"
    - path: "src/components/public/Catalogue/CatalogueSection.module.css"
      provides: "Styles searchWrapper, searchInput, resultCount, emptySearch, resetButton"
      contains: ".searchWrapper"
  key_links:
    - from: "src/components/public/Catalogue/CatalogueClient.tsx"
      to: "src/components/public/Catalogue/CatalogueSection.module.css"
      via: "styles.searchWrapper, styles.searchInput, etc."
      pattern: "styles\\.search"
    - from: "normalize(m.name)"
      to: "normalize(query)"
      via: "Array.filter() — comparaison des deux normalises"
      pattern: "normalize\\(m\\.name\\)\\.includes\\(normalize\\(query\\)\\)"
---

<objective>
Implementer la barre de recherche, le compteur de résultats et l'état vide dans CatalogueClient.tsx pour faire passer les 6 tests RED écrits en Plan 01.

Purpose: Livrer les 3 requirements SRCH-01, SRCH-02 et CAT-04 en modifiant uniquement CatalogueClient.tsx et CatalogueSection.module.css — zero nouvelle dépendance.
Output: Composant CatalogueClient fonctionnel avec filtre instantané, normalisation accents FR, compteur singulier/pluriel, état vide avec bouton reset. Suite de tests verte (13/13).
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-recherche-et-tats-interactifs/05-CONTEXT.md
@.planning/phases/05-recherche-et-tats-interactifs/05-UI-SPEC.md

<interfaces>
<!-- Fichier source actuel — lire avant de modifier -->

Depuis src/components/public/Catalogue/CatalogueClient.tsx (ETAT ACTUEL — avant modification) :
```typescript
'use client'

import type { ModelWithImages } from '@/types/database'
import { ProductCard } from './ProductCard'
import styles from './CatalogueSection.module.css'

interface CatalogueClientProps {
  models: ModelWithImages[]
}

export function CatalogueClient({ models }: CatalogueClientProps) {
  if (models.length === 0) {
    return (
      <section id="catalogue" className={styles.section}>
        <div className={styles.container}>
          <p className={styles.emptyMessage}>Nos canapes arrivent bientot.</p>
        </div>
      </section>
    )
  }

  return (
    <section id="catalogue" className={styles.section} aria-labelledby="catalogue-title">
      <div className={styles.container}>
        <div className={styles.sectionHeader}>
          <h2 id="catalogue-title" className={styles.sectionTitle}>Nos Canapes</h2>
          <p className={styles.sectionSubtitle}>
            Selectionnez une base pour commencer la configuration.
          </p>
        </div>
        <div className={styles.grid}>
          {models.map((model) => (
            <ProductCard key={model.id} model={model} onConfigure={undefined} />
          ))}
        </div>
      </div>
    </section>
  )
}
```

Classes CSS existantes dans CatalogueSection.module.css :
- `.section` — fond blanc, padding section
- `.container` — max-width 1280px, margin auto
- `.sectionHeader` — text-align center, margin-bottom 48px
- `.sectionTitle` — font-size 32px, font-weight 700
- `.sectionSubtitle` — font-size 16px, color muted, max-width 480px, margin auto
- `.grid` — CSS Grid responsive (1/2/3 colonnes selon breakpoint)
- `.emptyMessage` — text-align center, color muted, padding section
- `.errorMessage` — identique emptyMessage

Tokens CSS disponibles (src/app/globals.css) :
- `--color-background: #FFFFFF`
- `--surface-container-low: #F6F3EF`
- `--color-primary: #E49400`
- `--color-muted: #888888`
- `--color-text: #1D1D1B`
- `--color-border: #E5E5E5`
- `--spacing-xs: 4px` · `--spacing-sm: 8px` · `--spacing-md: 16px`
- `--spacing-lg: 24px` · `--spacing-xl: 32px` · `--spacing-2xl: 48px`
- `--spacing-section: 112px`
- `--font-size-sm: 0.875rem (14px)` · `--font-size-base: 1rem (16px)`
- `--radius-md: 8px` · `--radius-lg: 12px`
- `--container-padding-mobile: 24px`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementer CatalogueClient avec recherche, compteur et état vide</name>
  <files>src/components/public/Catalogue/CatalogueClient.tsx</files>

  <read_first>
    - src/components/public/Catalogue/CatalogueClient.tsx — fichier actuel a remplacer completement
    - src/__tests__/CatalogueClient.test.tsx — tests RED a faire passer (Plan 01 output)
    - .planning/phases/05-recherche-et-tats-interactifs/05-CONTEXT.md — decisions D-01 a D-13
    - .planning/phases/05-recherche-et-tats-interactifs/05-UI-SPEC.md — contrat visuel et interaction
  </read_first>

  <behavior>
    - Taper "mil" → filtre cards et affiche seulement Milano
    - Taper "canape" → trouve "Canapé Milano" (normalisation NFD des deux cotes)
    - Taper "zzz" → etat vide avec message + bouton reset (pas de cards)
    - Cliquer "Effacer la recherche" → remet toutes les cards + focus sur l'input
    - Compteur "2 canapés" avec 2 modeles, "1 canapé" avec 1 modele
  </behavior>

  <action>
Remplacer completement `src/components/public/Catalogue/CatalogueClient.tsx` par :

```typescript
'use client'

import { useState, useRef } from 'react'
import { Search, X } from 'lucide-react'
import type { ModelWithImages } from '@/types/database'
import { ProductCard } from './ProductCard'
import styles from './CatalogueSection.module.css'

// Fonction pure : normalise une chaine pour comparaison insensible aux accents et a la casse
// Ex: 'Canapé Milano' → 'canape milano' | 'canape' → 'canape'
function normalize(str: string): string {
  return str
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .toLowerCase()
    .trim()
}

interface CatalogueClientProps {
  models: ModelWithImages[]
}

export function CatalogueClient({ models }: CatalogueClientProps) {
  const [query, setQuery] = useState('')
  const inputRef = useRef<HTMLInputElement>(null)

  // Valeur derivee — pas de useState supplementaire ni de useEffect
  const filteredModels = query
    ? models.filter((m) => normalize(m.name).includes(normalize(query)))
    : models

  // Singulier/pluriel FR — per D-08
  const countLabel =
    filteredModels.length === 1
      ? '1 canapé'
      : `${filteredModels.length} canapés`

  function handleReset() {
    setQuery('')
    inputRef.current?.focus()
  }

  // Etat vide catalogue (models vide depuis Supabase)
  if (models.length === 0) {
    return (
      <section id="catalogue" className={styles.section}>
        <div className={styles.container}>
          <p className={styles.emptyMessage}>Nos canapes arrivent bientot.</p>
        </div>
      </section>
    )
  }

  return (
    <section
      id="catalogue"
      className={styles.section}
      aria-labelledby="catalogue-title"
    >
      <div className={styles.container}>
        {/* En-tete section — inchange */}
        <div className={styles.sectionHeader}>
          <h2 id="catalogue-title" className={styles.sectionTitle}>
            Nos Canapes
          </h2>
          <p className={styles.sectionSubtitle}>
            Selectionnez une base pour commencer la configuration.
          </p>
        </div>

        {/* Barre de recherche — D-01, D-02, D-03, D-04 */}
        <div className={styles.searchWrapper}>
          <Search size={16} aria-hidden="true" className={styles.searchIcon} />
          <input
            ref={inputRef}
            type="search"
            value={query}
            onChange={(e) => setQuery(e.target.value)}
            placeholder="Rechercher un canapé..."
            className={styles.searchInput}
            aria-label="Rechercher un canapé par nom"
          />
          {query && (
            <button
              type="button"
              onClick={handleReset}
              className={styles.clearButton}
              aria-label="Effacer la recherche"
            >
              <X size={14} aria-hidden="true" />
            </button>
          )}
        </div>

        {/* Compteur de resultats — CAT-04, D-08, D-09 */}
        <p className={styles.resultCount}>{countLabel}</p>

        {/* Grille ou etat vide recherche */}
        {filteredModels.length === 0 && query !== '' ? (
          /* Etat vide recherche — SRCH-02, D-11, D-12, D-13 */
          <div className={styles.emptySearch}>
            <p className={styles.emptyMessage}>
              Aucun canapé ne correspond à &ldquo;{query}&rdquo;
            </p>
            <button
              type="button"
              onClick={handleReset}
              className={styles.resetButton}
            >
              Effacer la recherche
            </button>
          </div>
        ) : (
          /* Grille de cards filtrées */
          <div className={styles.grid}>
            {filteredModels.map((model) => (
              <ProductCard
                key={model.id}
                model={model}
                onConfigure={undefined}
              />
            ))}
          </div>
        )}
      </div>
    </section>
  )
}
```

REGLES STRICTES :
- `filteredModels` est une valeur derivee — PAS un useState (anti-pattern documenté)
- PAS de useEffect pour le filtre (synchrone, pas un side-effect)
- PAS de debounce (D-06 — 20-30 produits max)
- normalize() appliquee aux DEUX operandes : `normalize(m.name).includes(normalize(query))` (Pitfall 1)
- normalize() inclut `.toLowerCase()` et `.trim()` (Pitfalls 2 et 3)
  </action>

  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx vitest run src/__tests__/CatalogueClient.test.tsx 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/components/public/Catalogue/CatalogueClient.tsx contient "import { useState, useRef } from 'react'"
    - src/components/public/Catalogue/CatalogueClient.tsx contient "import { Search, X } from 'lucide-react'"
    - src/components/public/Catalogue/CatalogueClient.tsx contient "normalize('NFD')"
    - src/components/public/Catalogue/CatalogueClient.tsx contient "normalize(m.name).includes(normalize(query))"
    - src/components/public/Catalogue/CatalogueClient.tsx contient "aria-label=\"Rechercher un canapé par nom\""
    - src/components/public/Catalogue/CatalogueClient.tsx contient "Aucun canapé ne correspond"
    - src/components/public/Catalogue/CatalogueClient.tsx contient "Effacer la recherche"
    - src/components/public/Catalogue/CatalogueClient.tsx contient "filteredModels.length === 1"
    - `npx vitest run src/__tests__/CatalogueClient.test.tsx` passe 13/13 tests
    - `npx tsc --noEmit` exit 0 (zero erreur TypeScript)
  </acceptance_criteria>

  <done>CatalogueClient.tsx implemente la recherche instantanee, la normalisation accents FR, le compteur singulier/pluriel, et l'etat vide avec reset. 13/13 tests verts.</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter les styles CSS pour la barre de recherche et les etats</name>
  <files>src/components/public/Catalogue/CatalogueSection.module.css</files>

  <read_first>
    - src/components/public/Catalogue/CatalogueSection.module.css — fichier existant a etendre (ne pas supprimer les styles existants)
    - .planning/phases/05-recherche-et-tats-interactifs/05-UI-SPEC.md — contrat visuel complet (Component Inventory, Spacing Scale, Color, Typography)
  </read_first>

  <action>
Ajouter en fin du fichier `src/components/public/Catalogue/CatalogueSection.module.css`, apres les media queries existantes, les classes CSS suivantes :

```css
/* === Phase 05 — Recherche et etats interactifs === */

/* Barre de recherche — D-01, D-04 : centree, max-width 480px */
.searchWrapper {
  position: relative;
  display: flex;
  align-items: center;
  max-width: 480px;
  margin: var(--spacing-xl) auto 0; /* 32px sous le sectionHeader */
}

/* Icone loupe — D-02 : positionnee a gauche, decorative */
.searchIcon {
  position: absolute;
  left: var(--spacing-md); /* 16px du bord gauche */
  color: var(--color-muted); /* #888888 */
  pointer-events: none; /* decoratif */
}

/* Champ de recherche — D-02 : fond teinte sans bordure, coins arrondis */
.searchInput {
  width: 100%;
  padding: var(--spacing-md); /* 16px tous cotes */
  padding-left: 2.5rem; /* 40px — espace pour l'icone loupe */
  background-color: var(--surface-container-low); /* #F6F3EF */
  border: none;
  border-radius: var(--radius-lg); /* 12px — coherence avec les cards */
  font-size: var(--font-size-base); /* 16px */
  font-family: inherit; /* Montserrat via root */
  color: var(--color-text); /* #1D1D1B */
  outline: none;
}

.searchInput:focus {
  box-shadow: 0 0 0 2px var(--color-primary); /* #E49400 — focus ring ambre */
}

/* Masquer le bouton clear natif webkit — evite doublon avec .clearButton */
.searchInput[type='search']::-webkit-search-cancel-button {
  display: none;
}

/* Bouton clear (X) dans le champ — Claude's Discretion */
.clearButton {
  position: absolute;
  right: var(--spacing-md); /* 16px du bord droit */
  background: none;
  border: none;
  color: var(--color-muted); /* #888888 */
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 32px; /* touch target accessibilite mobile */
  min-height: 32px;
  padding: 0;
  cursor: pointer;
}

.clearButton:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
  border-radius: var(--radius-md);
}

/* Compteur de resultats — CAT-04, D-08, D-09 : toujours visible */
.resultCount {
  text-align: center;
  font-size: var(--font-size-sm); /* 14px */
  color: var(--color-muted); /* #888888 */
  margin-top: var(--spacing-md); /* 16px sous la barre */
  margin-bottom: var(--spacing-xl); /* 32px avant la grille */
}

/* Conteneur etat vide recherche — SRCH-02, D-13 */
.emptySearch {
  text-align: center;
  padding: var(--spacing-section) 0; /* 112px — coherence avec .emptyMessage */
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--spacing-md); /* 16px entre message et bouton */
}

/* Bouton reset dans l'etat vide — D-12 : outline secondaire */
.resetButton {
  background: none;
  border: 1px solid var(--color-border); /* #E5E5E5 repos */
  border-radius: var(--radius-md); /* 8px */
  padding: var(--spacing-sm) var(--spacing-lg); /* 8px 24px */
  font-size: var(--font-size-sm); /* 14px */
  font-weight: 500;
  font-family: inherit;
  color: var(--color-muted); /* #888888 repos */
  cursor: pointer;
  transition: border-color 150ms ease, color 150ms ease;
}

.resetButton:hover {
  border-color: var(--color-primary); /* #E49400 hover */
  color: var(--color-primary);
}

.resetButton:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

/* Responsive barre de recherche — mobile full-width */
@media (max-width: 639px) {
  .searchWrapper {
    max-width: 100%; /* full-width mobile dans les marges du container */
  }
}
```

IMPORTANT :
- Ne pas modifier les classes existantes (.section, .container, .sectionHeader, .sectionTitle, .sectionSubtitle, .grid, .emptyMessage, .errorMessage et les media queries existantes)
- Ajouter uniquement apres les media queries existantes
- Respecter tous les tokens CSS de la liste ci-dessus — ne pas utiliser de valeurs hardcodees sauf pour 2.5rem (padding-left calculé pour l'icone)
  </action>

  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npm run build 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - src/components/public/Catalogue/CatalogueSection.module.css contient ".searchWrapper"
    - src/components/public/Catalogue/CatalogueSection.module.css contient ".searchInput"
    - src/components/public/Catalogue/CatalogueSection.module.css contient ".clearButton"
    - src/components/public/Catalogue/CatalogueSection.module.css contient ".resultCount"
    - src/components/public/Catalogue/CatalogueSection.module.css contient ".emptySearch"
    - src/components/public/Catalogue/CatalogueSection.module.css contient ".resetButton"
    - src/components/public/Catalogue/CatalogueSection.module.css contient "var(--surface-container-low)"
    - src/components/public/Catalogue/CatalogueSection.module.css contient "var(--color-primary)"
    - Les classes existantes (.section, .grid, .sectionTitle, etc.) sont toujours presentes
    - `npm run build` exit 0 (build production sans erreur)
    - `npx vitest run src/__tests__/CatalogueClient.test.tsx` toujours 13/13 verts
  </acceptance_criteria>

  <done>Tous les styles pour la barre de recherche, le compteur et l'etat vide sont en place. Build production propre. Suite de tests complete verte (13/13).</done>
</task>

</tasks>

<verification>
Suite complete :
1. `npx vitest run src/__tests__/CatalogueClient.test.tsx` → 13 tests passed (0 failed)
2. `npm run build` → build sans erreur
3. `npx tsc --noEmit` → zero erreur TypeScript
</verification>

<success_criteria>
- SRCH-01 : saisir "mil" affiche seulement Milano, saisir "canape" trouve "Canapé Milano"
- SRCH-02 : saisir "zzz" affiche "Aucun canapé ne correspond à 'zzz'" + bouton reset fonctionnel
- CAT-04 : compteur "2 canapés" / "1 canapé" toujours visible et a jour
- 13/13 tests verts
- Build production propre (npm run build exit 0)
- Zero erreur TypeScript (npx tsc --noEmit exit 0)
- CSS conforme aux tokens du design system (zero valeur hardcodee sauf 2.5rem justifiee)
</success_criteria>

<output>
Apres completion, creer `.planning/phases/05-recherche-et-tats-interactifs/05-02-SUMMARY.md`
</output>
