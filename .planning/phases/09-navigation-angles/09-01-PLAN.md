---
phase: 09-navigation-angles
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/public/Catalogue/ConfiguratorModal.tsx
  - src/components/public/Catalogue/ConfiguratorModal.module.css
  - src/__tests__/ConfiguratorModal.test.tsx
autonomous: true
requirements: [CONF-04, CONF-06]

must_haves:
  truths:
    - "Le client voit les thumbnails des angles disponibles sous l'image principale quand le modele a plusieurs angles"
    - "Cliquer un thumbnail change l'image principale avec un crossfade 200ms"
    - "Le thumbnail de l'angle actif a une bordure primary #E49400 3px + outline 2px"
    - "Quand un tissu est selectionne, seuls les angles ayant un rendu IA publie apparaissent en thumbnails"
    - "Si un seul angle a un rendu publie, la rangee de thumbnails est masquee"
    - "Au changement de tissu, l'angle est conserve si le nouveau tissu a un rendu pour cet angle, sinon reset au 3/4"
    - "Au changement de modele, l'angle reset au 3/4"
    - "L'alt text de l'image principale inclut le nom de l'angle quand applicable"
  artifacts:
    - path: "src/components/public/Catalogue/ConfiguratorModal.tsx"
      provides: "Rangee thumbnails, selectedAngle state, availableAngles, crossfade, handleFabricSelect avec preservation angle"
      contains: "useState<string | null>"
    - path: "src/components/public/Catalogue/ConfiguratorModal.module.css"
      provides: "Styles .thumbnailRow, .thumbnail, .thumbnailActive, .imageMain, @keyframes imageFadeIn"
      contains: ".thumbnailRow"
    - path: "src/__tests__/ConfiguratorModal.test.tsx"
      provides: "Tests Phase 9 — navigation angles (CONF-04, CONF-06, D-07)"
      contains: "Phase 9"
  key_links:
    - from: "ConfiguratorModal.tsx"
      to: "visuals[].model_image_id"
      via: "currentVisual filtre par model_id + fabric_id + model_image_id"
      pattern: "v\\.model_image_id === selectedAngle"
    - from: "ConfiguratorModal.tsx thumbnailRow"
      to: "availableAngles computed from model.model_images + visuals"
      via: "selectedFabricId ? filter par visuals publies : tous model_images"
      pattern: "availableAngles\\.length > 1"
    - from: "ConfiguratorModal.tsx handleFabricSelect"
      to: "selectedAngle preservation logic"
      via: "visuals.some check pour angle actuel dans nouveau tissu"
      pattern: "hasRenderForCurrentAngle"
---

<objective>
Navigation par angles dans le configurateur tissu — thumbnails cliquables sous l'image principale, crossfade 200ms, filtrage angles par tissu selectionne.

Purpose: Le client peut voir le canape sous differents angles (3/4, face, profil) avec le rendu IA du tissu choisi, completant l'experience configurateur.
Output: ConfiguratorModal enrichi avec rangee de thumbnails, etat selectedAngle, logique de filtrage angles, crossfade CSS, et tests unitaires couvrant CONF-04 + CONF-06.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-navigation-angles/09-CONTEXT.md
@.planning/phases/09-navigation-angles/09-RESEARCH.md
@.planning/phases/09-navigation-angles/09-UI-SPEC.md
@.planning/phases/09-navigation-angles/09-VALIDATION.md
@.planning/phases/08-configurateur-core/08-01-SUMMARY.md

<interfaces>
<!-- Types et contracts que l'executeur utilise. Extraits du codebase. -->

From src/types/database.ts:
```typescript
export type ModelImage = {
  id: string
  image_url: string
  model_id: string
  sort_order: number
  view_type: string
}

export type ModelWithImages = Model & {
  model_images: ModelImage[]
}

export type VisualWithFabricAndImage = GeneratedVisual & {
  fabric: Fabric
  model_image: ModelImage
}
// GeneratedVisual inclut: model_image_id: string, fabric_id: string, model_id: string, is_published: boolean
```

From src/components/public/Catalogue/ConfiguratorModal.tsx:
```typescript
export function getPrimaryImage(model_images: ModelImage[]): string | null
// Retourne image_url du view_type '3/4' ou premiere image, ou null si vide

export function formatPrice(price: number): string
// Retourne 'a partir de X EUR' format FR

interface ConfiguratorModalProps {
  model: ModelWithImages | null
  onClose: () => void
  fabrics: Fabric[]
  visuals: VisualWithFabricAndImage[]
}
```

From src/components/public/Catalogue/ConfiguratorModal.module.css:
```css
.swatchSelected {
  border-color: var(--color-primary);
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}
/* Pattern a reproduire pour .thumbnailActive */
```

From src/app/globals.css (tokens confirmes):
```css
--color-primary: #E49400;
--spacing-xs: 0.25rem;   /* 4px */
--spacing-sm: 0.5rem;    /* 8px */
--radius-sm: 4px;
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.07);
--transition-fast: 300ms ease;  /* NE PAS utiliser pour crossfade — D-06 demande 200ms */
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests Phase 9 — navigation angles (RED)</name>
  <files>src/__tests__/ConfiguratorModal.test.tsx</files>

  <read_first>
    - src/__tests__/ConfiguratorModal.test.tsx (fichier existant — comprendre fixtures, patterns de test, imports)
    - src/components/public/Catalogue/ConfiguratorModal.tsx (interface actuelle — comprendre les exports et props)
    - src/types/database.ts (types ModelImage, VisualWithFabricAndImage — structure des fixtures)
    - .planning/phases/09-navigation-angles/09-CONTEXT.md (decisions D-01 a D-16)
    - .planning/phases/09-navigation-angles/09-RESEARCH.md (patterns, code examples)
    - .planning/phases/09-navigation-angles/09-VALIDATION.md (test map)
  </read_first>

  <behavior>
    Fixtures multi-angles :
    - mockModelMultiAngle : model avec 3 model_images (view_type: '3/4' id='img-34', 'face' id='img-face', 'profil' id='img-profil')
    - mockVisualsMultiAngle : 3 visuals publies correspondant a chaque angle pour fabric-modal-001 (Velours Emeraude)
    - mockVisualsPartialAngles : 2 visuals publies (3/4 + face) pour fabric-modal-002 (Cuir Nappa) — angle profil ABSENT
    - mockVisualsSingleAngle : 1 seul visual publie (3/4) pour un tissu specifique — pour tester D-11

    Tests a ecrire (tous doivent ECHOUER car la fonctionnalite n'existe pas encore) :

    describe('Phase 9 — navigation angles', () => {
      - Test CONF-06a: "affiche les thumbnails de tous les angles quand aucun tissu selectionne"
        Render avec mockModelMultiAngle, pas de clic swatch. Verifier radiogroup "Choisir l'angle de vue" present, 3 radios (un par angle).
      - Test CONF-06b: "cliquer un thumbnail change limage principale"
        Render avec mockModelMultiAngle + allVisuals multi-angle. Selectionner tissu Velours. Cliquer thumbnail "Vue face". Verifier que l'image src contient le generated_image_url du visual face.
      - Test CONF-06c: "le thumbnail actif a aria-checked true"
        Render avec mockModelMultiAngle. Le thumbnail 3/4 doit avoir aria-checked="true" par defaut. Cliquer "Vue profil" → profil a aria-checked="true", 3/4 a aria-checked="false".
      - Test CONF-06d: "avec tissu selectionne, seuls les angles avec rendu publie sont affiches"
        Render avec mockModelMultiAngle + mockVisualsPartialAngles. Selectionner Cuir Nappa (2 angles). Verifier 2 radios dans le radiogroup angles (pas 3). Le thumbnail profil est absent.
      - Test D-11: "rangee thumbnails masquee si un seul angle publie"
        Render avec mockModelMultiAngle + mockVisualsSingleAngle. Selectionner le tissu qui n'a qu'un seul angle. Verifier que le radiogroup "Choisir l'angle de vue" est absent.
      - Test CONF-06e: "sans tissu, thumbnails montrent photos originales de tous les angles"
        Render avec mockModelMultiAngle. Verifier 3 thumbnails. Verifier que les img src dans les thumbnails correspondent aux image_url des model_images (photos originales), pas aux generated_image_url.
      - Test D-12: "changement de tissu preserve langle si rendu existe, sinon reset"
        Render avec mockModelMultiAngle + visuals multi-angle + visuals partiels. Selectionner Velours (3 angles). Cliquer thumbnail "Vue profil". Changer pour Cuir Nappa (pas de profil). Verifier que l'angle reset (thumbnail profil disparu, 3/4 ou face selectionne avec aria-checked="true").
      - Test D-16: "changement de modele reset langle au 3/4"
        Render avec mockModelMultiAngle. Cliquer thumbnail "Vue profil". Rerender avec un autre modele. Verifier que le thumbnail 3/4 du nouveau modele a aria-checked="true".
      - Test D-07: "alt text inclut le nom de langle quand tissu selectionne"
        Render avec mockModelMultiAngle + visuals multi-angle. Selectionner Velours. Cliquer thumbnail "Vue face". Verifier que l'image principale a alt contenant "vue face".
      - Test CONF-04: "limage principale affiche le rendu IA pour le tissu et angle selectionnes"
        Render avec mockModelMultiAngle + visuals multi-angle. Selectionner Velours. Cliquer "Vue profil". Verifier que img src = generated_image_url du visual correspondant au profil + Velours.
    })
  </behavior>

  <action>
    Ajouter dans `src/__tests__/ConfiguratorModal.test.tsx` :

    1. FIXTURES (apres les declarations existantes, avant le premier describe) :

    ```typescript
    // === Phase 9 fixtures — multi-angles ===
    const mockModelMultiAngle: ModelWithImages = {
      id: 'model-multi-angle',
      name: 'Canape Milano',
      slug: 'canape-milano',
      price: 1890,
      description: 'Canape modulable design',
      dimensions: '280x95x80',
      is_active: true,
      shopify_url: 'https://www.mobelunique.fr/products/canape-milano',
      created_at: '2026-01-01T00:00:00Z',
      model_images: [
        { id: 'img-34', model_id: 'model-multi-angle', image_url: 'https://test.supabase.co/model-photos/milano-34.jpg', view_type: '3/4', sort_order: 0 },
        { id: 'img-face', model_id: 'model-multi-angle', image_url: 'https://test.supabase.co/model-photos/milano-face.jpg', view_type: 'face', sort_order: 1 },
        { id: 'img-profil', model_id: 'model-multi-angle', image_url: 'https://test.supabase.co/model-photos/milano-profil.jpg', view_type: 'profil', sort_order: 2 },
      ],
    }
    ```

    Creer aussi `mockVisualsMultiAngle` (3 visuels publies pour fabric-modal-001 correspondant aux 3 model_image_id), `mockVisualsPartialAngles` (2 visuels pour fabric-modal-002 — img-34 + img-face, PAS img-profil), et `mockVisualsSingleAngle` (1 seul visual pour un tissu specifique — img-34 seulement).

    Chaque visual doit avoir la structure complete `VisualWithFabricAndImage` avec les proprietes `fabric` et `model_image` imbriquees.

    2. TESTS dans un nouveau `describe('Phase 9 — navigation angles', ...)` :

    Chaque test suit le pattern existant : `const user = userEvent.setup()`, `render(<ConfiguratorModal ... />)`, `screen.getByRole`/`queryByRole` pour verifier.

    Pour distinguer les deux radiogroups (swatches tissu vs thumbnails angle), utiliser `screen.getByRole('radiogroup', { name: 'Choisir l\'angle de vue' })` et `screen.getByRole('radiogroup', { name: 'Choisissez votre tissu' })`.

    Pour verifier l'image principale, utiliser `screen.getByAltText(...)` puis `getAttribute('src')`.

    3. Important — ne PAS modifier ConfiguratorModal.tsx ni le CSS. Uniquement le fichier de tests. Les tests doivent ECHOUER.

    4. Valider que les tests existants (Phase 6 + Phase 8) passent toujours : les nouvelles fixtures ne doivent pas interferer.
  </action>

  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx vitest run src/__tests__/ConfiguratorModal.test.tsx 2>&1 | tail -20</automated>
    Les tests existants (Phase 6, Phase 8) doivent rester VERTS.
    Les nouveaux tests "Phase 9 — navigation angles" doivent etre ROUGES (echec attendu — la fonctionnalite n'est pas implementee).
  </verify>

  <acceptance_criteria>
    - src/__tests__/ConfiguratorModal.test.tsx contient `describe('Phase 9`
    - src/__tests__/ConfiguratorModal.test.tsx contient `mockModelMultiAngle`
    - src/__tests__/ConfiguratorModal.test.tsx contient `model_images:` avec au moins 3 entries ayant view_type '3/4', 'face', 'profil'
    - src/__tests__/ConfiguratorModal.test.tsx contient `Choisir l'angle de vue` (aria-label du radiogroup angles)
    - src/__tests__/ConfiguratorModal.test.tsx contient au minimum 8 tests dans le describe Phase 9 (it(...) calls)
    - Les tests existants (Phase 6 + Phase 8) passent : `npx vitest run src/__tests__/ConfiguratorModal.test.tsx` montre 29+ tests passes
    - Les tests Phase 9 echouent (RED) car la fonctionnalite n'est pas implementee
    - `npx tsc --noEmit` passe sans erreur (les fixtures sont bien typees)
  </acceptance_criteria>

  <done>Les fixtures multi-angles et 10 tests Phase 9 sont crees. Les tests existants sont verts. Les tests Phase 9 sont rouges (fonctionnalite absente). TypeScript compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementation navigation angles (GREEN)</name>
  <files>src/components/public/Catalogue/ConfiguratorModal.tsx, src/components/public/Catalogue/ConfiguratorModal.module.css</files>

  <read_first>
    - src/components/public/Catalogue/ConfiguratorModal.tsx (etat actuel complet — comprendre position des hooks, guard, JSX)
    - src/components/public/Catalogue/ConfiguratorModal.module.css (etat actuel complet — comprendre structure CSS)
    - src/__tests__/ConfiguratorModal.test.tsx (tests RED a faire passer — comprendre les assertions attendues)
    - src/types/database.ts (types ModelImage, VisualWithFabricAndImage)
    - src/app/globals.css (tokens CSS : --spacing-xs, --spacing-sm, --radius-sm, --shadow-md, --color-primary, --transition-fast)
    - .planning/phases/09-navigation-angles/09-CONTEXT.md (decisions D-01 a D-16)
    - .planning/phases/09-navigation-angles/09-RESEARCH.md (patterns complets, code examples, pitfalls)
    - .planning/phases/09-navigation-angles/09-UI-SPEC.md (contrat visuel CSS)
  </read_first>

  <action>
    **A. ConfiguratorModal.tsx — Ajouts dans l'ordre du fichier:**

    1. **Nouvelle fonction utilitaire** (apres `getPrimaryImage`, avant `formatPrice`):
    ```typescript
    export function getPrimaryImageId(model_images: ModelImage[]): string | null {
      if (model_images.length === 0) return null
      const image34 = model_images.find((img) => img.view_type === '3/4')
      if (image34) return image34.id
      return model_images[0]?.id ?? null
    }
    ```

    2. **Nouveau useState** (apres le `useState<string | null>(null)` de selectedFabricId, AVANT le guard `if (!model) return null` — ligne 78 actuelle):
    ```typescript
    const [selectedAngle, setSelectedAngle] = useState<string | null>(null)
    ```

    3. **Modifier le useEffect existant sur `model?.id`** (lignes 73-75 actuelles) pour fusionner le reset angle:
    ```typescript
    useEffect(() => {
      setSelectedFabricId(null)
      if (model) {
        setSelectedAngle(getPrimaryImageId(model.model_images))
      } else {
        setSelectedAngle(null)
      }
    }, [model?.id])
    ```

    4. **Apres le guard `if (!model) return null`**, ajouter les calculs derives (avant `eligibleFabricIds`):
    ```typescript
    // Phase 9 — angles disponibles : tous (sans tissu) ou filtres par rendu publie (avec tissu)
    const availableAngles: ModelImage[] = selectedFabricId
      ? model.model_images.filter((img) =>
          visuals.some(
            (v) =>
              v.model_id === model.id &&
              v.model_image_id === img.id &&
              v.fabric_id === selectedFabricId &&
              v.is_published
          )
        )
      : model.model_images
    ```

    5. **Modifier la logique `currentVisual`** (lignes 96-102 actuelles) pour filtrer aussi par angle:
    ```typescript
    const currentVisual = selectedFabricId && selectedAngle
      ? visuals.find(
          (v) =>
            v.model_id === model.id &&
            v.fabric_id === selectedFabricId &&
            v.model_image_id === selectedAngle &&
            v.is_published
        ) ?? null
      : null
    ```

    6. **Modifier la logique `displayImageUrl`** (lignes 105-107 actuelles) pour prendre en compte l'angle selectionne:
    ```typescript
    const selectedAngleImage = model.model_images.find((img) => img.id === selectedAngle)
    const displayImageUrl =
      currentVisual?.generated_image_url ??
      selectedAngleImage?.image_url ??
      getPrimaryImage(model.model_images)
    const isOriginalFallback = selectedFabricId !== null && currentVisual === null
    ```

    7. **Alt text dynamique avec angle** (remplacer l'alt de l'Image principale, per D-07):
    ```typescript
    const angleLabel = selectedAngleImage?.view_type ?? ''
    const imageAlt =
      currentVisual && selectedFabric
        ? `Canape ${model.name} en tissu ${selectedFabric.name}${angleLabel ? ` \u2014 vue ${angleLabel}` : ''}`
        : selectedAngleImage
          ? `Canape ${model.name} \u2014 vue ${selectedAngleImage.view_type}`
          : `Canape ${model.name}`
    ```

    8. **Handler selection tissu avec preservation angle** (per D-12). Remplacer le simple `onClick={() => setSelectedFabricId(fabric.id)}` sur les swatches par un appel a `handleFabricSelect`:
    ```typescript
    const handleFabricSelect = (fabricId: string) => {
      setSelectedFabricId(fabricId)
      const hasRenderForCurrentAngle =
        selectedAngle !== null &&
        visuals.some(
          (v) =>
            v.model_id === model.id &&
            v.fabric_id === fabricId &&
            v.model_image_id === selectedAngle &&
            v.is_published
        )
      if (!hasRenderForCurrentAngle) {
        const anglesForFabric = model.model_images.filter((img) =>
          visuals.some(
            (v) =>
              v.model_id === model.id &&
              v.model_image_id === img.id &&
              v.fabric_id === fabricId &&
              v.is_published
          )
        )
        const default34 = anglesForFabric.find((img) => img.view_type === '3/4')
        setSelectedAngle(default34?.id ?? anglesForFabric[0]?.id ?? null)
      }
    }
    ```
    Mettre a jour le onClick du swatch : `onClick={() => handleFabricSelect(fabric.id)}`

    9. **JSX — Image principale** : ajouter `key={displayImageUrl}` et `className={styles.imageMain}` sur le `<Image>` dans `.imageWrapper`, et remplacer l'alt statique par `imageAlt`.

    10. **JSX — Rangee de thumbnails** : ajouter juste apres le closing `</div>` de `.imageWrapper` et avant `</div>` de `.inner` (cote gauche desktop):
    ```tsx
    {availableAngles.length > 1 && (
      <div
        className={styles.thumbnailRow}
        role="radiogroup"
        aria-label="Choisir l'angle de vue"
      >
        {availableAngles.map((img) => (
          <button
            key={img.id}
            type="button"
            role="radio"
            aria-checked={selectedAngle === img.id}
            aria-label={`Vue ${img.view_type}`}
            className={`${styles.thumbnail} ${selectedAngle === img.id ? styles.thumbnailActive : ''}`}
            onClick={() => setSelectedAngle(img.id)}
          >
            <Image
              src={img.image_url}
              alt=""
              fill
              style={{ objectFit: 'cover' }}
              sizes="72px"
            />
          </button>
        ))}
      </div>
    )}
    ```

    **ATTENTION layout mobile vs desktop:** La rangee thumbnails doit etre DANS la colonne gauche (avec l'image) en desktop. En mobile c'est naturellement sous l'image (flex-direction: column). Placer le JSX thumbnailRow juste apres `.imageWrapper` mais AVANT `.body`. Wrapper `.imageWrapper` + `.thumbnailRow` dans un nouveau `<div className={styles.leftColumn}>` si necessaire pour le layout 2 colonnes desktop, OU placer directement apres imageWrapper dans .inner.

    Verifier que le layout 2 colonnes desktop CSS (`@media (min-width: 640px)`) fonctionne avec la rangee de thumbnails ajoutee. La `.imageWrapper` a `width: 50%` et `flex-shrink: 0` en desktop. La thumbnailRow doit etre dans cette meme colonne gauche. Solution recommandee : wrapper l'imageWrapper et thumbnailRow dans un div `.leftColumn` qui prend `width: 50%; flex-shrink: 0` en desktop, et retirer ces proprietes de `.imageWrapper` en desktop.

    **B. ConfiguratorModal.module.css — Ajouts (a la fin, avant le bloc responsive):**

    Ajouter le bloc CSS exact tel que specifie dans 09-UI-SPEC.md section "CSS a creer":

    ```css
    /* === Phase 9 — Navigation angles === */

    .leftColumn {
      display: flex;
      flex-direction: column;
    }

    .thumbnailRow {
      display: flex;
      flex-direction: row;
      gap: var(--spacing-xs);
      margin-top: var(--spacing-sm);
      overflow-x: auto;
      scrollbar-width: none;
      padding: 0 var(--spacing-sm);
    }

    .thumbnailRow::-webkit-scrollbar {
      display: none;
    }

    .thumbnail {
      position: relative;
      flex-shrink: 0;
      width: 72px;
      height: 54px;
      border-radius: var(--radius-sm);
      overflow: hidden;
      border: 3px solid transparent;
      cursor: pointer;
      background: none;
      padding: 0;
      transition: box-shadow var(--transition-fast);
    }

    .thumbnail:hover {
      box-shadow: var(--shadow-md);
    }

    .thumbnailActive {
      border-color: var(--color-primary);
      outline: 2px solid var(--color-primary);
      outline-offset: 2px;
    }

    .imageMain {
      animation: imageFadeIn 200ms ease;
    }

    @keyframes imageFadeIn {
      from { opacity: 0; }
      to   { opacity: 1; }
    }
    ```

    **C. CSS responsive — Modifier le media query desktop `@media (min-width: 640px)`:**

    Ajouter dans le bloc `@media (min-width: 640px)`:
    ```css
    .leftColumn {
      width: 50%;
      flex-shrink: 0;
    }

    .imageWrapper {
      width: 100%;  /* remplace le 50% actuel — maintenant dans leftColumn */
    }
    ```

    Garder `min-height: 300px`, `border-radius`, `aspect-ratio: auto` sur `.imageWrapper` desktop.

    Retirer `width: 50%` et `flex-shrink: 0` de la regle `.imageWrapper` desktop existante (ces proprietes passent a `.leftColumn`).

    **D. CSS responsive — Mobile `@media (max-width: 639px)` :**

    La thumbnailRow sera naturellement sous l'image en mobile (flex column). Ajouter dans le bloc mobile si necessaire:
    ```css
    .thumbnailRow {
      padding: 0 var(--spacing-sm);
    }
    ```

    **VERIFICATION CRITIQUE apres implementation:**
    - Executer `npx vitest run src/__tests__/ConfiguratorModal.test.tsx` — TOUS les tests (Phase 6 + Phase 8 + Phase 9) doivent etre VERTS
    - Executer `npm test` — suite complete verte
    - Executer `npx tsc --noEmit` — zero erreur TypeScript
    - Executer `npm run build` — build production OK
  </action>

  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx vitest run src/__tests__/ConfiguratorModal.test.tsx 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -5</automated>
    Tous les tests ConfiguratorModal passent (Phase 6 + Phase 8 + Phase 9 = ~40 tests).
    TypeScript compile sans erreur.
  </verify>

  <acceptance_criteria>
    - ConfiguratorModal.tsx contient `useState<string | null>(null)` pour selectedAngle (deuxieme useState apres selectedFabricId)
    - ConfiguratorModal.tsx contient `getPrimaryImageId` exportee
    - ConfiguratorModal.tsx contient `availableAngles` calcule depuis selectedFabricId et visuals
    - ConfiguratorModal.tsx contient `v.model_image_id === selectedAngle` dans le filtre currentVisual
    - ConfiguratorModal.tsx contient `handleFabricSelect` avec logique `hasRenderForCurrentAngle`
    - ConfiguratorModal.tsx contient `role="radiogroup"` avec `aria-label="Choisir l'angle de vue"`
    - ConfiguratorModal.tsx contient `aria-checked={selectedAngle === img.id}` sur les thumbnails
    - ConfiguratorModal.tsx contient `key={displayImageUrl}` sur le Image principal
    - ConfiguratorModal.tsx contient `className={styles.imageMain}` sur le Image principal
    - ConfiguratorModal.tsx contient `vue ${angleLabel}` ou equivalent dans imageAlt (per D-07)
    - ConfiguratorModal.tsx contient `sizes="72px"` sur les Image thumbnails
    - ConfiguratorModal.tsx contient `availableAngles.length > 1` pour conditionner l'affichage thumbnailRow (per D-11)
    - ConfiguratorModal.module.css contient `.thumbnailRow` avec `display: flex` et `overflow-x: auto`
    - ConfiguratorModal.module.css contient `.thumbnail` avec `width: 72px` et `height: 54px`
    - ConfiguratorModal.module.css contient `.thumbnailActive` avec `border-color: var(--color-primary)`
    - ConfiguratorModal.module.css contient `@keyframes imageFadeIn` avec `200ms`
    - ConfiguratorModal.module.css contient `.imageMain` avec `animation: imageFadeIn 200ms ease`
    - `npx vitest run src/__tests__/ConfiguratorModal.test.tsx` sort 0 avec 0 echecs
    - `npx tsc --noEmit` sort 0
    - `npm run build` sort 0
  </acceptance_criteria>

  <done>Navigation angles implementee : thumbnails cliquables sous l'image, crossfade 200ms, filtrage par tissu, preservation angle, alt text dynamique. Tous les tests verts. Build propre.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/__tests__/ConfiguratorModal.test.tsx` — tous tests verts (Phase 6 + 8 + 9)
2. `npm test` — suite complete verte
3. `npx tsc --noEmit` — zero erreur TypeScript
4. `npm run build` — build production sans erreur
5. Verification manuelle par l'utilisateur : ouvrir le modal, selectionner un tissu, cliquer les thumbnails, verifier le crossfade et le changement d'image
</verification>

<success_criteria>
- CONF-04 : Le modal affiche le rendu IA publie pour le tissu ET l'angle selectionnes
- CONF-06 : Le client navigue entre les angles via thumbnails cliquables
- D-01 a D-16 : Toutes les decisions verrouillees respectees
- Zero regression sur les tests existants (Phase 6 + Phase 8)
- Build production sans erreur
</success_criteria>

<output>
After completion, create `.planning/phases/09-navigation-angles/09-01-SUMMARY.md`
</output>
