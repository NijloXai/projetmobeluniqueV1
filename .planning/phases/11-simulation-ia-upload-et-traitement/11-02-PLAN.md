---
phase: 11-simulation-ia-upload-et-traitement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/public/Catalogue/ConfiguratorModal.tsx
  - src/components/public/Catalogue/ConfiguratorModal.module.css
autonomous: true
requirements:
  - SIM-01

must_haves:
  truths:
    - "L'utilisateur voit un CTA 'Visualiser chez moi' sous le bouton Shopify dans le configurateur"
    - "Le CTA ouvre une etape simulation avec zone upload drag & drop"
    - "L'utilisateur peut glisser ou choisir une photo JPEG/PNG/HEIC de maximum 15 Mo"
    - "L'erreur s'affiche en francais si le fichier est trop gros ou au mauvais format"
    - "Apres selection, un apercu s'affiche avec bouton 'Lancer la simulation'"
    - "Pendant la generation, une barre de progression simulee avec 3 etapes s'affiche"
    - "L'utilisateur peut annuler la generation en cours"
    - "En cas d'erreur IA, un message inline et un bouton 'Reessayer' s'affichent"
    - "Le bouton retour ramene a l'etape tissu sans perdre la selection"
    - "Le bandeau rappel config affiche le mini swatch et nom du tissu"
  artifacts:
    - path: "src/components/public/Catalogue/ConfiguratorModal.tsx"
      provides: "Etape simulation complete avec state machine 5 etats, DnD, fetch avec AbortController"
      contains: "simulationState"
    - path: "src/components/public/Catalogue/ConfiguratorModal.module.css"
      provides: "Styles zone upload, barre progression, bandeau rappel, etats idle/preview/generating/error"
      contains: "uploadZone"
  key_links:
    - from: "ConfiguratorModal.tsx"
      to: "/api/simulate"
      via: "fetch('/api/simulate', { method: 'POST', body: formData, signal })"
      pattern: "fetch.*api/simulate"
    - from: "ConfiguratorModal.tsx"
      to: "ConfiguratorModal.module.css"
      via: "CSS Modules import styles"
      pattern: "styles\\.uploadZone"
---

<objective>
Etendre ConfiguratorModal avec une etape simulation complete : CTA "Visualiser chez moi", zone upload drag & drop, preview photo, barre de progression simulee, annulation, gestion d'erreurs inline. Couvre D-01 a D-19 cote client.

Purpose: Permettre a l'utilisateur d'uploader une photo de son salon et lancer la simulation IA directement depuis le configurateur.
Output: ConfiguratorModal etendu avec etape simulation fonctionnelle (tous etats sauf 'done' qui est Phase 12).
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-simulation-ia-upload-et-traitement/11-CONTEXT.md
@.planning/phases/11-simulation-ia-upload-et-traitement/11-RESEARCH.md
@.planning/phases/11-simulation-ia-upload-et-traitement/11-UI-SPEC.md

<interfaces>
<!-- Composant existant a etendre -->
From src/components/public/Catalogue/ConfiguratorModal.tsx (320 lignes):
```typescript
interface ConfiguratorModalProps {
  model: ModelWithImages | null
  onClose: () => void
  fabrics: Fabric[]
  visuals: VisualWithFabricAndImage[]
}

// State existant dans le composant :
const [selectedFabricId, setSelectedFabricId] = useState<string | null>(null)
const [selectedAngle, setSelectedAngle] = useState<string | null>(null)
// Computed :
// selectedFabric: Fabric | null (lookup depuis eligibleFabrics)
// displayImageUrl: string (visual IA ou photo angle)
// model.model_images: ModelImage[]
```

From src/types/database.ts:
```typescript
// Fabric.name: string — nom du tissu
// Fabric.swatch_url: string | null — URL swatch miniature
// ModelWithImages: model avec model_images[]
```

From src/lib/utils.ts:
```typescript
export function getPrimaryImage(model_images: ModelImage[]): string | null
export function calculatePrice(basePrice: number, isPremium: boolean): number
export function formatPrice(price: number): string
```

From src/app/globals.css (tokens CSS utilises):
```css
--color-primary: #E49400;
--color-background-alt: #F8F4EE;
--color-error: #BA1A1A;
--color-muted: #888888;
--color-overlay: rgba(0,0,0,0.55);
--surface-container: #F0EDEA;
--outline-variant: #D8C3AD;
--radius-sm: 4px; --radius-md: 8px; --radius-lg: 12px;
--spacing-xs: 0.25rem; --spacing-sm: 0.5rem; --spacing-md: 1rem;
--spacing-lg: 1.5rem; --spacing-xl: 2rem; --spacing-2xl: 3rem;
--font-size-xs: 0.75rem; --font-size-sm: 0.875rem; --font-size-base: 1rem;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter les styles CSS simulation dans ConfiguratorModal.module.css</name>
  <files>src/components/public/Catalogue/ConfiguratorModal.module.css</files>
  <read_first>
    - src/components/public/Catalogue/ConfiguratorModal.module.css (etat actuel complet — 344 lignes)
    - src/app/globals.css (tokens CSS — 80 premieres lignes)
    - .planning/phases/11-simulation-ia-upload-et-traitement/11-UI-SPEC.md (spec visuelle complete)
  </read_first>
  <action>
Ajouter les classes CSS suivantes a la FIN de ConfiguratorModal.module.css, apres la section `/* === Responsive === */` existante. Inserer un commentaire separateur `/* === Phase 11 — Simulation IA === */` avant les nouvelles classes.

**Classes a creer (toutes les valeurs sont exactes, issues de la UI-SPEC) :**

1. `.ctaSimulation` — CTA "Visualiser chez moi" (D-02) :
   - `display: block; width: 100%; margin-top: var(--spacing-sm); padding: 0 var(--spacing-lg); height: 48px; line-height: 48px; text-align: center; font-size: var(--font-size-base); font-weight: 600; color: var(--color-text); background: transparent; border: 1px solid var(--color-text); border-radius: var(--radius-md); cursor: pointer; transition: background-color 150ms ease, color 150ms ease;`
   - Hover : `background: var(--color-text); color: var(--color-background);`
   - Focus-visible : `outline: 2px solid var(--color-primary); outline-offset: 2px;`

2. `.simulationStep` — Wrapper de l'etape simulation (animation D-01) :
   - `animation: slideIn 200ms ease;`
   - Declarer `@keyframes slideIn { from { opacity: 0; transform: translateX(-8px); } to { opacity: 1; transform: translateX(0); } }`

3. `.configRecap` — Bandeau rappel config (D-05) :
   - `display: flex; align-items: center; gap: var(--spacing-sm); padding: var(--spacing-md); background: var(--color-background-alt); border-radius: var(--radius-md); margin-bottom: var(--spacing-md);`

4. `.configRecapSwatch` — Mini swatch dans bandeau :
   - `width: 24px; height: 24px; border-radius: var(--radius-sm); object-fit: cover; flex-shrink: 0;`

5. `.configRecapText` — Texte du bandeau :
   - `font-size: var(--font-size-sm); font-weight: 600; color: var(--color-text); flex: 1;`

6. `.configRecapLink` — Lien "Modifier" :
   - `font-size: var(--font-size-sm); font-weight: 600; color: var(--color-primary); background: none; border: none; cursor: pointer; text-decoration: none;`
   - Hover : `text-decoration: underline;`

7. `.backButton` — Bouton retour (D-03) :
   - `font-size: var(--font-size-sm); font-weight: 600; color: var(--color-muted); background: none; border: none; cursor: pointer; padding: var(--spacing-xs) 0; margin-bottom: var(--spacing-md);`
   - Hover : `color: var(--color-text);`

8. `.uploadExplainer` — Texte explicatif (D-06) :
   - `font-size: var(--font-size-base); font-weight: 400; color: var(--color-text); margin-bottom: var(--spacing-md); line-height: 1.6;`

9. `.uploadZone` — Zone DnD idle (D-08) :
   - `position: relative; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: var(--spacing-sm); padding: var(--spacing-2xl) var(--spacing-lg); background: var(--color-background-alt); border: 2px dashed var(--outline-variant); border-radius: var(--radius-lg); cursor: pointer; transition: border-color 150ms ease, background-color 150ms ease;`

10. `.uploadZoneDragging` — Etat isDragging :
    - `border-color: var(--color-primary); background: rgba(228, 148, 0, 0.05);`

11. `.uploadIcon` — Icone upload :
    - `width: 32px; height: 32px; color: var(--color-muted);`

12. `.uploadText` — Texte "Glissez votre photo ici" :
    - `font-size: var(--font-size-base); font-weight: 400; color: var(--color-muted);`

13. `.uploadOr` — Texte "ou" :
    - `font-size: var(--font-size-sm); color: var(--color-muted);`

14. `.uploadButton` — Bouton "Choisir une photo" :
    - `height: 44px; padding: 0 var(--spacing-lg); font-size: var(--font-size-base); font-weight: 600; color: var(--color-background); background: var(--color-text); border: none; border-radius: var(--radius-md); cursor: pointer; transition: opacity 150ms ease;`
    - Hover : `opacity: 0.85;`

15. `.uploadFormats` — Texte formats acceptes :
    - `font-size: var(--font-size-xs); color: var(--color-muted);`

16. `.uploadHiddenInput` — Input file cache :
    - `position: absolute; width: 0; height: 0; opacity: 0; overflow: hidden;`

17. `.previewContainer` — Container preview (D-09) :
    - `position: relative; width: 100%; aspect-ratio: 4 / 3; border-radius: var(--radius-lg); overflow: hidden;`

18. `.previewImage` — Image preview :
    - `width: 100%; height: 100%; object-fit: cover; animation: imageFadeIn 200ms ease;` (reutilise le keyframe existant)

19. `.previewOverlay` — Gradient overlay bas pour lien "Changer de photo" :
    - `position: absolute; bottom: 0; left: 0; right: 0; height: 30%; background: linear-gradient(transparent, rgba(0,0,0,0.4)); display: flex; align-items: flex-end; justify-content: flex-end; padding: var(--spacing-md);`

20. `.changePhotoLink` — Lien "Changer de photo" :
    - `font-size: var(--font-size-sm); font-weight: 600; color: #FFFFFF; text-decoration: underline; background: none; border: none; cursor: pointer;`

21. `.launchButton` — Bouton "Lancer la simulation" :
    - `display: block; width: 100%; height: 48px; margin-top: var(--spacing-md); font-size: var(--font-size-base); font-weight: 600; color: var(--color-text); background: var(--color-primary); border: none; border-radius: var(--radius-md); cursor: pointer; transition: opacity 150ms ease;`
    - Hover : `opacity: 0.9;`
    - Disabled : `opacity: 0.5; cursor: not-allowed;`

22. `.generatingContainer` — Container etat generating (D-12) :
    - `position: relative; width: 100%; aspect-ratio: 4 / 3; border-radius: var(--radius-lg); overflow: hidden;`

23. `.generatingOverlay` — Overlay sombre sur photo salon :
    - `position: absolute; inset: 0; background: rgba(0,0,0,0.65); border-radius: var(--radius-lg); display: flex; flex-direction: column; align-items: center; justify-content: center; gap: var(--spacing-md); padding: var(--spacing-lg);`

24. `.progressBarTrack` — Barre progression track :
    - `width: 80%; max-width: 240px; height: 6px; background: var(--surface-container); border-radius: var(--radius-full); overflow: hidden;`

25. `.progressBarFill` — Barre progression fill :
    - `height: 100%; background: var(--color-primary); border-radius: var(--radius-full); transition: width 300ms ease;`

26. `.progressPercent` — Indicateur pourcentage :
    - `font-size: var(--font-size-xs); font-weight: 600; color: var(--color-muted);`

27. `.stepList` — Liste 3 etapes :
    - `display: flex; flex-direction: column; gap: var(--spacing-xs); margin-top: var(--spacing-sm);`

28. `.stepItem` — Item etape (flex row, icone + texte) :
    - `display: flex; align-items: center; gap: var(--spacing-sm); font-size: var(--font-size-sm);`

29. `.stepDone` — Etape terminee :
    - `font-weight: 600; color: rgba(255,255,255,0.7);`

30. `.stepActive` — Etape active :
    - `font-weight: 600; color: #FFFFFF;`

31. `.stepPending` — Etape en attente :
    - `font-weight: 400; color: rgba(255,255,255,0.5);`

32. `.stepIcon` — Icone etape (16x16) :
    - `width: 16px; height: 16px; flex-shrink: 0;`

33. `.spinner` — Animation spinner pour etape active :
    - `animation: spinRotate 1s linear infinite;`
    - Declarer `@keyframes spinRotate { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }`

34. `.cancelButton` — Bouton "Annuler" (D-15) :
    - `height: 44px; padding: 0 var(--spacing-lg); font-size: var(--font-size-sm); font-weight: 600; color: #FFFFFF; background: transparent; border: 1px solid rgba(255,255,255,0.4); border-radius: var(--radius-md); cursor: pointer; margin-top: var(--spacing-md); transition: background-color 150ms ease;`
    - Hover : `background: rgba(255,255,255,0.1);`

35. `.errorMessage` — Message erreur inline (D-17) :
    - `font-size: var(--font-size-sm); color: var(--color-error); margin-top: var(--spacing-sm);`

36. `.retryButton` — Bouton "Reessayer" :
    - Meme style que `.launchButton` mais avec `margin-top: var(--spacing-sm);`

37. `.simulationTitle` — Titre "Simulation" dans colonne droite :
    - `font-size: var(--font-size-xl); font-weight: 600; color: var(--color-text); line-height: 1.2; margin: 0 0 var(--spacing-md) 0;`

**Section responsive — ajouter dans le media query `@media (min-width: 640px)` existant :**
- `.uploadZone` : padding `var(--spacing-2xl) var(--spacing-lg)` (deja par defaut, pas de changement)
- `.generatingContainer` : `aspect-ratio: auto; min-height: 300px;` (pour correspondre a imageWrapper desktop)
- `.previewContainer` : `aspect-ratio: auto; min-height: 300px;`

**Section mobile — ajouter dans le media query `@media (max-width: 639px)` existant :**
- `.previewContainer` : `max-height: 240px;`
- `.generatingContainer` : `max-height: 240px;`
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ConfiguratorModal.module.css contient le commentaire `/* === Phase 11`
    - ConfiguratorModal.module.css contient `.uploadZone {`
    - ConfiguratorModal.module.css contient `.uploadZoneDragging {`
    - ConfiguratorModal.module.css contient `.previewContainer {`
    - ConfiguratorModal.module.css contient `.generatingOverlay {`
    - ConfiguratorModal.module.css contient `.progressBarFill {`
    - ConfiguratorModal.module.css contient `.cancelButton {`
    - ConfiguratorModal.module.css contient `.errorMessage {`
    - ConfiguratorModal.module.css contient `.ctaSimulation {`
    - ConfiguratorModal.module.css contient `.configRecap {`
    - ConfiguratorModal.module.css contient `.launchButton {`
    - ConfiguratorModal.module.css contient `@keyframes slideIn`
    - ConfiguratorModal.module.css contient `@keyframes spinRotate`
    - ConfiguratorModal.module.css contient `.backButton {`
    - ConfiguratorModal.module.css contient `.changePhotoLink {`
    - `npx tsc --noEmit` sort sans erreur
  </acceptance_criteria>
  <done>Toutes les classes CSS de la simulation (37 classes + 2 keyframes + adaptations responsive) sont ajoutees a ConfiguratorModal.module.css. Le fichier compile sans erreur.</done>
</task>

<task type="auto">
  <name>Task 2: Etendre ConfiguratorModal.tsx — state machine, DnD, preview, step switching</name>
  <files>src/components/public/Catalogue/ConfiguratorModal.tsx</files>
  <read_first>
    - src/components/public/Catalogue/ConfiguratorModal.tsx (etat actuel complet — 320 lignes)
    - src/components/public/Catalogue/ConfiguratorModal.module.css (apres Task 1 — verifier les noms de classes)
    - src/lib/utils.ts (getPrimaryImage, formatPrice, calculatePrice)
    - src/types/database.ts (types Fabric, ModelWithImages)
    - .planning/phases/11-simulation-ia-upload-et-traitement/11-RESEARCH.md (patterns DnD, AbortController, progress timer)
    - .planning/phases/11-simulation-ia-upload-et-traitement/11-UI-SPEC.md (spec complete des etats)
  </read_first>
  <action>
Modifier ConfiguratorModal.tsx pour ajouter l'etape simulation. Le composant reste un seul fichier (D-01). Les modifications sont :

**A. Nouveaux imports et types (en haut du fichier) :**
Aucun nouveau import de package — tout est natif React. Ajouter le type :
```typescript
type SimulationState = 'idle' | 'preview' | 'generating' | 'done' | 'error'
```

**B. Nouveaux states (apres les useState existants, avant le useEffect de reset) :**
```typescript
// Phase 11 — Simulation state
const [modalStep, setModalStep] = useState<'configurator' | 'simulation'>('configurator')
const [simulationState, setSimulationState] = useState<SimulationState>('idle')
const [selectedFile, setSelectedFile] = useState<File | null>(null)
const [previewUrl, setPreviewUrl] = useState<string | null>(null)
const [progress, setProgress] = useState(0)
const [progressStage, setProgressStage] = useState(0) // 0, 1, 2 pour les 3 etapes
const [errorMessage, setErrorMessage] = useState<string | null>(null)
const [resultBlobUrl, setResultBlobUrl] = useState<string | null>(null)
const [isDragging, setIsDragging] = useState(false)

const abortControllerRef = useRef<AbortController | null>(null)
const progressTimerRef = useRef<ReturnType<typeof setInterval> | null>(null)
const progressPhase2TimerRef = useRef<ReturnType<typeof setInterval> | null>(null)
const dragCounterRef = useRef(0)
const fileInputRef = useRef<HTMLInputElement>(null)
```

**C. Reset modalStep quand le modele change :**
Dans le useEffect existant qui reset `selectedFabricId` (ligne ~68-84), ajouter apres `setSelectedFabricId(null)` :
```typescript
setModalStep('configurator')
setSimulationState('idle')
setSelectedFile(null)
if (previewUrl) URL.revokeObjectURL(previewUrl)
setPreviewUrl(null)
if (resultBlobUrl) URL.revokeObjectURL(resultBlobUrl)
setResultBlobUrl(null)
setProgress(0)
setProgressStage(0)
setErrorMessage(null)
```

**D. Cleanup useEffect pour Object URLs (nouveau, apres les useEffect existants) :**
```typescript
useEffect(() => {
  return () => {
    if (previewUrl) URL.revokeObjectURL(previewUrl)
    if (resultBlobUrl) URL.revokeObjectURL(resultBlobUrl)
    if (progressTimerRef.current) clearInterval(progressTimerRef.current)
    if (progressPhase2TimerRef.current) clearInterval(progressPhase2TimerRef.current)
    if (abortControllerRef.current) abortControllerRef.current.abort()
  }
}, []) // eslint-disable-line react-hooks/exhaustive-deps
```

**E. File validation function (avant le return) :**
```typescript
const ACCEPTED_TYPES = new Set(['image/jpeg', 'image/png', 'image/heic', 'image/heif'])
const MAX_SIZE_BYTES = 15 * 1024 * 1024

const validateFile = useCallback((file: File): string | null => {
  if (file.size > MAX_SIZE_BYTES) {
    return 'Ce fichier depasse 15 Mo. Choisissez une photo plus legere.'
  }
  const isAcceptedType = ACCEPTED_TYPES.has(file.type)
  const isHeicByExtension = /\.(heic|heif)$/i.test(file.name)
  if (!isAcceptedType && !isHeicByExtension) {
    return 'Format non supporte. Utilisez JPEG, PNG ou HEIC.'
  }
  return null
}, [])
```

**F. File selection handler :**
```typescript
const handleFileSelected = useCallback((file: File) => {
  const error = validateFile(file)
  if (error) {
    setErrorMessage(error)
    setSimulationState('idle')
    return
  }
  if (previewUrl) URL.revokeObjectURL(previewUrl)
  const url = URL.createObjectURL(file)
  setSelectedFile(file)
  setPreviewUrl(url)
  setSimulationState('preview')
  setErrorMessage(null)
}, [previewUrl, validateFile])
```

**G. Drag & drop handlers (compteur anti-flicker — RESEARCH.md Pattern 2) :**
```typescript
const handleDragEnter = useCallback((e: React.DragEvent<HTMLDivElement>) => {
  e.preventDefault()
  dragCounterRef.current++
  if (dragCounterRef.current === 1) setIsDragging(true)
}, [])

const handleDragLeave = useCallback((e: React.DragEvent<HTMLDivElement>) => {
  e.preventDefault()
  dragCounterRef.current--
  if (dragCounterRef.current === 0) setIsDragging(false)
}, [])

const handleDragOver = useCallback((e: React.DragEvent<HTMLDivElement>) => {
  e.preventDefault()
}, [])

const handleDrop = useCallback((e: React.DragEvent<HTMLDivElement>) => {
  e.preventDefault()
  dragCounterRef.current = 0
  setIsDragging(false)
  const file = e.dataTransfer.files[0]
  if (file) handleFileSelected(file)
}, [handleFileSelected])

const handleInputChange = useCallback((e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0]
  if (file) handleFileSelected(file)
  e.target.value = '' // Reset pour permettre re-selection du meme fichier
}, [handleFileSelected])
```

**H. Progress timer functions (D-13 — 2 phases de vitesse) :**
```typescript
const stopProgressTimer = useCallback(() => {
  if (progressTimerRef.current) { clearInterval(progressTimerRef.current); progressTimerRef.current = null }
  if (progressPhase2TimerRef.current) { clearInterval(progressPhase2TimerRef.current); progressPhase2TimerRef.current = null }
}, [])

const startProgressTimer = useCallback(() => {
  stopProgressTimer()
  let current = 0
  setProgress(0)
  setProgressStage(0)

  // Phase rapide : 0-30% en ~1s (intervalle 50ms, +1.5 par tick)
  progressTimerRef.current = setInterval(() => {
    current += 1.5
    if (current >= 30) {
      current = 30
      if (progressTimerRef.current) clearInterval(progressTimerRef.current)
      progressTimerRef.current = null
      setProgressStage(1)

      // Phase lente : 30-70% en ~4s (intervalle 200ms, +1 par tick)
      progressPhase2TimerRef.current = setInterval(() => {
        current += 1
        if (current >= 70) {
          current = 70
          if (progressPhase2TimerRef.current) clearInterval(progressPhase2TimerRef.current)
          progressPhase2TimerRef.current = null
          setProgressStage(2)
        }
        setProgress(Math.round(current))
      }, 200)
    }
    setProgress(Math.round(current))
  }, 50)
}, [stopProgressTimer])
```

**I. Fetch simulation handler (D-15, D-16, D-17, D-18) :**
```typescript
const handleLancerSimulation = useCallback(async () => {
  if (!selectedFile || !model) return
  if (abortControllerRef.current) abortControllerRef.current.abort()

  const controller = new AbortController()
  abortControllerRef.current = controller

  setSimulationState('generating')
  setErrorMessage(null)
  startProgressTimer()

  try {
    const formData = new FormData()
    formData.append('image', selectedFile)
    formData.append('model_id', model.id)
    if (selectedFabricId) formData.append('fabric_id', selectedFabricId)

    const response = await fetch('/api/simulate', {
      method: 'POST',
      body: formData,
      signal: controller.signal,
    })

    if (!response.ok) {
      const data = await response.json().catch(() => ({ error: 'La simulation a echoue. Verifiez votre connexion et reessayez.' }))
      throw new Error(data.error ?? 'La simulation a echoue. Verifiez votre connexion et reessayez.')
    }

    const blob = await response.blob()
    if (resultBlobUrl) URL.revokeObjectURL(resultBlobUrl)
    const url = URL.createObjectURL(blob)

    stopProgressTimer()
    setProgress(100)
    setProgressStage(2)
    setResultBlobUrl(url)
    setSimulationState('done')
  } catch (err) {
    stopProgressTimer()
    if (err instanceof Error && err.name === 'AbortError') {
      setSimulationState('preview')
      setProgress(0)
      setProgressStage(0)
    } else {
      const message = err instanceof Error ? err.message : 'La simulation a echoue. Verifiez votre connexion et reessayez.'
      setErrorMessage(message)
      setSimulationState('error')
    }
  }
}, [selectedFile, model, selectedFabricId, startProgressTimer, stopProgressTimer, resultBlobUrl])

const handleAnnuler = useCallback(() => {
  if (abortControllerRef.current) abortControllerRef.current.abort()
}, [])

const handleReessayer = useCallback(() => {
  handleLancerSimulation()
}, [handleLancerSimulation])
```

**J. Step switching handlers (D-01, D-02, D-03) :**
```typescript
const handleGoToSimulation = useCallback(() => {
  setModalStep('simulation')
  setSimulationState('idle')
  setErrorMessage(null)
}, [])

const handleBackToConfigurator = useCallback(() => {
  // Annuler toute generation en cours
  if (abortControllerRef.current) abortControllerRef.current.abort()
  stopProgressTimer()
  setModalStep('configurator')
  // Conserver la selection tissu (D-03)
  // Reset simulation state mais garder le fichier si deja selectionne
  setSimulationState(selectedFile ? 'preview' : 'idle')
  setProgress(0)
  setProgressStage(0)
  setErrorMessage(null)
}, [stopProgressTimer, selectedFile])
```

**K. SVG inline icons (pas de nouvelle dependance) :**
Declarer 3 petits SVG en tant que constantes JSX avant le return :
```typescript
const UploadIcon = (
  <svg className={styles.uploadIcon} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
    <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
    <polyline points="17 8 12 3 7 8" />
    <line x1="12" y1="3" x2="12" y2="15" />
  </svg>
)

const CheckIcon = (
  <svg className={styles.stepIcon} viewBox="0 0 16 16" fill="none" stroke="var(--color-primary)" strokeWidth="2" strokeLinecap="round" aria-hidden="true">
    <polyline points="3 8 6.5 11.5 13 5" />
  </svg>
)

const SpinnerIcon = (
  <svg className={`${styles.stepIcon} ${styles.spinner}`} viewBox="0 0 16 16" fill="none" stroke="#FFFFFF" strokeWidth="2" strokeLinecap="round" aria-hidden="true">
    <path d="M8 2a6 6 0 1 1-5.2 3" />
  </svg>
)

const PendingIcon = (
  <svg className={styles.stepIcon} viewBox="0 0 16 16" fill="none" stroke="rgba(255,255,255,0.5)" strokeWidth="1.5" aria-hidden="true">
    <circle cx="8" cy="8" r="5" />
  </svg>
)
```

**L. JSX conditionnel — modifier le return :**

Le JSX existant du `<div className={styles.inner}>` doit etre conditionne par `modalStep`. Quand `modalStep === 'configurator'`, afficher le contenu actuel INCHANGE (leftColumn + body) PLUS le CTA "Visualiser chez moi". Quand `modalStep === 'simulation'`, afficher le contenu simulation.

1. **Ajouter le CTA "Visualiser chez moi"** dans la colonne droite (`.body`), APRES le lien Shopify existant :
```jsx
<button
  type="button"
  className={styles.ctaSimulation}
  onClick={handleGoToSimulation}
>
  Visualiser chez moi
</button>
```

2. **Conditionner le contenu de `.inner`** : encapsuler tout le contenu existant de `.inner` dans `{modalStep === 'configurator' && ( ... )}` et ajouter `{modalStep === 'simulation' && ( ... )}` pour le rendu simulation.

3. **Contenu simulation (modalStep === 'simulation')** dans un fragment `<div className={styles.simulationStep}>` contenant deux colonnes :

**Colonne gauche (zone upload/preview/generating) :**
```jsx
<div className={styles.leftColumn}>
  {/* Etat idle : zone upload DnD */}
  {simulationState === 'idle' && (
    <div
      className={`${styles.uploadZone} ${isDragging ? styles.uploadZoneDragging : ''}`}
      onDragEnter={handleDragEnter}
      onDragLeave={handleDragLeave}
      onDragOver={handleDragOver}
      onDrop={handleDrop}
      role="button"
      tabIndex={0}
      onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); fileInputRef.current?.click() } }}
      aria-label="Zone de depot de fichier"
    >
      {UploadIcon}
      <span className={styles.uploadText}>Glissez votre photo ici</span>
      <span className={styles.uploadOr}>ou</span>
      <button type="button" className={styles.uploadButton} onClick={() => fileInputRef.current?.click()}>
        Choisir une photo
      </button>
      <span className={styles.uploadFormats} id="upload-formats">JPEG, PNG, HEIC — max 15 Mo</span>
      <input
        ref={fileInputRef}
        type="file"
        accept="image/jpeg,image/png,image/heic,image/heif"
        className={styles.uploadHiddenInput}
        onChange={handleInputChange}
        aria-label="Selectionner une photo de votre salon"
        aria-describedby="upload-formats"
      />
    </div>
  )}

  {/* Etat preview : apercu image */}
  {(simulationState === 'preview' || simulationState === 'error') && previewUrl && (
    <>
      <div className={styles.previewContainer}>
        <img src={previewUrl} alt="Apercu de votre salon" className={styles.previewImage} />
        <div className={styles.previewOverlay}>
          <button type="button" className={styles.changePhotoLink} onClick={() => fileInputRef.current?.click()}>
            Changer de photo
          </button>
        </div>
        <input
          ref={fileInputRef}
          type="file"
          accept="image/jpeg,image/png,image/heic,image/heif"
          className={styles.uploadHiddenInput}
          onChange={handleInputChange}
          aria-label="Selectionner une autre photo"
        />
      </div>
      {simulationState === 'error' && errorMessage && (
        <p className={styles.errorMessage} role="alert" aria-live="assertive">{errorMessage}</p>
      )}
      {simulationState === 'error' ? (
        <button type="button" className={styles.retryButton} onClick={handleReessayer}>
          Reessayer
        </button>
      ) : (
        <button type="button" className={styles.launchButton} onClick={handleLancerSimulation}>
          Lancer la simulation
        </button>
      )}
    </>
  )}

  {/* Etat generating : photo fond + overlay progression */}
  {simulationState === 'generating' && previewUrl && (
    <div className={styles.generatingContainer}>
      <img src={previewUrl} alt="" className={styles.previewImage} />
      <div className={styles.generatingOverlay}>
        <span className={styles.stepActive}>
          {progressStage === 0 ? 'Analyse de la piece' : progressStage === 1 ? 'Integration du canape' : 'Finition et eclairage'}
        </span>
        <div className={styles.progressBarTrack} role="progressbar" aria-valuenow={progress} aria-valuemin={0} aria-valuemax={100}>
          <div className={styles.progressBarFill} style={{ width: `${progress}%` }} />
        </div>
        <span className={styles.progressPercent}>{progress} %</span>
        <div className={styles.stepList} aria-live="polite">
          {['Analyse de la piece', 'Integration du canape', 'Finition et eclairage'].map((label, i) => (
            <div key={label} className={`${styles.stepItem} ${i < progressStage ? styles.stepDone : i === progressStage ? styles.stepActive : styles.stepPending}`}>
              {i < progressStage ? CheckIcon : i === progressStage ? SpinnerIcon : PendingIcon}
              <span>{label}</span>
            </div>
          ))}
        </div>
        <button type="button" className={styles.cancelButton} onClick={handleAnnuler}>
          Annuler
        </button>
      </div>
    </div>
  )}

  {/* Etat idle : erreur de validation fichier */}
  {simulationState === 'idle' && errorMessage && (
    <p className={styles.errorMessage} role="alert" aria-live="assertive">{errorMessage}</p>
  )}
</div>
```

**Colonne droite (body simulation) :**
```jsx
<div className={styles.body}>
  <button type="button" className={styles.backButton} onClick={handleBackToConfigurator}>
    &larr; Modifier la configuration
  </button>

  <h2 className={styles.simulationTitle}>Simulation</h2>

  <p className={styles.uploadExplainer}>
    Prenez votre salon en photo, on y place votre canape
  </p>

  {/* Bandeau rappel config (D-05) */}
  <div className={styles.configRecap}>
    {selectedFabric?.swatch_url ? (
      <img src={selectedFabric.swatch_url} alt="" className={styles.configRecapSwatch} />
    ) : (
      <div className={styles.configRecapSwatch} style={{ background: 'var(--surface-container)' }} />
    )}
    <span className={styles.configRecapText}>
      {selectedFabric ? `${model.name} \u00d7 ${selectedFabric.name}` : `Canape original \u00b7 Aucun tissu selectionne`}
    </span>
    <button type="button" className={styles.configRecapLink} onClick={handleBackToConfigurator}>
      Modifier
    </button>
  </div>
</div>
```

**IMPORTANT :** Ne PAS dupliquer le `<input ref={fileInputRef}>` — il ne doit exister qu'une fois dans le DOM a la fois. Pour les etats `preview`/`error`, le fileInputRef est deja dans le previewContainer. Pour l'etat `idle`, il est dans uploadZone. Les deux sont mutuellement exclusifs grace aux conditions sur simulationState.

**IMPORTANT :** L'etat `done` (simulationState === 'done') n'affiche rien de special dans cette phase — ce sera gere en Phase 12. Pour l'instant, rester sur le generating container jusqu'a ce que Phase 12 ajoute le rendu.

**M. Ajustement du `model?.id` dependency dans le useEffect reset :**
Le useEffect de reset doit aussi nettoyer les Object URLs. Ajouter les lignes du point C dans le useEffect existant.
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ConfiguratorModal.tsx contient `type SimulationState = 'idle' | 'preview' | 'generating' | 'done' | 'error'`
    - ConfiguratorModal.tsx contient `const [modalStep, setModalStep] = useState<'configurator' | 'simulation'>`
    - ConfiguratorModal.tsx contient `const [simulationState, setSimulationState] = useState<SimulationState>('idle')`
    - ConfiguratorModal.tsx contient `const [isDragging, setIsDragging] = useState(false)`
    - ConfiguratorModal.tsx contient `dragCounterRef.current`
    - ConfiguratorModal.tsx contient `const abortControllerRef = useRef<AbortController | null>(null)`
    - ConfiguratorModal.tsx contient `handleDragEnter`
    - ConfiguratorModal.tsx contient `handleDragLeave`
    - ConfiguratorModal.tsx contient `handleDrop`
    - ConfiguratorModal.tsx contient `handleFileSelected`
    - ConfiguratorModal.tsx contient `validateFile`
    - ConfiguratorModal.tsx contient `15 * 1024 * 1024`
    - ConfiguratorModal.tsx contient `handleLancerSimulation`
    - ConfiguratorModal.tsx contient `handleAnnuler`
    - ConfiguratorModal.tsx contient `handleReessayer`
    - ConfiguratorModal.tsx contient `handleGoToSimulation`
    - ConfiguratorModal.tsx contient `handleBackToConfigurator`
    - ConfiguratorModal.tsx contient `startProgressTimer`
    - ConfiguratorModal.tsx contient `stopProgressTimer`
    - ConfiguratorModal.tsx contient `URL.revokeObjectURL`
    - ConfiguratorModal.tsx contient `fetch('/api/simulate'`
    - ConfiguratorModal.tsx contient `signal: controller.signal`
    - ConfiguratorModal.tsx contient `AbortError`
    - ConfiguratorModal.tsx contient `Visualiser chez moi`
    - ConfiguratorModal.tsx contient `Modifier la configuration`
    - ConfiguratorModal.tsx contient `Lancer la simulation`
    - ConfiguratorModal.tsx contient `Changer de photo`
    - ConfiguratorModal.tsx contient `Choisir une photo`
    - ConfiguratorModal.tsx contient `Glissez votre photo ici`
    - ConfiguratorModal.tsx contient `Annuler`
    - ConfiguratorModal.tsx contient `Reessayer`
    - ConfiguratorModal.tsx contient `role="progressbar"`
    - ConfiguratorModal.tsx contient `aria-live="polite"`
    - ConfiguratorModal.tsx contient `aria-live="assertive"`
    - ConfiguratorModal.tsx contient `accept="image/jpeg,image/png,image/heic,image/heif"`
    - ConfiguratorModal.tsx NE contient PAS `import.*react-dropzone`
    - ConfiguratorModal.tsx NE contient PAS le mot `any` (TypeScript strict)
    - `npx tsc --noEmit` sort sans erreur
  </acceptance_criteria>
  <done>ConfiguratorModal.tsx etendu avec etape simulation complete : state machine 5 etats, DnD HTML5 natif avec compteur anti-flicker, preview via createObjectURL, barre de progression simulee 2 phases, fetch avec AbortController, erreurs inline, CTA "Visualiser chez moi", bouton retour. TypeScript compile sans erreur.</done>
</task>

<task type="auto">
  <name>Task 3: Build verification et nettoyage final</name>
  <files>src/components/public/Catalogue/ConfiguratorModal.tsx, src/components/public/Catalogue/ConfiguratorModal.module.css</files>
  <read_first>
    - src/components/public/Catalogue/ConfiguratorModal.tsx (apres Task 2)
    - src/components/public/Catalogue/ConfiguratorModal.module.css (apres Task 1)
  </read_first>
  <action>
Executer le build complet (`npx tsc --noEmit && npm run build`) et corriger toute erreur :

1. **TypeScript strict** : verifier qu'aucun `any` implicite n'est present. Les types les plus risques sont :
   - `e.dataTransfer.files[0]` — deja type `File | undefined` (le guard `if (file)` suffit)
   - `response.json()` — retourne `unknown` en strict. Caster en `{ error?: string }` si necessaire
   - Les closures useCallback — verifier que les dependency arrays sont complets

2. **CSS Modules unused classes** : si le build avertit sur des classes CSS non utilisees, verifier que les noms de classes correspondent exactement entre le TSX et le CSS. Les noms sont en camelCase dans le TSX (ex: `styles.uploadZone`) et doivent correspondre a `.uploadZone` dans le CSS.

3. **Verifier que les classes CSS referees dans le TSX existent dans le CSS** :
   Lister toutes les occurrences de `styles.` dans le TSX et verifier chaque nom dans le CSS.

4. **Verifier l'absence de warnings** dans `npm run build` lies aux composants modifies.

5. Si des erreurs TS apparaissent avec `response.json()`, caster le resultat :
```typescript
const data = await response.json().catch(() => ({ error: 'La simulation a echoue.' })) as { error?: string }
```

6. Verifier que le composant ne contient PAS de `console.log` de debug.

7. Verifier que `eslint-disable-line react-hooks/exhaustive-deps` est bien sur le useEffect cleanup uniquement.
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sort avec exit code 0
    - `npm run build` sort avec exit code 0
    - Aucun `console.log` dans ConfiguratorModal.tsx (sauf s'il etait deja present avant)
    - Aucun `any` dans ConfiguratorModal.tsx
    - Chaque `styles.XXX` reference dans le TSX correspond a une classe `.XXX` dans le CSS
  </acceptance_criteria>
  <done>Build TypeScript et Next.js passent sans erreur. Pas de code mort, pas de any, pas de console.log de debug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User file input -> client validation | Fichier potentiellement malicieux, trop gros, ou au mauvais format |
| Client -> /api/simulate | FormData avec fichier non verifie cote serveur (la validation serveur est dans Plan 01) |
| Object URL -> browser memory | Fuite memoire si revokeObjectURL oublie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | File input validation bypass | mitigate | Double validation : client (validateFile avec ACCEPTED_TYPES + MAX_SIZE_BYTES) + serveur (Plan 01 MAX_FILE_SIZE). Le client est une UX convenience, pas une securite |
| T-11-06 | Spoofing | MIME type spoofing | mitigate | Fallback extension pour HEIC (.heic/.heif regex). Accept attribute sur input comme premier filtre. Validation serveur fait foi |
| T-11-07 | DoS | Memory leak via Object URLs | mitigate | revokeObjectURL dans handleFileSelected (avant creation nouvelle URL), dans reset useEffect (model change), et dans cleanup useEffect (unmount) |
| T-11-08 | DoS | Timer leak on unmount | mitigate | clearInterval dans stopProgressTimer, appele dans tous les chemins (success, error, abort, unmount cleanup) |
| T-11-09 | Denial of Service | Large file in memory | accept | Le fichier est en memoire client uniquement pendant l'upload. La limite de 15 Mo est raisonnable pour un navigateur moderne. Pas de mitigation supplementaire necessaire |
| T-11-10 | Repudiation | AbortError vs real error | mitigate | Distinction explicite via err.name === 'AbortError' — l'annulation ne genere pas de message d'erreur utilisateur |
</threat_model>

<verification>
```bash
cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit && npm run build
```

Verification manuelle (par l'utilisateur) :
1. Ouvrir un produit dans le configurateur
2. Cliquer "Visualiser chez moi" -> l'etape simulation s'affiche
3. Glisser une photo JPEG -> preview apparait
4. Cliquer "Lancer la simulation" -> barre de progression animee
5. Cliquer "Annuler" -> retour a l'etat preview
6. Tenter un fichier > 15 Mo -> message d'erreur en francais
7. Cliquer "Modifier la configuration" -> retour au configurateur avec tissu conserve
8. Sur mobile : verifier layout 1 colonne
</verification>

<success_criteria>
- CTA "Visualiser chez moi" visible sous le bouton Shopify (D-02)
- Zone upload avec DnD desktop + input file (D-08)
- Validation client JPEG/PNG/HEIC 15 Mo max avec erreurs francais (D-10, D-11)
- Preview image apres selection (D-09)
- Barre de progression simulee avec 3 etapes (D-12, D-13, D-14)
- Bouton "Annuler" fonctionnel avec AbortController (D-15)
- Tissu optionnel pour la simulation (D-16 — client envoie fabric_id si present)
- Erreurs inline + bouton "Reessayer" (D-17, D-18)
- useState local, pas de Zustand (D-19)
- Build TypeScript + Next.js passe sans erreur
</success_criteria>

<output>
After completion, create `.planning/phases/11-simulation-ia-upload-et-traitement/11-02-SUMMARY.md`
</output>
