---
phase: 08-configurateur-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/public/Catalogue/ConfiguratorModal.tsx
  - src/components/public/Catalogue/ConfiguratorModal.module.css
  - src/__tests__/ConfiguratorModal.test.tsx
autonomous: false
requirements: [CONF-01, CONF-02, CONF-03, CONF-05, CONF-07, CONF-08, CONF-09, CONF-10]

must_haves:
  truths:
    - "Le client voit une grille de swatches cliquables pour les tissus ayant un rendu publie pour ce modele"
    - "Le swatch selectionne a une bordure primary (#E49400) visuellement distincte"
    - "Les tissus premium affichent un badge 'Premium' sur leur swatch"
    - "Le rendu IA publie s'affiche quand un tissu est selectionne"
    - "La photo originale s'affiche avec badge 'Photo originale' quand aucun rendu n'existe pour le tissu"
    - "Le prix se met a jour immediatement lors de la selection (base + 80 EUR si premium)"
    - "Le CTA 'Acheter sur Shopify' est visible et ouvre shopify_url en nouvel onglet"
    - "Le CTA est masque si shopify_url est null"
  artifacts:
    - path: "src/components/public/Catalogue/ConfiguratorModal.tsx"
      provides: "Logique selection tissu, affichage rendu IA, prix dynamique, CTA Shopify"
      contains: "selectedFabricId"
    - path: "src/components/public/Catalogue/ConfiguratorModal.module.css"
      provides: "Styles swatches, badges, prix, CTA"
      contains: "swatchGrid"
    - path: "src/__tests__/ConfiguratorModal.test.tsx"
      provides: "Tests Phase 8 configurateur (CONF-02 a CONF-10)"
      contains: "Phase 8"
  key_links:
    - from: "ConfiguratorModal.tsx"
      to: "src/lib/utils.ts"
      via: "import calculatePrice, formatPrice"
      pattern: "import.*calculatePrice.*formatPrice.*from.*@/lib/utils"
    - from: "ConfiguratorModal.tsx"
      to: "visuals prop"
      via: "filtrage eligibleFabricIds = new Set(visuals.filter(...))"
      pattern: "visuals\\.filter"
    - from: "ConfiguratorModal.tsx"
      to: "fabrics prop"
      via: "eligibleFabrics = fabrics.filter(f => eligibleFabricIds.has(f.id))"
      pattern: "fabrics\\.filter"
---

<objective>
Implementer le configurateur tissu complet dans ConfiguratorModal : grille de swatches cliquables, affichage du rendu IA avec fallback photo originale, prix dynamique avec detail premium, et CTA Shopify conditionnel.

Purpose: Le client peut selectionner un tissu, visualiser le rendu IA du canape dans ce tissu, lire le prix mis a jour et acceder a l'achat — c'est la proposition de valeur coeur de l'application.

Output: ConfiguratorModal.tsx enrichi (remplace le placeholder), ConfiguratorModal.module.css etendu, tests mis a jour.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-fetch-donn-es-c-blage-props/07-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/database.ts:
```typescript
export type Fabric = Database['public']['Tables']['fabrics']['Row']
// Row: { id: string, name: string, slug: string, category: string | null, is_active: boolean, is_premium: boolean, swatch_url: string | null, reference_image_url: string | null, created_at: string }

export type ModelWithImages = Model & { model_images: ModelImage[] }
// Model Row: { id: string, name: string, slug: string, price: number, description: string | null, dimensions: string | null, is_active: boolean, shopify_url: string | null, created_at: string }

export type VisualWithFabricAndImage = GeneratedVisual & { fabric: Fabric, model_image: ModelImage }
// GeneratedVisual Row: { id: string, model_id: string, model_image_id: string, fabric_id: string, generated_image_url: string, is_validated: boolean, is_published: boolean, created_at: string }
```

From src/lib/utils.ts:
```typescript
export function calculatePrice(basePrice: number, isPremium: boolean): number
// Returns: isPremium ? basePrice + 80 : basePrice

export function formatPrice(price: number): string
// Returns: Intl.NumberFormat('fr-FR', { style: 'currency', currency: 'EUR' }).format(price)
// Example: 1590 -> "1 590,00 EUR" (exact format depends on Intl)
```

From src/components/public/Catalogue/ConfiguratorModal.tsx (current):
```typescript
export function getPrimaryImage(model_images: ModelImage[]): string | null
// Returns image_url of view_type '3/4', or first image, or null

export function formatPrice(price: number): string
// LOCAL function — returns 'a partir de ' + formatted price + ' EUR'
// CONFLICT: same name as utils.ts formatPrice — must use alias import

interface ConfiguratorModalProps {
  model: ModelWithImages | null
  onClose: () => void
  fabrics: Fabric[]
  visuals: VisualWithFabricAndImage[]
}
```

From src/app/globals.css (relevant tokens):
```css
--color-primary: #E49400;
--color-primary-dark: #845400;
--color-text: #1D1D1B;
--color-muted: #888888;
--color-background: #FFFFFF;
--color-background-alt: #F8F4EE;
--radius-sm: 4px;
--radius-md: 8px;
--radius-lg: 12px;
--spacing-xs: 0.25rem;  /* 4px */
--spacing-sm: 0.5rem;   /* 8px */
--spacing-md: 1rem;     /* 16px */
--spacing-lg: 1.5rem;   /* 24px */
--spacing-xl: 2rem;     /* 32px */
--shadow-md: 0 4px 6px rgba(0, 0, 0, 0.07);
--transition-fast: 300ms ease;
--font-size-xs: 0.75rem; /* 12px */
--font-size-sm: 0.875rem; /* 14px */
--font-size-base: 1rem;  /* 16px */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests Phase 8 — scaffolds + mise a jour tests existants</name>
  <files>src/__tests__/ConfiguratorModal.test.tsx</files>
  <read_first>
    - src/__tests__/ConfiguratorModal.test.tsx
    - src/components/public/Catalogue/ConfiguratorModal.tsx
    - src/types/database.ts
    - src/lib/utils.ts
  </read_first>
  <action>
Modifier `src/__tests__/ConfiguratorModal.test.tsx` pour preparer les tests Phase 8. IMPORTANT : ces tests doivent ECHOUER (RED) tant que Task 2 n'est pas implementee.

**1. Enrichir les mock data existantes :**

Ajouter un `mockModelWithShopify` base sur `mockModel` mais avec `shopify_url: 'https://www.mobelunique.fr/products/canape-elegance'`.

Ajouter un `mockFabricPremium` dans le tableau mockFabrics :
```typescript
const mockFabricPremium: Fabric = {
  id: 'fabric-modal-002',
  name: 'Cuir Nappa',
  slug: 'cuir-nappa',
  category: 'cuir',
  is_active: true,
  is_premium: true,
  swatch_url: 'https://test.supabase.co/storage/v1/object/public/fabric-swatches/cuir-nappa.jpg',
  reference_image_url: null,
  created_at: '2026-01-01T00:00:00Z',
}
```

Ajouter un `mockVisualPremium` dans le tableau mockVisuals pour le tissu premium :
```typescript
const mockVisualPremium: VisualWithFabricAndImage = {
  id: 'visual-modal-002',
  model_id: 'test-uuid-modal',
  model_image_id: 'img-modal-001',
  fabric_id: 'fabric-modal-002',
  generated_image_url: 'https://test.supabase.co/storage/v1/object/public/generated-visuals/vm002.jpg',
  is_validated: true,
  is_published: true,
  created_at: '2026-01-03T00:00:00Z',
  fabric: mockFabricPremium,
  model_image: mockModel.model_images[0],
}
```

Creer un helper pour les mock data Phase 8 :
```typescript
const allFabrics = [mockFabrics[0], mockFabricPremium]
const allVisuals = [mockVisuals[0], mockVisualPremium]
```

**2. Mettre a jour les tests placeholder existants :**

Les tests `[MODAL-03] affiche le message Configurateur a venir` et `[MODAL-03] affiche le texte explicatif placeholder` doivent etre SUPPRIMES car le placeholder est remplace par le configurateur. Le test `[MODAL-03] affiche le prix formate` doit etre conserve mais adapte pour verifier que le prix initial affiche "A partir de" (format sans tissu selectionne).

**3. Ajouter le describe "Phase 8 — configurateur" avec ces tests :**

- `[CONF-01] affiche uniquement les swatches des tissus avec rendu publie pour ce modele` — Rendre avec `allFabrics` et `allVisuals`, verifier que 2 boutons `role="radio"` existent dans le `role="radiogroup"` (les 2 tissus ont des rendus). Ajouter un 3e tissu dans `allFabrics` (sans visual correspondant) et verifier qu'il n'apparait PAS dans la grille.

- `[CONF-02] cliquer un swatch met a jour l'etat et affiche le rendu IA` — Rendre avec `allFabrics` et `allVisuals`, cliquer le premier swatch (via `userEvent.click`), verifier que `aria-checked="true"` est mis a jour sur le swatch clique, et que l'image principale a un `src` contenant `vm001.jpg` (generated_image_url du visual correspondant).

- `[CONF-03] les tissus premium affichent un badge Premium` — Rendre avec `allFabrics` et `allVisuals`, verifier que le texte "Premium" est present dans le document exactement 1 fois (seul mockFabricPremium est premium).

- `[CONF-05] affiche la photo originale avec badge quand pas de rendu pour le tissu` — Verifier que a l'etat initial (aucun tissu selectionne) il n'y a PAS de badge "Photo originale" (c'est la photo originale mais pas un fallback). Puis tester le scenario fallback : le badge n'apparait que quand un tissu est selectionne MAIS aucun visual n'existe pour ce tissu+modele.

- `[CONF-07] le prix se met a jour quand un tissu standard est selectionne` — Rendre avec `allFabrics` et `allVisuals`, cliquer le swatch du tissu standard (Velours Emeraude, prix 1590), verifier que le texte affiche contient "1 590" ET NE contient PAS "A partir de" (dans le bloc prix).

- `[CONF-08] le detail surcout premium est visible pour un tissu premium` — Rendre avec `allFabrics` et `allVisuals`, cliquer le swatch du tissu premium (Cuir Nappa, prix 1590 + 80 = 1670), verifier que le texte "1 670" est present ET que le texte contenant "80" et "premium" est present (detail surcout).

- `[CONF-09] CTA Acheter sur Shopify redirige vers shopify_url` — Rendre avec `mockModelWithShopify`, `allFabrics`, `allVisuals`, verifier qu'un lien avec texte "Acheter sur Shopify" existe, a `href` egal a `mockModelWithShopify.shopify_url`, a `target="_blank"`, et a `rel` contenant "noopener".

- `[CONF-10] CTA masque si shopify_url est null` — Rendre avec `mockModel` (shopify_url: null), verifier que le texte "Acheter sur Shopify" N'EST PAS dans le document.

**4. NE PAS modifier les tests existants qui passent** (sauf suppression des 2 tests placeholder mentionnes ci-dessus et adaptation du test prix).

**Contraintes :**
- NE PAS importer `calculatePrice` ou `formatPrice` depuis utils.ts dans les tests — les tests verifient le comportement visible (texte rendu), pas les fonctions internes.
- Conserver l'import existant `{ ConfiguratorModal, getPrimaryImage, formatPrice }` depuis ConfiguratorModal (la fonction locale est toujours exportee dans Task 2).
- Les nouveaux tests doivent ECHOUER apres Task 1 (le placeholder est encore en place). C'est attendu — Task 2 les fait passer au vert.
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `src/__tests__/ConfiguratorModal.test.tsx` contient un describe "Phase 8" avec au minimum 8 tests couvrant CONF-01, CONF-02, CONF-03, CONF-05, CONF-07, CONF-08, CONF-09, CONF-10
    - Les tests placeholder "[MODAL-03] affiche le message Configurateur a venir" et "[MODAL-03] affiche le texte explicatif placeholder" sont SUPPRIMES
    - `npx tsc --noEmit` passe sans erreur (types corrects)
    - Les tests existants non modifies passent toujours (`npx vitest run src/__tests__/ConfiguratorModal.test.tsx --reporter=verbose 2>&1 | grep -E "PASS|FAIL"`)
    - Les nouveaux tests ECHOUENT (attendu — RED phase du TDD)
  </acceptance_criteria>
  <done>Fichier test enrichi avec mock data premium, tests Phase 8 scaffoldes, tests placeholder supprimes. TypeScript compile. Les tests existants passent, les nouveaux echouent (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementer le configurateur — swatches, rendu IA, prix, CTA</name>
  <files>
    src/components/public/Catalogue/ConfiguratorModal.tsx
    src/components/public/Catalogue/ConfiguratorModal.module.css
  </files>
  <read_first>
    - src/components/public/Catalogue/ConfiguratorModal.tsx
    - src/components/public/Catalogue/ConfiguratorModal.module.css
    - src/__tests__/ConfiguratorModal.test.tsx
    - src/lib/utils.ts
    - src/types/database.ts
    - src/app/globals.css
    - .planning/phases/08-configurateur-core/08-CONTEXT.md
    - .planning/phases/08-configurateur-core/08-UI-SPEC.md
    - .planning/phases/08-configurateur-core/08-RESEARCH.md
  </read_first>
  <behavior>
    - Test CONF-01: eligibleFabrics ne contient que les tissus avec rendu publie pour ce modele — la grille affiche 2 swatches quand 2 tissus ont des visuels, exclut ceux sans visual
    - Test CONF-02: cliquer un swatch change aria-checked et affiche generated_image_url dans l'image principale
    - Test CONF-03: badge "Premium" visible exactement sur les swatches premium
    - Test CONF-05: photo originale affichee avec badge quand fallback
    - Test CONF-07: prix exact sans "A partir de" quand tissu standard selectionne
    - Test CONF-08: prix total + detail "+ 80 EUR" quand tissu premium selectionne
    - Test CONF-09: lien Shopify avec href, target="_blank", rel="noopener noreferrer"
    - Test CONF-10: pas de lien Shopify quand shopify_url null
  </behavior>
  <action>
Modifier `ConfiguratorModal.tsx` et `ConfiguratorModal.module.css` pour implementer le configurateur complet. Voici les modifications EXACTES a apporter :

**A. ConfiguratorModal.tsx — Imports et fonctions :**

1. Ajouter `useState` a l'import React : `import { useState, useRef, useEffect, useCallback } from 'react'`

2. Ajouter l'import depuis utils.ts avec ALIAS (per RESEARCH.md, evite le conflit avec la formatPrice locale) :
```typescript
import { calculatePrice, formatPrice as formatPriceUtil } from '@/lib/utils'
```

3. CONSERVER la fonction locale `formatPrice` exportee (ligne 16-18) telle quelle — elle est utilisee par les tests existants et pour l'etat initial "a partir de X EUR". NE PAS la supprimer.

4. CONSERVER la fonction `getPrimaryImage` telle quelle.

**B. ConfiguratorModal.tsx — Retirer le prefixe underscore des props :**

Changer la signature de destructuration de :
```typescript
export function ConfiguratorModal({ model, onClose, fabrics: _fabrics, visuals: _visuals }: ConfiguratorModalProps)
```
a :
```typescript
export function ConfiguratorModal({ model, onClose, fabrics, visuals }: ConfiguratorModalProps)
```

**C. ConfiguratorModal.tsx — State et hooks (AVANT le guard `if (!model) return null`) :**

Ajouter le useState APRES les hooks existants (useRef, useEffect scroll, useEffect dialog, useCallback) mais AVANT le guard :
```typescript
const [selectedFabricId, setSelectedFabricId] = useState<string | null>(null)
```

Ajouter un useEffect pour reset le state quand le modele change (per RESEARCH.md Pattern 2) :
```typescript
useEffect(() => {
  setSelectedFabricId(null)
}, [model?.id])
```

Ce useEffect doit etre place AVANT le guard `if (!model) return null`.

**D. ConfiguratorModal.tsx — Derivations (APRES le guard) :**

Apres `if (!model) return null`, ajouter les derivations suivantes :

```typescript
// D-01: Filtrage tissus eligibles (ayant au moins un rendu publie pour ce modele)
const eligibleFabricIds = new Set(
  visuals
    .filter(v => v.model_id === model.id && v.is_published)
    .map(v => v.fabric_id)
)

const eligibleFabrics = fabrics.filter(
  f => eligibleFabricIds.has(f.id) && f.swatch_url !== null
)

// Lookup du tissu et visual selectionnes
const selectedFabric = selectedFabricId
  ? eligibleFabrics.find(f => f.id === selectedFabricId) ?? null
  : null

const currentVisual = selectedFabricId
  ? visuals.find(
      v => v.model_id === model.id &&
           v.fabric_id === selectedFabricId &&
           v.is_published
    ) ?? null
  : null

// Image a afficher : rendu IA si disponible, sinon photo originale
const originalImageUrl = getPrimaryImage(model.model_images)
const displayImageUrl = currentVisual?.generated_image_url ?? originalImageUrl
const isOriginalFallback = selectedFabricId !== null && currentVisual === null
```

**E. ConfiguratorModal.tsx — JSX : remplacer le bloc .imageWrapper et .body**

Remplacer le bloc image actuel (lignes 96-106) par un bloc qui affiche `displayImageUrl` avec alt contextuel et badge "Photo originale" conditionnel :

```tsx
{displayImageUrl && (
  <div className={styles.imageWrapper}>
    <Image
      src={displayImageUrl}
      alt={
        currentVisual && selectedFabric
          ? `Canape ${model.name} en tissu ${selectedFabric.name}`
          : `Canape ${model.name}`
      }
      fill
      style={{ objectFit: 'cover' }}
      sizes="(max-width: 640px) 100vw, 50vw"
    />
    {isOriginalFallback && (
      <span className={styles.badgeOriginalPhoto}>Photo originale</span>
    )}
  </div>
)}
```

Remplacer le contenu du `.body` (lignes 108-121). Supprimer le bloc `.placeholder` et le remplacer par le configurateur :

```tsx
<div className={styles.body}>
  <h2 id="modal-title" className={styles.modelName}>{model.name}</h2>

  {/* Prix dynamique — per D-10, D-11, D-12 */}
  {selectedFabric ? (
    <div className={styles.priceBlock}>
      <p className={styles.price}>
        {formatPriceUtil(calculatePrice(model.price, selectedFabric.is_premium))}
      </p>
      {selectedFabric.is_premium && (
        <p className={styles.priceSupplement}>+ 80&nbsp;EUR &middot; tissu premium</p>
      )}
    </div>
  ) : (
    <p className={styles.price}>{formatPrice(model.price)}</p>
  )}

  {model.description && (
    <p className={styles.description}>{model.description}</p>
  )}

  <hr className={styles.separator} />

  {/* Grille swatches — per D-04, D-05, D-06, CONF-01 */}
  <div className={styles.configurator}>
    <p className={styles.swatchLabel}>Choisissez votre tissu</p>

    {eligibleFabrics.length > 0 ? (
      <div
        role="radiogroup"
        aria-label="Choisissez votre tissu"
        className={styles.swatchGrid}
      >
        {eligibleFabrics.map(fabric => (
          <button
            key={fabric.id}
            type="button"
            role="radio"
            aria-checked={selectedFabricId === fabric.id}
            aria-label={`${fabric.name}${fabric.is_premium ? ' \u2014 Premium' : ''}`}
            className={`${styles.swatch} ${selectedFabricId === fabric.id ? styles.swatchSelected : ''}`}
            onClick={() => setSelectedFabricId(fabric.id)}
          >
            <Image
              src={fabric.swatch_url!}
              alt=""
              fill
              style={{ objectFit: 'cover' }}
              sizes="56px"
            />
            {fabric.is_premium && (
              <span className={styles.badgePremium} aria-hidden="true">Premium</span>
            )}
          </button>
        ))}
      </div>
    ) : (
      <p className={styles.emptySwatches}>Aucun tissu disponible pour ce modele.</p>
    )}
  </div>

  {/* CTA Shopify — per D-14, D-15, CONF-09, CONF-10 */}
  {model.shopify_url && (
    <a
      href={model.shopify_url}
      target="_blank"
      rel="noopener noreferrer"
      className={styles.ctaShopify}
      aria-label={`Acheter ${model.name} sur Shopify \u2014 ouvre un nouvel onglet`}
    >
      Acheter sur Shopify
    </a>
  )}
</div>
```

**F. ConfiguratorModal.module.css — Ajouter les styles suivants :**

Apres le bloc `.placeholderText` existant (qui peut etre conserve ou supprime — le supprimer puisque les classes `.placeholder`, `.placeholderTitle`, `.placeholderText` ne sont plus utilisees dans le JSX), ajouter :

```css
/* === Phase 8 — Configurateur === */

/* Bloc configurateur (remplace placeholder) */
.configurator {
  background: var(--color-background-alt);
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
}

/* Label section swatches */
.swatchLabel {
  font-size: var(--font-size-sm);
  font-weight: 600;
  color: var(--color-muted);
  margin: 0 0 var(--spacing-md) 0;
}

/* Grille de swatches — CSS Grid responsive */
.swatchGrid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(56px, 1fr));
  gap: var(--spacing-xs);
}

/* Swatch individuel — cible tactile 56x56px (WCAG 2.5.5) */
.swatch {
  position: relative;
  width: 56px;
  height: 56px;
  border-radius: var(--radius-md);
  overflow: hidden;
  border: 3px solid transparent;
  cursor: pointer;
  background: none;
  padding: 0;
  transition: box-shadow var(--transition-fast);
  flex-shrink: 0;
}

.swatch:hover {
  box-shadow: var(--shadow-md);
}

.swatchSelected {
  border-color: var(--color-primary);
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

/* Badge Premium sur swatch */
.badgePremium {
  position: absolute;
  bottom: 2px;
  right: 2px;
  background: rgba(228, 148, 0, 0.15);
  color: var(--color-primary-dark);
  font-size: var(--font-size-xs);
  font-weight: 600;
  border-radius: var(--radius-sm);
  padding: 2px 4px;
  line-height: 1;
  pointer-events: none;
}

/* Badge Photo originale sur image principale */
.badgeOriginalPhoto {
  position: absolute;
  bottom: var(--spacing-sm);
  left: var(--spacing-sm);
  background: rgba(0, 0, 0, 0.55);
  color: #FFFFFF;
  font-size: var(--font-size-xs);
  font-weight: 600;
  border-radius: var(--radius-sm);
  padding: var(--spacing-xs) var(--spacing-sm);
  pointer-events: none;
}

/* Etat vide — aucun tissu disponible */
.emptySwatches {
  font-size: var(--font-size-sm);
  color: var(--color-muted);
  margin: 0;
}

/* Bloc prix dynamique */
.priceBlock {
  margin-top: var(--spacing-sm);
}

.priceSupplement {
  font-size: var(--font-size-sm);
  color: var(--color-muted);
  margin-top: 2px;
}

/* CTA Acheter sur Shopify */
.ctaShopify {
  display: block;
  width: 100%;
  margin-top: var(--spacing-xl);
  padding: 0 var(--spacing-lg);
  height: 48px;
  line-height: 48px;
  text-align: center;
  text-decoration: none;
  font-size: var(--font-size-base);
  font-weight: 600;
  color: var(--color-text);
  background: transparent;
  border: 1px solid var(--color-text);
  border-radius: var(--radius-md);
  cursor: pointer;
  transition: background-color var(--transition-fast), color var(--transition-fast);
}

.ctaShopify:hover {
  background: var(--color-text);
  color: var(--color-background);
}
```

**G. SUPPRIMER les classes CSS inutilisees :**

Supprimer `.placeholder`, `.placeholderTitle`, `.placeholderText` du CSS Module (les classes ne sont plus referees dans le JSX). Ceci elimine le code mort per CLAUDE.md.

**H. Ajustement desktop `.body` :**

Dans le media query `@media (min-width: 640px)`, le `.body` a `justify-content: center` — cela centre verticalement le contenu. Avec l'ajout du configurateur (plus de contenu), changer en `justify-content: flex-start` pour permettre le scroll naturel quand le contenu depasse.

**Contraintes strictes :**
- NE PAS ajouter `any` — TypeScript strict (per CLAUDE.md)
- NE PAS modifier CatalogueSection, CatalogueClient, ProductCard (per D-02)
- NE PAS utiliser Tailwind ou shadcn/ui (per CLAUDE.md)
- NE PAS ajouter d'animation au changement d'image (per UI-SPEC — instantane)
- NE PAS pre-selectionner un tissu a l'ouverture (per D-09 — etat initial null)
- NE PAS utiliser Zustand (per REQUIREMENTS.md — reserve v10.0)
- CONSERVER la formatPrice locale exportee (per conflit resolution — alias import)
- Le useEffect reset (model?.id) est AVANT le guard `if (!model) return null`
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx vitest run src/__tests__/ConfiguratorModal.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/ConfiguratorModal.test.tsx` : TOUS les tests passent (anciens + nouveaux Phase 8)
    - `npx tsc --noEmit` : zero erreur TypeScript
    - `npm test` : suite complete verte
    - ConfiguratorModal.tsx contient `useState<string | null>(null)` pour selectedFabricId
    - ConfiguratorModal.tsx contient `import { calculatePrice, formatPrice as formatPriceUtil } from '@/lib/utils'`
    - ConfiguratorModal.tsx contient `role="radiogroup"` et `role="radio"`
    - ConfiguratorModal.tsx contient `eligibleFabricIds` avec `new Set`
    - ConfiguratorModal.tsx contient `model.shopify_url &&` pour le CTA conditionnel
    - ConfiguratorModal.tsx contient `isOriginalFallback` et badge "Photo originale"
    - ConfiguratorModal.module.css contient `.swatchGrid`, `.swatch`, `.swatchSelected`, `.badgePremium`, `.badgeOriginalPhoto`, `.ctaShopify`
    - Les classes `.placeholder`, `.placeholderTitle`, `.placeholderText` sont SUPPRIMEES du CSS
    - Le texte "Configurateur a venir" n'apparait plus dans ConfiguratorModal.tsx
  </acceptance_criteria>
  <done>Le configurateur est fonctionnel : grille swatches cliquables, rendu IA avec fallback, prix dynamique avec detail premium, CTA Shopify conditionnel. Tous les tests passent. Zero code mort.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verification visuelle configurateur</name>
  <files>src/components/public/Catalogue/ConfiguratorModal.tsx</files>
  <action>
Verification visuelle par l'utilisateur du configurateur tissu complet.

Ce qui a ete construit :
- Grille de swatches cliquables (miniatures tissu)
- Bordure orange (#E49400) sur le swatch selectionne
- Badge "Premium" sur les swatches de tissus premium
- Image principale change pour afficher le rendu IA du tissu selectionne
- Badge "Photo originale" quand pas de rendu IA pour le tissu
- Prix dynamique : "A partir de X EUR" sans selection, prix exact avec selection, detail "+ 80 EUR tissu premium" si premium
- CTA "Acheter sur Shopify" visible si shopify_url existe, masque sinon

Instructions de verification :
1. Demarrer le serveur dev : `npm run dev`
2. Ouvrir http://localhost:3000 dans le navigateur
3. Scroller jusqu'au catalogue et cliquer sur une carte produit pour ouvrir le modal
4. Verifier :
   a. La grille de swatches est visible sous la description (fond beige clair #F8F4EE)
   b. Aucun tissu n'est pre-selectionne a l'ouverture — l'image est la photo originale
   c. Cliquer un swatch : la bordure orange apparait, l'image change pour le rendu IA
   d. Les tissus premium ont un badge "Premium" en bas a droite de leur swatch
   e. Le prix change immediatement quand on selectionne un tissu
   f. Un tissu premium affiche le detail "+ 80 EUR tissu premium" sous le prix
   g. Si le modele a un shopify_url, le bouton "Acheter sur Shopify" est visible en bas
   h. Sur mobile (< 640px) : le modal est plein ecran, les swatches sont accessibles en scroll
5. Tester le changement de produit : fermer le modal, ouvrir un autre produit — verifier que la selection tissu est reset (aucun tissu selectionne)

Tapez "approved" si le configurateur fonctionne correctement, ou decrivez les problemes a corriger.
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>L'utilisateur a verifie visuellement que le configurateur fonctionne : swatches, rendu IA, prix dynamique, CTA Shopify. Build production OK.</done>
</task>

</tasks>

<verification>
Phase 8 gate checks:
1. `npx vitest run src/__tests__/ConfiguratorModal.test.tsx` — tous les tests verts
2. `npm test` — suite complete verte (aucune regression)
3. `npx tsc --noEmit` — zero erreur TypeScript
4. `npm run build` — build production reussit
5. Verification visuelle : configurateur fonctionnel dans le modal
</verification>

<success_criteria>
- Le client voit les swatches cliquables des tissus avec rendu publie pour le modele selectionne
- Le swatch selectionne est visuellement distinct (bordure primary)
- Les tissus premium affichent un badge "Premium"
- Le rendu IA s'affiche quand un tissu est selectionne, fallback photo originale avec badge sinon
- Le prix se met a jour immediatement (base + 80 EUR si premium)
- Le CTA Shopify est conditionnel et ouvre en nouvel onglet
- Tous les tests passent, zero erreur TypeScript, build production OK
</success_criteria>

<output>
After completion, create `.planning/phases/08-configurateur-core/08-01-SUMMARY.md`
</output>
