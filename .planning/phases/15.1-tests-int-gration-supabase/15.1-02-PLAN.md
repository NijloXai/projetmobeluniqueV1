---
phase: 15.1-tests-int-gration-supabase
plan: 02
type: execute
wave: 2
depends_on: [15.1-01]
files_modified:
  - src/__tests__/integration/routes/public-models.integration.test.ts
  - src/__tests__/integration/routes/auth-rls.integration.test.ts
autonomous: true
requirements:
  - D-05
  - D-06
  - D-07
  - D-08
  - D-12

must_haves:
  truths:
    - "GET /api/models retourne uniquement les modeles actifs (pas Roma Inactif)"
    - "GET /api/models/[slug] retourne le modele par slug avec ses images"
    - "GET /api/models/[slug]/visuals retourne les visuels publies par modele"
    - "requireAdmin() avec un vrai JWT retourne 200 (pas mocke)"
    - "requireAdmin() sans token retourne 401"
    - "Un user non authentifie ne peut pas acceder aux routes admin"
    - "Les routes publiques filtrent is_active=true"
  artifacts:
    - path: "src/__tests__/integration/routes/public-models.integration.test.ts"
      provides: "Tests integration routes publiques (GET /api/models, /[slug], /[slug]/visuals)"
      min_lines: 80
    - path: "src/__tests__/integration/routes/auth-rls.integration.test.ts"
      provides: "Tests integration auth reelle + RLS policies"
      min_lines: 60
  key_links:
    - from: "public-models.integration.test.ts"
      to: "src/app/api/models/route.ts"
      via: "testApiHandler appHandler"
      pattern: "import.*api/models/route"
    - from: "auth-rls.integration.test.ts"
      to: "src/app/api/admin/models/route.ts"
      via: "testApiHandler appHandler"
      pattern: "import.*admin/models/route"
---

<objective>
Tester les 3 routes publiques (models listing, model by slug, visuals by model) et verifier l'auth reelle (JWT via signInWithPassword) + les politiques RLS (user non auth bloque sur les routes admin, modeles inactifs invisibles sur routes publiques).

Purpose: Valider que les routes publiques retournent les bonnes donnees filtrees et que l'auth avec un vrai token Supabase fonctionne en integration.
Output: 2 fichiers de test d'integration couvrant routes publiques + auth + RLS
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15.1-tests-int-gration-supabase/15.1-RESEARCH.md
@.planning/phases/15.1-tests-int-gration-supabase/15.1-01-SUMMARY.md

<interfaces>
<!-- Routes publiques a tester -->
From src/app/api/models/route.ts:
```typescript
// GET /api/models — Liste tous les modeles actifs avec model_images, tries par created_at desc
export async function GET(): Promise<NextResponse>
// Retourne: Array<Model & { model_images: ModelImage[] }> filtre is_active=true
```

From src/app/api/models/[slug]/route.ts:
```typescript
// GET /api/models/:slug — Retourne un modele par slug avec images et visuels
export async function GET(request: NextRequest, { params }: { params: Promise<{ slug: string }> }): Promise<NextResponse>
// Retourne: ModelWithImagesAndVisuals ou 404
```

From src/app/api/models/[slug]/visuals/route.ts:
```typescript
// GET /api/models/:slug/visuals — Retourne les visuels publies pour un modele
export async function GET(request: NextRequest, { params }: { params: Promise<{ slug: string }> }): Promise<NextResponse>
// Retourne: Array<GeneratedVisual & { fabric: Fabric; model_image: ModelImage }> filtre is_published=true
```

From src/app/api/admin/models/route.ts:
```typescript
// GET /api/admin/models — Liste tous les modeles (admin, pas de filtre is_active)
// POST /api/admin/models — Cree un modele
export async function GET(request: NextRequest): Promise<NextResponse>
export async function POST(request: NextRequest): Promise<NextResponse>
```

<!-- Helpers disponibles (Plan 01) -->
From src/__tests__/integration/helpers/auth.ts:
```typescript
export async function loginAsAdmin(): Promise<{ accessToken: string; cookieHeader: string }>
```

From src/__tests__/integration/helpers/seed.ts:
```typescript
export async function seedAdminUser(): Promise<string>
export async function seedTestData(): Promise<void>
```

From src/__tests__/integration/helpers/cleanup.ts:
```typescript
export async function cleanupTestData(options?: { modelSlugs?: string[]; fabricSlugs?: string[] }): Promise<void>
export async function cleanupAll(): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests integration routes publiques (3 routes, cas nominal + erreur)</name>
  <files>src/__tests__/integration/routes/public-models.integration.test.ts</files>
  <read_first>
    src/app/api/models/route.ts,
    src/app/api/models/[slug]/route.ts,
    src/app/api/models/[slug]/visuals/route.ts,
    src/__tests__/integration/helpers/seed.ts,
    src/__tests__/integration/helpers/supabase-admin.ts,
    src/__tests__/integration/helpers/cleanup.ts,
    src/types/database.ts
  </read_first>
  <action>
Creer `src/__tests__/integration/routes/public-models.integration.test.ts`.

CRITIQUE : `import { testApiHandler } from 'next-test-api-route-handler'` DOIT etre le PREMIER import du fichier (Pitfall 1 RESEARCH.md — NTARH patche les internals Next.js).

Structure du fichier :

```typescript
import { testApiHandler } from 'next-test-api-route-handler' // PREMIER IMPORT OBLIGATOIRE
import * as modelsRoute from '@/app/api/models/route'
import * as modelBySlugRoute from '@/app/api/models/[slug]/route'
import * as modelVisualsRoute from '@/app/api/models/[slug]/visuals/route'
import { seedAdminUser, seedTestData } from '../helpers/seed'
import { adminClient } from '../helpers/supabase-admin'
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
```

**beforeAll:** Appeler `seedAdminUser()` puis `seedTestData()` pour s'assurer que les donnees de test existent dans l'instance locale.

**afterAll:** Appeler `cleanupAll()` ou laisser les donnees de seed (elles seront nettoyees par les autres suites si necessaire). Le plus simple est de ne PAS nettoyer les donnees de seed dans cette suite (elles sont idempotentes via upsert).

**Tests a ecrire :**

1. `describe('GET /api/models')`:
   - `it('retourne les modeles actifs uniquement')` — Verifier que la reponse est un tableau, que 'Milano Test' est present, que 'Roma Inactif' est ABSENT (per D-12 — RLS + filtre is_active).
   - `it('retourne les model_images triees par sort_order')` — Verifier que Milano Test a 2 images, que la premiere est 'face' (sort_order 0).
   - `it('retourne un tableau vide si aucun modele actif')` — Creer un etat temporaire ou tous les modeles sont inactifs via adminClient, verifier la reponse, puis restaurer.

2. `describe('GET /api/models/[slug]')`:
   - `it('retourne le modele par slug avec images')` — Verifier `params: { slug: 'milano-test' }`, status 200, data contient name, price, model_images.
   - `it('retourne 404 pour un slug inexistant')` — `params: { slug: 'inexistant-xyz' }`, status 404.
   - `it('retourne 404 pour un modele inactif')` — `params: { slug: 'roma-inactif' }`, status 404 (car la route filtre is_active=true).

3. `describe('GET /api/models/[slug]/visuals')`:
   - `it('retourne les visuels publies pour le modele')` — `params: { slug: 'milano-test' }`, verifier que le visuel publie est present avec fabric info.
   - `it('retourne un tableau vide si aucun visuel publie')` — Utiliser un slug avec aucun visuel publie ou un modele sans visuel.
   - `it('retourne 404 pour un slug inexistant')` — `params: { slug: 'inexistant' }`, status 404.

Pattern NTARH pour les routes avec params dynamiques :
```typescript
await testApiHandler({
  appHandler: modelBySlugRoute,
  params: { slug: 'milano-test' },
  async test({ fetch }) {
    const res = await fetch({ method: 'GET' })
    expect(res.status).toBe(200)
    const data = await res.json()
    expect(data.name).toBe('Milano Test')
  },
})
```

Chaque assertion doit verifier le status HTTP ET le contenu de la reponse JSON.
  </action>
  <verify>
    <automated>npx vitest run --project integration src/__tests__/integration/routes/public-models.integration.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/__tests__/integration/routes/public-models.integration.test.ts
    - First import line is `import { testApiHandler } from 'next-test-api-route-handler'`
    - File contains `import * as modelsRoute from '@/app/api/models/route'`
    - File contains `import * as modelBySlugRoute from '@/app/api/models/[slug]/route'`
    - File contains `import * as modelVisualsRoute from '@/app/api/models/[slug]/visuals/route'`
    - File contains `describe('GET /api/models'` with at least 2 test cases
    - File contains `describe('GET /api/models/[slug]'` with at least 2 test cases including 404
    - File contains `describe('GET /api/models/[slug]/visuals'` with at least 2 test cases
    - File contains `expect(res.status).toBe(200)` and `expect(res.status).toBe(404)`
    - File contains assertion checking 'Roma Inactif' is absent from public results (D-12 RLS verification)
    - File contains `seedTestData()` in beforeAll
    - All tests pass with `npx vitest run --project integration`
  </acceptance_criteria>
  <done>9 tests d'integration pour les 3 routes publiques (listing actifs, slug, visuels publies) avec cas nominal + 404 + RLS verification, tous passent au vert</done>
</task>

<task type="auto">
  <name>Task 2: Tests integration auth reelle + RLS policies</name>
  <files>src/__tests__/integration/routes/auth-rls.integration.test.ts</files>
  <read_first>
    src/lib/supabase/admin.ts,
    src/app/api/admin/models/route.ts,
    src/__tests__/integration/helpers/auth.ts,
    src/__tests__/integration/helpers/seed.ts,
    src/__tests__/integration/helpers/supabase-admin.ts
  </read_first>
  <action>
Creer `src/__tests__/integration/routes/auth-rls.integration.test.ts`.

CRITIQUE : `import { testApiHandler } from 'next-test-api-route-handler'` DOIT etre le PREMIER import.

```typescript
import { testApiHandler } from 'next-test-api-route-handler' // PREMIER IMPORT OBLIGATOIRE
import * as adminModelsRoute from '@/app/api/admin/models/route'
import * as adminFabricsRoute from '@/app/api/admin/fabrics/route'
import { loginAsAdmin } from '../helpers/auth'
import { seedAdminUser, seedTestData } from '../helpers/seed'
import { describe, it, expect, beforeAll } from 'vitest'
```

**beforeAll:** `seedAdminUser()` puis `seedTestData()`.

**Tests a ecrire :**

1. `describe('Auth reelle — requireAdmin (D-07, D-08)')`:
   - `it('retourne 401 sans Cookie d\'authentification')` — Appeler GET /api/admin/models sans header Cookie. Verifier status 401 et message 'Non authentifie'.
   - `it('retourne 401 avec un token invalide/expire')` — Envoyer `Cookie: sb-default-auth-token=invalide`. Verifier status 401.
   - `it('retourne 200 avec un JWT valide via signInWithPassword')` — Appeler `loginAsAdmin()`, utiliser le cookieHeader dans la requete. Verifier status 200 et que la reponse est un tableau de modeles.
   - `it('retourne 200 avec JWT pour POST admin aussi')` — Creer un modele via POST /api/admin/models avec le JWT. Verifier status 201. Nettoyer le modele cree.

2. `describe('RLS policies (D-12)')`:
   - `it('les routes publiques ne montrent pas les modeles inactifs')` — Ce test est aussi dans Plan 02 Task 1 mais valide ici que c'est bien une politique RLS et pas juste un WHERE.
   - `it('les routes admin montrent TOUS les modeles (actifs et inactifs)')` — GET /api/admin/models avec JWT. Verifier que 'Roma Inactif' est present dans la reponse.
   - `it('un user non auth ne peut pas creer via POST /api/admin/models')` — POST sans Cookie. Verifier 401.
   - `it('un user non auth ne peut pas lister les tissus admin')` — GET /api/admin/fabrics sans Cookie. Verifier 401.

Pour le test POST, utiliser un modele avec un slug unique (prefixer `test-auth-`) et le supprimer dans `afterAll` via adminClient :
```typescript
afterAll(async () => {
  await adminClient.from('models').delete().like('slug', 'test-auth-%')
})
```
  </action>
  <verify>
    <automated>npx vitest run --project integration src/__tests__/integration/routes/auth-rls.integration.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/__tests__/integration/routes/auth-rls.integration.test.ts
    - First import is `import { testApiHandler } from 'next-test-api-route-handler'`
    - File contains `loginAsAdmin()` call in beforeAll or in tests
    - File contains test verifying 401 without Cookie header
    - File contains test verifying 401 with invalid token
    - File contains test verifying 200 with valid JWT from signInWithPassword
    - File contains test verifying admin routes show inactive models (Roma Inactif)
    - File contains test verifying unauthenticated POST returns 401
    - File contains `expect(res.status).toBe(401)` at least twice
    - File contains `expect(res.status).toBe(200)` at least once
    - File contains cleanup via adminClient in afterAll
    - All tests pass with `npx vitest run --project integration`
  </acceptance_criteria>
  <done>8 tests d'integration pour l'auth reelle (JWT via signInWithPassword, 401 sans token, 401 token invalide) et les politiques RLS (admin voit inactifs, public ne voit pas inactifs, non-auth bloque sur POST), tous passent au vert</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon -> admin routes | Les tests verifient que les routes admin rejettent les requetes non authentifiees |
| RLS public vs all | Les tests verifient que les routes publiques filtrent is_active=true |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-04 | Elevation of Privilege | admin routes sans token | mitigate | Test explicite 401 sans Cookie dans auth-rls.integration.test.ts |
| T-15.1-05 | Information Disclosure | modeles inactifs visibles publiquement | mitigate | Test explicite que GET /api/models exclut 'Roma Inactif' |
| T-15.1-06 | Spoofing | token JWT invalide | mitigate | Test explicite 401 avec token invalide |
</threat_model>

<verification>
- `npx vitest run --project integration` passe tous les tests (public-models + auth-rls)
- Les routes publiques retournent uniquement les modeles/visuels actifs/publies
- requireAdmin() avec vrai JWT retourne 200, sans token retourne 401
</verification>

<success_criteria>
Les 3 routes publiques et l'auth reelle avec RLS sont testees en integration. Minimum 15 tests passant au vert.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-tests-int-gration-supabase/15.1-02-SUMMARY.md`
</output>
