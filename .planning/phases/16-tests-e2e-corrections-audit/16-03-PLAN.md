---
phase: 16-tests-e2e-corrections-audit
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02
files_modified:
  - e2e/public.spec.ts
  - e2e/admin.spec.ts
autonomous: true
requirements:
  - E2E-02
  - E2E-03

must_haves:
  truths:
    - "Le parcours public catalogue -> configurateur -> simulation s'execute sans erreur E2E"
    - "Le parcours admin generate -> validate -> publish s'execute sans erreur E2E"
    - "Les tests verifient les cas d'erreur (404, auth 401, upload invalide)"
    - "Les tests verifient la responsivite sur desktop (1280x720) et mobile (375x667)"
    - "L'audit accessibilite WCAG s'execute sur chaque page traversee"
    - "npx playwright test passe au vert"
  artifacts:
    - path: "e2e/public.spec.ts"
      provides: "Tests E2E parcours public : catalogue, configurateur, simulation, responsive, accessibilite"
      min_lines: 100
    - path: "e2e/admin.spec.ts"
      provides: "Tests E2E parcours admin : generate, validate, publish, error cases, accessibilite"
      min_lines: 80
  key_links:
    - from: "e2e/public.spec.ts"
      to: "e2e/fixtures/axe.ts"
      via: "import test from fixtures/axe"
      pattern: "makeAxeBuilder"
    - from: "e2e/admin.spec.ts"
      to: "playwright/.auth/admin.json"
      via: "storageState from playwright.config.ts"
      pattern: "storageState"
---

<objective>
Ecrire les tests E2E Playwright couvrant les deux parcours utilisateur critiques : (1) parcours public catalogue vers configurateur vers simulation, et (2) parcours admin generate vers validate vers publish. Inclure les cas d'erreur, la responsivite, et l'audit accessibilite WCAG.

Purpose: Garantir que les flux utilisateur critiques fonctionnent de bout en bout. Ces tests constituent le filet de securite pour les deployments futurs et verifient que toutes les corrections audit n'ont pas casse de fonctionnalite.
Output: Deux fichiers de test Playwright qui passent au vert, couvrant happy path + error cases + responsive + accessibilite.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tests-e2e-corrections-audit/16-CONTEXT.md
@.planning/phases/16-tests-e2e-corrections-audit/16-RESEARCH.md
@.planning/phases/16-tests-e2e-corrections-audit/16-02-SUMMARY.md

<interfaces>
<!-- Pages et composants traverses par les parcours E2E -->

Public flow (no auth):
- / (page.tsx) -> Hero + HowItWorks + CatalogueSection
- CatalogueSection renders ProductCard[] in a grid
- Click ProductCard -> opens ConfiguratorModal (dialog)
- ConfiguratorModal: fabric swatches + angle thumbnails + generated visuals display
- Simulation section: upload photo -> simulate API -> result display (mock Sharp, ~5ms)

Admin flow (requires auth via storageState):
- /admin/login -> signInWithPassword -> redirect /admin
- /admin/produits -> list of models with images
- /admin/produits/[id] -> ModelForm + IAGenerationSection
- IAGenerationSection: select fabric -> click "Generer" -> POST /api/admin/generate
- After generate: click "Valider" -> POST /api/admin/visuals/[id]/validate
- After validate: click "Publier" -> POST /api/admin/visuals/[id]/publish

Key selectors (verify in read_first):
- Catalogue section: likely has id="catalogue" or data-testid
- ProductCard: has role="article" or data-testid
- ConfiguratorModal: dialog role, fabric swatches, angle thumbnails
- Simulation: file upload input, generate button, result image

storageState for admin tests: loaded automatically from playwright.config.ts project "chromium"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests E2E parcours public — catalogue, configurateur, simulation, responsive, accessibilite (per D-11, D-12, D-13)</name>
  <read_first>
    - playwright.config.ts
    - e2e/fixtures/axe.ts
    - e2e/auth.setup.ts
    - src/app/page.tsx (structure de la page d'accueil — sections, ids, testids)
    - src/components/public/Catalogue/CatalogueSection.tsx (rendu serveur, structure HTML)
    - src/components/public/Catalogue/CatalogueClient.tsx (grille, recherche, structure HTML)
    - src/components/public/Catalogue/ProductCard.tsx (structure carte produit, bouton CTA)
    - src/components/public/Catalogue/ConfiguratorModal.tsx (dialog, swatches, angles, images)
    - src/components/public/Simulation/SimulationSection.tsx (si existe — upload, bouton simuler)
    - src/components/public/Hero/Hero.tsx (structure hero)
    - .planning/phases/16-tests-e2e-corrections-audit/16-RESEARCH.md (Patterns 4, 7 — fixture axe, viewport responsive)
  </read_first>
  <files>e2e/public.spec.ts</files>
  <action>
Creer `e2e/public.spec.ts` avec les tests suivants. Utiliser la fixture axe de `./fixtures/axe` pour les tests d'accessibilite.

IMPORTANT : Avant d'ecrire les tests, lire les composants sources pour identifier les selecteurs corrects (roles ARIA, textes visibles, data-testid). Privilegier `getByRole()`, `getByText()`, `getByLabel()` avant `data-testid` ou selecteurs CSS.

**Structure du fichier :**

```typescript
import { test, expect } from './fixtures/axe'

// Viewports pour tests responsive (per D-12)
const viewports = [
  { name: 'desktop', width: 1280, height: 720 },
  { name: 'mobile', width: 375, height: 667 },
]
```

**Tests a ecrire :**

1. **Page d'accueil — chargement et sections visibles (happy path):**
   - Naviguer vers `/`
   - Verifier que le Hero est visible (heading H1 ou texte cle)
   - Verifier que la section HowItWorks est visible (3 etapes)
   - Verifier que la section Catalogue est visible (au moins 1 produit)
   - Verifier le titre de la page (`Mobel Unique` ou equivalent)

2. **Catalogue — affichage produits et recherche:**
   - Naviguer vers `/` puis scroller jusqu'au catalogue
   - Verifier que les cartes produit sont affichees (au moins 1)
   - Si la recherche existe : taper un terme dans le champ de recherche, verifier que les resultats filtrent
   - Verifier l'image du produit (src contient supabase.co ou placeholder)

3. **Configurateur — ouverture et navigation (happy path):**
   - Cliquer sur un bouton CTA d'un ProductCard (ex: "Configurer" ou "Voir")
   - Verifier que le dialog/modal s'ouvre (role dialog visible)
   - Verifier que les swatches tissu sont visibles
   - Cliquer sur un swatch tissu — verifier que l'image change
   - Si des thumbnails d'angles sont visibles : cliquer sur un autre angle — verifier que l'image change
   - Fermer le dialog (bouton fermer ou Escape)
   - Verifier que le dialog est ferme

4. **Simulation — upload et generation (happy path, mock IA):**
   - Naviguer vers la section simulation (si accessible depuis la page d'accueil ou le configurateur)
   - Si la simulation a un input fichier : uploader une image de test (creer un PNG 1x1 en memoire ou utiliser un fichier fixture)
   - Cliquer sur le bouton de simulation
   - Attendre le resultat (mock Sharp est rapide ~5ms)
   - Verifier que l'image de resultat est affichee
   - Note : si la simulation n'est accessible que depuis le configurateur, integrer ce test dans le flow du test 3

5. **Erreur 404 — page inexistante:**
   - Naviguer vers `/page-inexistante-xyz`
   - Verifier que la page 404 Next.js s'affiche (ou un message d'erreur)

6. **Responsive — tests sur les deux viewports (per D-12):**
   - Pour chaque viewport (desktop 1280x720, mobile 375x667) :
     - `page.setViewportSize(viewport)`
     - Naviguer vers `/`
     - Verifier que le catalogue est visible
     - Verifier que le header est visible
     - Sur mobile : verifier que le menu hamburger est visible (si existe) ou que le header s'adapte

7. **Accessibilite — audit WCAG (per D-13):**
   - Sur la page d'accueil (`/`) : executer `makeAxeBuilder().analyze()` et verifier `violations.length === 0` ou lister les violations
   - Apres ouverture du configurateur (dialog) : executer l'audit axe
   - Utiliser `page.waitForLoadState('networkidle')` avant l'audit pour eviter les faux positifs framer-motion (Pitfall 5 du RESEARCH.md)
   - Si des violations persistent et sont des faux positifs connus (framer-motion aria-hidden), les exclure avec `.disableRules(['aria-hidden-focus'])` et documenter pourquoi

**Precautions :**
- Ne JAMAIS utiliser `page.waitForTimeout()` (anti-pattern Playwright). Utiliser `expect(locator).toBeVisible()`, `page.waitForURL()`, ou `page.waitForLoadState()`.
- Les tests publics n'ont PAS besoin de storageState (pas d'auth admin). Ne pas ajouter `test.use({ storageState: ... })` dans ce fichier.
- Le Mock Sharp est actif automatiquement car NANO_BANANA_API_KEY n'est pas defini (per D-07).
- Si Supabase local n'a pas de donnees seed, les tests de catalogue verront 0 produits. Dans ce cas, les tests doivent quand meme passer (verification que la section est visible meme vide). Les donnees de seed sont gerees par les helpers d'integration Phase 15.1 via Supabase CLI.
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx playwright test e2e/public.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - e2e/public.spec.ts existe et contient au minimum 5 blocs `test(`
    - e2e/public.spec.ts importe depuis `./fixtures/axe` (pas directement de `@playwright/test`)
    - e2e/public.spec.ts contient `makeAxeBuilder` (audit accessibilite)
    - e2e/public.spec.ts contient `setViewportSize` avec les deux viewports (1280x720 et 375x667)
    - e2e/public.spec.ts contient `page.goto('/')` 
    - e2e/public.spec.ts ne contient PAS `waitForTimeout` (anti-pattern)
    - e2e/public.spec.ts ne contient PAS `storageState` (pas de test admin ici)
    - npx playwright test e2e/public.spec.ts retourne exit code 0
  </acceptance_criteria>
  <done>Tests E2E parcours public passent au vert : page d'accueil, catalogue, configurateur, simulation (mock), 404, responsive desktop+mobile, audit accessibilite WCAG</done>
</task>

<task type="auto">
  <name>Task 2: Tests E2E parcours admin — generate, validate, publish, error cases, accessibilite (per D-11, D-13)</name>
  <read_first>
    - playwright.config.ts (storageState et project chromium)
    - e2e/fixtures/axe.ts
    - e2e/auth.setup.ts
    - src/app/admin/(protected)/produits/page.tsx (liste des produits admin)
    - src/app/admin/(protected)/produits/[id]/page.tsx (detail produit)
    - src/app/admin/(protected)/produits/IAGenerationSection.tsx (boutons generer/valider/publier)
    - src/app/admin/(protected)/produits/ModelForm.tsx (formulaire produit)
    - src/app/admin/(protected)/tissus/page.tsx (liste tissus)
    - src/app/admin/login/page.tsx (formulaire login — pour test 401)
    - .planning/phases/16-tests-e2e-corrections-audit/16-RESEARCH.md (Pattern 3 — admin storageState)
  </read_first>
  <files>e2e/admin.spec.ts</files>
  <action>
Creer `e2e/admin.spec.ts` avec les tests suivants. Ce fichier utilise le storageState du projet `chromium` configure dans `playwright.config.ts` — l'auth admin est deja chargee automatiquement via les project dependencies.

IMPORTANT : Avant d'ecrire les tests, lire les pages et composants admin pour identifier les selecteurs corrects (boutons, labels, textes visibles). Privilegier `getByRole()`, `getByText()`, `getByLabel()`.

**Structure du fichier :**

```typescript
import { test, expect } from './fixtures/axe'
import path from 'path'

// storageState est injecte par playwright.config.ts project "chromium"
// Pas besoin de test.use({ storageState: ... }) ici
```

**Tests a ecrire :**

1. **Admin dashboard — acces avec auth (happy path):**
   - Naviguer vers `/admin` (ou `/admin/produits` selon le layout)
   - Verifier que la page admin s'affiche (heading "Produits" ou sidebar visible)
   - Verifier qu'il n'y a PAS de redirection vers `/admin/login` (auth valide)

2. **Admin produits — liste des modeles:**
   - Naviguer vers `/admin/produits`
   - Verifier que la liste de produits s'affiche (au moins les headers de tableau ou cartes)
   - Si des produits existent dans la seed DB : verifier qu'au moins un modele est visible

3. **Admin generate -> validate -> publish (happy path complet, per SC-3):**
   - Naviguer vers `/admin/produits`
   - Cliquer sur un produit (lien vers la page detail)
   - Attendre le chargement de la page detail (heading avec le nom du produit)
   - Dans la section IAGenerationSection :
     a. Selectionner un tissu dans le selecteur (dropdown ou liste)
     b. Cliquer sur le bouton "Generer" (ou equivalent)
     c. Attendre que la generation se termine (mock Sharp ~5ms, attendre disparition du spinner ou apparition de l'image generee)
     d. Verifier que le visuel genere est affiche
   - Cliquer sur le bouton "Valider" (validate)
     e. Attendre la confirmation (badge "Valide" ou changement d'etat visuel)
   - Cliquer sur le bouton "Publier" (publish)
     f. Attendre la confirmation (badge "Publie" ou changement d'etat visuel)
   - Verifier que le visuel est marque comme publie

   Note : Si les boutons ne sont pas directement visibles, scroller vers la section IA. Si le flow necessite de selectionner un angle d'image d'abord, le faire.

4. **Admin 401 — acces sans auth:**
   - Creer un contexte de navigateur SANS storageState :
   ```typescript
   test('admin non authentifie redirige vers login', async ({ browser }) => {
     const context = await browser.newContext() // pas de storageState
     const page = await context.newPage()
     await page.goto('/admin/produits')
     await expect(page).toHaveURL(/.*login.*/)
     await context.close()
   })
   ```
   - Verifier que la navigation vers `/admin/produits` sans auth redirige vers `/admin/login`

5. **Admin tissus — page accessible:**
   - Naviguer vers `/admin/tissus` (si la page existe dans le layout admin)
   - Verifier que la liste des tissus s'affiche

6. **Accessibilite admin — audit WCAG (per D-13):**
   - Sur `/admin/produits` : executer `makeAxeBuilder().analyze()`
   - Verifier que les violations sont 0 (ou exclure les faux positifs connus avec justification)
   - Si une page detail produit est accessible : auditer aussi

**Precautions :**
- Le storageState est charge automatiquement par le projet `chromium` de playwright.config.ts. Ne PAS re-specifier `test.use({ storageState: ... })` sauf pour le test 401 qui cree un contexte sans auth.
- Le Mock Sharp est actif — les generations sont quasi-instantanees (~5ms). Pas besoin de timeout long pour attendre les resultats IA.
- Si la seed DB est vide (pas de modeles/tissus), adapter les tests pour verifier les etats vides plutot que les donnees specifiques.
- Ne JAMAIS utiliser `page.waitForTimeout()`.
- Pour le test 401, utiliser `browser.newContext()` pour creer un contexte sans cookies au lieu de manipuler le storageState.
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx playwright test e2e/admin.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - e2e/admin.spec.ts existe et contient au minimum 4 blocs `test(`
    - e2e/admin.spec.ts importe depuis `./fixtures/axe`
    - e2e/admin.spec.ts contient `makeAxeBuilder` (audit accessibilite)
    - e2e/admin.spec.ts contient un test qui navigue vers `/admin/produits` et verifie l'absence de redirection login
    - e2e/admin.spec.ts contient un test 401 avec `browser.newContext()` sans storageState
    - e2e/admin.spec.ts ne contient PAS `waitForTimeout` (anti-pattern)
    - npx playwright test e2e/admin.spec.ts retourne exit code 0
  </acceptance_criteria>
  <done>Tests E2E parcours admin passent au vert : dashboard, liste produits, generate->validate->publish complet, 401 sans auth, tissus, audit accessibilite WCAG</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test E2E -> /admin/* | Acces via storageState (cookies JWT test local) |
| Test E2E -> Supabase local | Donnees de test dans instance Docker locale |
| Test E2E -> Mock Sharp | Pas d'appel API externe (Gemini non utilise) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Information Disclosure | Test credentials in code | mitigate | Credentials de test lues depuis env vars (.env.test.local), valeurs par defaut sont pour Supabase local uniquement |
| T-16-12 | Tampering | Test data in shared DB | accept | Workers: 1 empeche les conflits, Supabase local reset entre runs |
</threat_model>

<verification>
```bash
# Tous les tests E2E passent
npx playwright test

# Verification finale combinee (SC-1 a SC-4)
npx playwright test && npm run build && npx tsc --noEmit
```
</verification>

<success_criteria>
- npx playwright test passe au vert (exit code 0)
- e2e/public.spec.ts couvre : page d'accueil, catalogue, configurateur, simulation, 404, responsive, accessibilite
- e2e/admin.spec.ts couvre : dashboard, produits, generate->validate->publish, 401, accessibilite
- Aucun test n'utilise waitForTimeout
- Les tests d'accessibilite @axe-core sont executes sur chaque page traversee
</success_criteria>

<output>
After completion, create `.planning/phases/16-tests-e2e-corrections-audit/16-03-SUMMARY.md`
</output>
