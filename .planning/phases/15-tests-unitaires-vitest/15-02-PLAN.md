---
phase: 15-tests-unitaires-vitest
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/require-admin.test.ts
  - src/__tests__/generate-route.test.ts
  - src/__tests__/generate-all-route.test.ts
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "requireAdmin retourne error 401 quand le token est absent (user null)"
    - "requireAdmin retourne error 401 quand getUser retourne une erreur (token expire)"
    - "requireAdmin retourne un client supabase valide quand le user est authentifie"
    - "La route generate retourne 401 avec message francais quand requireAdmin echoue"
    - "La route generate-all retourne 401 avec message francais quand requireAdmin echoue"
  artifacts:
    - path: "src/__tests__/require-admin.test.ts"
      provides: "Tests unitaires requireAdmin() avec mock supabase/server"
      min_lines: 40
    - path: "src/__tests__/generate-route.test.ts"
      provides: "Test 401 ajoute au describe existant"
      contains: "retourne 401 si non authentifie"
    - path: "src/__tests__/generate-all-route.test.ts"
      provides: "Test 401 ajoute au describe existant"
      contains: "retourne 401 si non authentifie"
  key_links:
    - from: "src/__tests__/require-admin.test.ts"
      to: "src/lib/supabase/admin.ts"
      via: "import dynamique apres vi.mock server"
      pattern: "await import.*@/lib/supabase/admin"
    - from: "src/__tests__/generate-route.test.ts"
      to: "src/lib/supabase/admin.ts"
      via: "vi.mocked(requireAdmin) surcharge ponctuelle"
      pattern: "vi\\.mocked.*requireAdmin.*mockResolvedValueOnce"
    - from: "src/__tests__/generate-all-route.test.ts"
      to: "src/lib/supabase/admin.ts"
      via: "vi.mocked(requireAdmin) surcharge ponctuelle"
      pattern: "vi\\.mocked.*requireAdmin.*mockResolvedValueOnce"
---

<objective>
Creer les tests unitaires pour requireAdmin() et ajouter les tests 401 aux routes admin generate et generate-all.

Purpose: Combler le gap TEST-03 — verifier que requireAdmin() retourne 401 sur token absent/expire (D-01) et que les routes admin propagent ce 401 avec message francais (D-02).
Output: require-admin.test.ts (nouveau), generate-route.test.ts et generate-all-route.test.ts (etendus avec cas 401)
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tests-unitaires-vitest/15-RESEARCH.md

<interfaces>
<!-- Implementation requireAdmin() depuis src/lib/supabase/admin.ts -->

```typescript
// src/lib/supabase/admin.ts
import { createClient } from '@/lib/supabase/server'
import { NextResponse } from 'next/server'

export async function requireAdmin() {
  const supabase = await createClient()
  const { data: { user }, error } = await supabase.auth.getUser()

  if (error || !user) {
    return {
      supabase: null,
      user: null,
      error: NextResponse.json(
        { error: 'Non authentifié. Connectez-vous pour accéder à cette ressource.' },
        { status: 401 }
      ),
    }
  }

  return { supabase, user, error: null }
}
```

<!-- Pattern mock existant dans generate-route.test.ts (pour D-02) -->

```typescript
// src/__tests__/generate-route.test.ts (existant)
vi.mock('@/lib/supabase/admin', () => ({
  requireAdmin: vi.fn(() => Promise.resolve({ supabase: mockSupabase, error: null })),
}))
// ...
const { POST, maxDuration } = await import('@/app/api/admin/generate/route')
```

<!-- Pattern mock Supabase server dans simulate-route.test.ts (modele pour D-01) -->

```typescript
// src/__tests__/simulate-route.test.ts
vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn(() => Promise.resolve({ from: mockFrom })),
}))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Creer require-admin.test.ts — test unitaire requireAdmin (D-01)</name>
  <files>src/__tests__/require-admin.test.ts</files>
  <read_first>
    - src/lib/supabase/admin.ts
    - src/lib/supabase/server.ts
    - src/__tests__/simulate-route.test.ts
  </read_first>
  <action>
Creer `src/__tests__/require-admin.test.ts` pour tester la logique interne de requireAdmin() (per D-01).

**PIEGE CRITIQUE (Pitfall 1 de RESEARCH.md)** : Mocker `@/lib/supabase/server` (pas `@/lib/supabase/admin`). Si on mock `@/lib/supabase/admin`, on court-circuite la fonction qu'on teste.

**Structure du fichier :**

```typescript
/**
 * Tests Phase 15 : requireAdmin() unitaire (D-01)
 *
 * Mock @/lib/supabase/server pour controler auth.getUser().
 * Verifie 3 cas : token absent, token expire, user authentifie.
 */

import { describe, it, expect, vi, beforeEach } from 'vitest'

// Mock du client server — controle ce que auth.getUser() retourne
const mockGetUser = vi.fn()

vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn(() => Promise.resolve({
    auth: { getUser: mockGetUser },
    // Ajouter from() stub pour que le client Supabase retourne soit complet
    from: vi.fn(),
  })),
}))

// Import APRES les mocks (pattern etabli)
const { requireAdmin } = await import('@/lib/supabase/admin')

describe('requireAdmin()', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('retourne error 401 si token absent (user null)', async () => {
    mockGetUser.mockResolvedValueOnce({ data: { user: null }, error: null })
    const result = await requireAdmin()
    expect(result.error).not.toBeNull()
    expect(result.supabase).toBeNull()
    expect(result.user).toBeNull()
    expect((result.error as Response).status).toBe(401)
    // Verifier le message francais
    const json = await (result.error as Response).json()
    expect(json.error).toContain('authentifi')
  })

  it('retourne error 401 si getUser retourne une erreur (token expire)', async () => {
    mockGetUser.mockResolvedValueOnce({
      data: { user: null },
      error: { message: 'JWT expired' },
    })
    const result = await requireAdmin()
    expect(result.error).not.toBeNull()
    expect(result.supabase).toBeNull()
    expect(result.user).toBeNull()
    expect((result.error as Response).status).toBe(401)
  })

  it('retourne supabase valide si user authentifie', async () => {
    mockGetUser.mockResolvedValueOnce({
      data: { user: { id: 'admin-user-id', email: 'admin@example.com' } },
      error: null,
    })
    const result = await requireAdmin()
    expect(result.error).toBeNull()
    expect(result.supabase).not.toBeNull()
    expect(result.user).toBeDefined()
    expect(result.user?.id).toBe('admin-user-id')
  })
})
```

**Points d'attention :**
- Le cast `(result.error as Response).status` est necessaire car TypeScript ne sait pas que error est une NextResponse (Pitfall 3 RESEARCH.md)
- `mockGetUser` controle directement ce que `supabase.auth.getUser()` retourne
- Si `(result.error as Response).json()` echoue (Response deja consommee), utiliser `.clone()` avant `.json()`
- Le `from: vi.fn()` stub dans createClient empeche une erreur si requireAdmin passe le client supabase complet au retour
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx vitest run src/__tests__/require-admin.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/require-admin.test.ts existe
    - Le fichier contient `vi.mock('@/lib/supabase/server'` (PAS `vi.mock('@/lib/supabase/admin'`)
    - Le fichier contient `await import('@/lib/supabase/admin')`
    - Le fichier contient 3 tests : token absent (user null), token expire (getUser error), user authentifie
    - Chaque test 401 verifie `(result.error as Response).status` === 401
    - Le test authentifie verifie `result.error` === null et `result.supabase` !== null
    - `npx vitest run src/__tests__/require-admin.test.ts` passe avec 3 tests, 0 echecs
  </acceptance_criteria>
  <done>require-admin.test.ts contient 3 tests passants verifiant les 3 cas de requireAdmin : token absent → 401, token expire → 401, user valide → supabase client</done>
</task>

<task type="auto">
  <name>Task 2: Ajouter tests 401 dans generate-route.test.ts et generate-all-route.test.ts (D-02)</name>
  <files>src/__tests__/generate-route.test.ts, src/__tests__/generate-all-route.test.ts</files>
  <read_first>
    - src/__tests__/generate-route.test.ts
    - src/__tests__/generate-all-route.test.ts
    - src/app/api/admin/generate/route.ts
    - src/app/api/admin/generate-all/route.ts
  </read_first>
  <action>
Ajouter un test `retourne 401 si non authentifie` dans chaque fichier de test route admin existant (per D-02).

**Pattern utilise** : Les deux fichiers mockent deja `@/lib/supabase/admin` globalement avec `requireAdmin` en succes. On utilise `vi.mocked(requireAdmin).mockResolvedValueOnce(...)` pour surcharger ponctuellement le mock dans un seul test.

**Dans `src/__tests__/generate-route.test.ts`** — ajouter ce test APRES le dernier `it()` dans le `describe('POST /api/admin/generate')` :

```typescript
it('retourne 401 si non authentifie', async () => {
  const { requireAdmin } = await import('@/lib/supabase/admin')
  vi.mocked(requireAdmin).mockResolvedValueOnce({
    supabase: null,
    user: null,
    error: NextResponse.json(
      { error: 'Non authentifie. Connectez-vous pour acceder a cette ressource.' },
      { status: 401 }
    ),
  })

  const response = await POST(makeRequest({ model_id: 'm1', model_image_id: 'mi1', fabric_id: 'f1' }) as never)
  expect(response.status).toBe(401)
  const json = await response.json()
  expect(json.error).toContain('authentifi')
})
```

**Ajouter l'import NextResponse** en haut du fichier generate-route.test.ts si absent :
```typescript
import { NextResponse } from 'next/server'
```

**Dans `src/__tests__/generate-all-route.test.ts`** — ajouter un test similaire APRES le dernier `it()` dans le `describe('POST /api/admin/generate-all')` :

```typescript
it('retourne 401 si non authentifie', async () => {
  const { requireAdmin } = await import('@/lib/supabase/admin')
  vi.mocked(requireAdmin).mockResolvedValueOnce({
    supabase: null,
    user: null,
    error: NextResponse.json(
      { error: 'Non authentifie. Connectez-vous pour acceder a cette ressource.' },
      { status: 401 }
    ),
  })

  const response = await POST(makeRequest({ model_id: 'm1', fabric_id: 'f1' }) as never)
  expect(response.status).toBe(401)
  const json = await response.json()
  expect(json.error).toContain('authentifi')
})
```

**Ajouter l'import NextResponse** en haut du fichier generate-all-route.test.ts si absent :
```typescript
import { NextResponse } from 'next/server'
```

**Points d'attention :**
- `vi.mocked(requireAdmin).mockResolvedValueOnce(...)` surcharge le mock global pour un seul appel — le test suivant retrouve le comportement par defaut (succes)
- Le message dans le test utilise `'authentifi'` (sans accent) dans `toContain` pour matcher le message reel `'Non authentifié...'` qui contient un accent — `toContain` fait du string matching
- NE PAS modifier les tests existants, uniquement AJOUTER les nouveaux `it()` blocs
- Verifier que la route admin lit bien `requireAdmin()` et retourne `error` si present (lire la route source avant d'ecrire)
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx vitest run src/__tests__/generate-route.test.ts src/__tests__/generate-all-route.test.ts --reporter=verbose 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - generate-route.test.ts contient `it('retourne 401 si non authentifie'`
    - generate-route.test.ts contient `import { NextResponse } from 'next/server'`
    - generate-route.test.ts contient `expect(response.status).toBe(401)`
    - generate-all-route.test.ts contient `it('retourne 401 si non authentifie'`
    - generate-all-route.test.ts contient `import { NextResponse } from 'next/server'`
    - generate-all-route.test.ts contient `expect(response.status).toBe(401)`
    - `npx vitest run src/__tests__/generate-route.test.ts` passe avec 7 tests (6 existants + 1 nouveau), 0 echecs
    - `npx vitest run src/__tests__/generate-all-route.test.ts` passe avec 6 tests (5 existants + 1 nouveau), 0 echecs
    - Les tests existants ne sont pas modifies (seul ajout de l'import NextResponse et du nouveau it())
  </acceptance_criteria>
  <done>generate-route.test.ts et generate-all-route.test.ts contiennent chacun un test 401 verifiant que la route retourne 401 avec message francais quand requireAdmin echoue</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase test-only — aucun code de production modifie, aucune surface d'attaque creee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | I (Information Disclosure) | require-admin.test.ts | accept | Les tests mockent le client Supabase — aucune connexion reelle a la BDD. Pas de secret expose. |
| T-15-04 | E (Elevation of Privilege) | generate-route 401 test | accept | Le test verifie que l'elevation de privilege est BLOQUEE (401 retourne). C'est un test de securite defensive. |
</threat_model>

<verification>
```bash
# Tests du plan
cd /Users/salah/Desktop/projetmobelunique && npx vitest run src/__tests__/require-admin.test.ts src/__tests__/generate-route.test.ts src/__tests__/generate-all-route.test.ts --reporter=verbose

# Suite complete inchangee
cd /Users/salah/Desktop/projetmobelunique && npm test
```
</verification>

<success_criteria>
- require-admin.test.ts existe avec 3 tests couvrant token absent, token expire, user authentifie
- generate-route.test.ts contient 7 tests (6 existants + 1 test 401)
- generate-all-route.test.ts contient 6 tests (5 existants + 1 test 401)
- `npm test` passe au vert avec tous les tests
- Les tests existants ne sont pas modifies ou casses
</success_criteria>

<output>
After completion, create `.planning/phases/15-tests-unitaires-vitest/15-02-SUMMARY.md`
</output>
