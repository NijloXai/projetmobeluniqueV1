---
phase: 01-fondation-header
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/brand/logo-white.png
  - public/brand/logo-black.png
  - public/brand/icon-192.png
  - public/brand/icon-512.png
  - src/app/favicon.ico
  - src/app/icon.png
  - src/app/apple-icon.png
  - src/app/manifest.ts
  - src/components/public/Header/Header.tsx
  - src/components/public/Header/Header.module.css
autonomous: true
requirements: [FOND-01, FOND-02, FOND-03, FOND-04, HEAD-01, HEAD-02, HEAD-03, HEAD-04]

must_haves:
  truths:
    - "Le header affiche le vrai logo Mobel Unique (monogramme cursif + texte) au lieu du carre ambre MU"
    - "Le logo est blanc sur fond hero sombre (header transparent) et noir apres scroll 80px (header blanc)"
    - "Le lien Retour a la boutique pointe vers https://www.mobelunique.fr/ et ouvre dans le meme onglet"
    - "Le favicon Mobel Unique apparait dans l'onglet du navigateur (plus le triangle Next.js)"
    - "Le manifest web est servi a /manifest.webmanifest avec les icones PWA"
    - "Les 5 fichiers SVG par defaut de Next.js sont supprimes de public/"
  artifacts:
    - path: "public/brand/logo-white.png"
      provides: "Logo blanc complet pour header transparent"
    - path: "public/brand/logo-black.png"
      provides: "Logo noir complet pour header scrolle"
    - path: "public/brand/icon-192.png"
      provides: "Icone PWA 192x192 pour manifest"
    - path: "public/brand/icon-512.png"
      provides: "Icone PWA 512x512 pour manifest"
    - path: "src/app/favicon.ico"
      provides: "Favicon ICO convention Next.js App Router"
    - path: "src/app/icon.png"
      provides: "Icon PNG 32x32 convention Next.js App Router"
    - path: "src/app/apple-icon.png"
      provides: "Apple touch icon 180x180 convention Next.js App Router"
    - path: "src/app/manifest.ts"
      provides: "Web manifest TypeScript avec MetadataRoute.Manifest"
      exports: ["default"]
    - path: "src/components/public/Header/Header.tsx"
      provides: "Header avec next/image conditionnel selon scrolled"
      contains: "next/image"
    - path: "src/components/public/Header/Header.module.css"
      provides: "Style .logoImage pour logo PNG"
  key_links:
    - from: "src/components/public/Header/Header.tsx"
      to: "/brand/logo-white.png, /brand/logo-black.png"
      via: "next/image src conditionnel sur scrolled state"
      pattern: "scrolled.*logo-black.*logo-white"
    - from: "src/app/manifest.ts"
      to: "/brand/icon-192.png, /brand/icon-512.png"
      via: "icons array dans MetadataRoute.Manifest"
      pattern: "icon-192|icon-512"
---

<objective>
Integrer les vrais assets brand Mobel Unique dans le header et les metadonnees du site.

Purpose: Remplacer le placeholder generique "MU" (carre ambre + texte HTML) par le vrai logo client (monogramme cursif + texte "MOBEL UNIQUE" en PNG), ajouter le favicon et les icones PWA selon les conventions Next.js App Router, corriger le lien Shopify, et nettoyer les fichiers Next.js par defaut.

Output: Header avec logo conditionnel blanc/noir, favicon dans l'onglet, manifest PWA, public/ nettoye.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fondation-header/01-CONTEXT.md
@.planning/phases/01-fondation-header/01-RESEARCH.md
@.planning/phases/01-fondation-header/01-UI-SPEC.md
@.planning/phases/01-fondation-header/01-01-SUMMARY.md

<interfaces>
<!-- Fichiers existants que l'executeur modifie — lus pour contexte -->

From src/components/public/Header/Header.tsx (ACTUEL — a modifier):
```tsx
'use client'
import { useState, useEffect } from 'react'
import Link from 'next/link'
import styles from './Header.module.css'

export function Header() {
  const [scrolled, setScrolled] = useState(false)
  // ... scroll listener useEffect
  return (
    <>
      <a href="#main-content" className={styles.skipLink}>Aller au contenu</a>
      <header className={`${styles.header}${scrolled ? ` ${styles.scrolled}` : ''}`} role="banner">
        <Link href="/" className={styles.brand} aria-label="Mobel Unique — Accueil">
          <div className={styles.logo} aria-hidden="true">MU</div>
          <span className={styles.brandName}>Mobel Unique</span>
        </Link>
        <a href="#" className={styles.shopifyLink} rel="noopener noreferrer"
           aria-label="Retourner a la boutique Shopify">
          Retour a la boutique
        </a>
      </header>
    </>
  )
}
```

From src/components/public/Header/Header.module.css (classes a modifier):
```css
/* A SUPPRIMER: .logo (div carre ambre), .brandName (texte HTML) */
/* A AJOUTER: .logoImage (pour next/image PNG) */
/* A CONSERVER TEL QUEL: .skipLink, .header, .scrolled, .brand, .shopifyLink, responsive */
```

From src/app/layout.tsx (NE PAS MODIFIER — pas de metadata.icons):
```tsx
export const metadata: Metadata = {
  title: { template: '%s | Mobel Unique', default: 'Mobel Unique — Canapes personnalisables Paris' },
  description: 'Configurateur IA...',
}
// NE PAS ajouter metadata.icons — conflit avec fichiers convention (RESEARCH.md pitfall 1)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preparer les assets brand et les icones convention Next.js</name>
  <files>
    public/brand/logo-white.png
    public/brand/logo-black.png
    public/brand/icon-192.png
    public/brand/icon-512.png
    src/app/favicon.ico
    src/app/icon.png
    src/app/apple-icon.png
    src/app/manifest.ts
  </files>
  <read_first>
    fichier-mobelunique/Logo Design/Logo-01.png
    fichier-mobelunique/Logo Design/Logo-04.png
    fichier-mobelunique/Favicon/Favicon-04.png
    fichier-mobelunique/Appicon/Appicon 1024X1024-01.png
    src/app/layout.tsx
  </read_first>
  <action>
    **Etape 1 — Creer le dossier et copier les logos (per D-03, D-10)**

    ```bash
    mkdir -p public/brand
    cp "fichier-mobelunique/Logo Design/Logo-04.png" public/brand/logo-white.png
    cp "fichier-mobelunique/Logo Design/Logo-01.png" public/brand/logo-black.png
    ```

    **Etape 2 — Generer favicon.ico 32x32 via sips (per D-04)**

    ```bash
    sips -z 32 32 "fichier-mobelunique/Favicon/Favicon-04.png" --out /tmp/mu-favicon-32.png
    sips -s format ico /tmp/mu-favicon-32.png --out src/app/favicon.ico
    rm /tmp/mu-favicon-32.png
    ```

    **Etape 3 — Generer icon.png 32x32 (per D-04)**

    ```bash
    sips -z 32 32 "fichier-mobelunique/Favicon/Favicon-04.png" --out src/app/icon.png
    ```

    **Etape 4 — Generer apple-icon.png 180x180 (per D-05)**

    ```bash
    sips -z 180 180 "fichier-mobelunique/Appicon/Appicon 1024X1024-01.png" --out src/app/apple-icon.png
    ```

    **Etape 5 — Generer icones manifest PWA (per D-05)**

    ```bash
    sips -z 192 192 "fichier-mobelunique/Appicon/Appicon 1024X1024-01.png" --out public/brand/icon-192.png
    sips -z 512 512 "fichier-mobelunique/Appicon/Appicon 1024X1024-01.png" --out public/brand/icon-512.png
    ```

    **Etape 6 — Supprimer les assets Next.js par defaut (per D-12)**

    ```bash
    rm -f public/file.svg public/globe.svg public/next.svg public/vercel.svg public/window.svg
    ```

    Verifier qu'il ne reste aucun fichier .svg dans public/ apres suppression.

    **Etape 7 — Creer src/app/manifest.ts (per D-05, D-06)**

    Creer le fichier `src/app/manifest.ts` avec le contenu exact suivant :

    ```typescript
    import type { MetadataRoute } from 'next'

    export default function manifest(): MetadataRoute.Manifest {
      return {
        name: 'Mobel Unique — Canapes personnalisables Paris',
        short_name: 'Mobel Unique',
        description: 'Configurateur IA de canapes personnalisables.',
        start_url: '/',
        display: 'standalone',
        background_color: '#FFFFFF',
        theme_color: '#E49400',
        icons: [
          {
            src: '/brand/icon-192.png',
            sizes: '192x192',
            type: 'image/png',
          },
          {
            src: '/brand/icon-512.png',
            sizes: '512x512',
            type: 'image/png',
          },
        ],
      }
    }
    ```

    **IMPORTANT — NE PAS modifier layout.tsx.** Ne pas ajouter `metadata.icons` dans layout.tsx — les fichiers convention (favicon.ico, icon.png, apple-icon.png) dans src/app/ suffisent. Ajouter metadata.icons en parallele causerait des balises `<link>` dupliques (RESEARCH.md pitfall 1).
  </action>
  <verify>
    <automated>test -f public/brand/logo-white.png && test -f public/brand/logo-black.png && test -f public/brand/icon-192.png && test -f public/brand/icon-512.png && test -f src/app/favicon.ico && test -f src/app/icon.png && test -f src/app/apple-icon.png && test -f src/app/manifest.ts && test $(ls public/*.svg 2>/dev/null | wc -l) -eq 0 && echo "ALL ASSETS OK" || echo "ASSET CHECK FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `public/brand/logo-white.png` existe et sa taille est > 100KB (logo source ~130KB)
    - `public/brand/logo-black.png` existe et sa taille est > 100KB (logo source ~142KB)
    - `public/brand/icon-192.png` existe (taille 192x192, verifiable via `sips -g pixelHeight -g pixelWidth public/brand/icon-192.png`)
    - `public/brand/icon-512.png` existe (taille 512x512)
    - `src/app/favicon.ico` existe et sa taille est > 0 octets
    - `src/app/icon.png` existe (taille 32x32)
    - `src/app/apple-icon.png` existe (taille 180x180)
    - `src/app/manifest.ts` contient `MetadataRoute.Manifest` et `theme_color: '#E49400'`
    - `ls public/*.svg 2>/dev/null | wc -l` retourne 0 (aucun SVG Next.js par defaut restant)
    - `npx tsc --noEmit` passe sans erreur pour manifest.ts
  </acceptance_criteria>
  <done>
    Les 7 assets brand sont en place (2 logos, 2 icones PWA, favicon.ico, icon.png, apple-icon.png), le manifest.ts est cree et type-safe, et les 5 SVG Next.js par defaut sont supprimes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remplacer le logo placeholder par next/image conditionnel et corriger le lien Shopify</name>
  <files>
    src/components/public/Header/Header.tsx
    src/components/public/Header/Header.module.css
  </files>
  <read_first>
    src/components/public/Header/Header.tsx
    src/components/public/Header/Header.module.css
    .planning/phases/01-fondation-header/01-UI-SPEC.md
    .planning/phases/01-fondation-header/01-RESEARCH.md
  </read_first>
  <action>
    **Modifier Header.tsx (per D-01, D-02, D-07, D-08, D-09)**

    Remplacer le contenu de `src/components/public/Header/Header.tsx` par :

    ```tsx
    'use client'

    import { useState, useEffect } from 'react'
    import Image from 'next/image'
    import Link from 'next/link'
    import styles from './Header.module.css'

    export function Header() {
      const [scrolled, setScrolled] = useState(false)

      useEffect(() => {
        const handleScroll = () => {
          const isScrolled = window.scrollY > 80
          setScrolled(prev => prev !== isScrolled ? isScrolled : prev)
        }
        window.addEventListener('scroll', handleScroll, { passive: true })
        return () => window.removeEventListener('scroll', handleScroll)
      }, [])

      return (
        <>
          <a href="#main-content" className={styles.skipLink}>
            Aller au contenu
          </a>
          <header
            className={`${styles.header}${scrolled ? ` ${styles.scrolled}` : ''}`}
            role="banner"
          >
            <Link href="/" className={styles.brand} aria-label="Mobel Unique — Accueil">
              <Image
                src={scrolled ? '/brand/logo-black.png' : '/brand/logo-white.png'}
                alt="Mobel Unique"
                width={144}
                height={144}
                className={styles.logoImage}
                priority
              />
            </Link>
            <a
              href="https://www.mobelunique.fr/"
              className={styles.shopifyLink}
              rel="noopener"
              aria-label="Retourner a la boutique Mobel Unique"
            >
              Retour a la boutique
            </a>
          </header>
        </>
      )
    }
    ```

    Changements par rapport a l'existant :
    1. Ajout `import Image from 'next/image'`
    2. Remplacement de `<div className={styles.logo} aria-hidden="true">MU</div>` + `<span className={styles.brandName}>Mobel Unique</span>` par un seul `<Image>` avec src conditionnel selon `scrolled` (per D-02)
    3. `width={144} height={144}` : ratio 1:1 = ratio du canvas source PNG 4168x4167. La taille rendue est controlee par CSS `.logoImage { height: 36px; width: auto }` (per RESEARCH.md Pattern 1)
    4. `priority` : desactive lazy loading (logo above-the-fold, LCP candidate)
    5. `href` du lien Shopify : `"#"` remplace par `"https://www.mobelunique.fr/"` (per D-08)
    6. `rel` du lien Shopify : `"noopener noreferrer"` remplace par `"noopener"` seul (meme onglet, per D-09)
    7. `aria-label` du lien Shopify : `"Retourner a la boutique Shopify"` remplace par `"Retourner a la boutique Mobel Unique"` (per UI-SPEC)
    8. Suppression de `target` (pas de `target="_blank"` — meme onglet, per D-09)

    **Modifier Header.module.css**

    Dans `src/components/public/Header/Header.module.css` :

    1. **SUPPRIMER** le bloc `.logo` (lignes 53-66 — div carre ambre "MU")
    2. **SUPPRIMER** le bloc `.brandName` (lignes 77-81 — texte HTML "Mobel Unique")
    3. **AJOUTER** apres le bloc `.brand` le nouveau bloc :

    ```css
    /* Logo image conditionnel blanc/noir (HEAD-01) */
    .logoImage {
      height: 36px;
      width: auto;
      object-fit: contain;
      display: block;
    }
    ```

    4. **CONSERVER TEL QUEL** : `.skipLink`, `.skipLink:focus`, `.header`, `.scrolled`, `.brand`, `.shopifyLink`, `.shopifyLink:hover`, et les media queries responsive. Ne modifier AUCUN de ces blocs.

    Le bloc `.brand` existant (`display: flex; align-items: center; gap: var(--spacing-sm); text-decoration: none; color: inherit;`) reste valide — le gap n'a plus d'effet avec un seul enfant (l'image), ce qui est correct.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && grep -c "next/image" src/components/public/Header/Header.tsx && grep -c "logo-white" src/components/public/Header/Header.tsx && grep -c "logo-black" src/components/public/Header/Header.tsx && grep -c "mobelunique.fr" src/components/public/Header/Header.tsx && grep -c "logoImage" src/components/public/Header/Header.module.css && echo "HEADER CHECK OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passe sans erreur
    - `grep "import Image from 'next/image'" src/components/public/Header/Header.tsx` retourne une correspondance
    - `grep "logo-white.png" src/components/public/Header/Header.tsx` retourne une correspondance
    - `grep "logo-black.png" src/components/public/Header/Header.tsx` retourne une correspondance
    - `grep "mobelunique.fr" src/components/public/Header/Header.tsx` retourne une correspondance
    - `grep "target" src/components/public/Header/Header.tsx` ne retourne AUCUNE correspondance (pas de target="_blank")
    - `grep "noreferrer" src/components/public/Header/Header.tsx` ne retourne AUCUNE correspondance (rel="noopener" seul)
    - `grep "\.logo " src/components/public/Header/Header.module.css` ne retourne AUCUNE correspondance (ancien bloc .logo supprime)
    - `grep "\.brandName" src/components/public/Header/Header.module.css` ne retourne AUCUNE correspondance (ancien bloc supprime)
    - `grep "\.logoImage" src/components/public/Header/Header.module.css` retourne une correspondance (nouveau bloc ajoute)
    - `grep "height: 36px" src/components/public/Header/Header.module.css` retourne une correspondance
    - `grep "priority" src/components/public/Header/Header.tsx` retourne une correspondance
    - `npm run build` complete sans erreur
  </acceptance_criteria>
  <done>
    Le header affiche le vrai logo Mobel Unique via next/image — blanc sur hero sombre, noir apres scroll. Le lien Shopify pointe vers https://www.mobelunique.fr/ dans le meme onglet. Les anciens blocs CSS .logo et .brandName sont supprimes, remplaces par .logoImage.
  </done>
</task>

</tasks>

<verification>
Verifications globales apres les 2 taches :

1. `npm run build` passe sans erreur (TypeScript + build Next.js)
2. `ls public/brand/` contient exactement : logo-white.png, logo-black.png, icon-192.png, icon-512.png
3. `ls src/app/favicon.ico src/app/icon.png src/app/apple-icon.png src/app/manifest.ts` — les 4 fichiers existent
4. `ls public/*.svg 2>/dev/null | wc -l` retourne 0
5. `grep "logo-white\|logo-black" src/components/public/Header/Header.tsx` retourne 2 correspondances
6. `grep "mobelunique.fr" src/components/public/Header/Header.tsx` retourne 1 correspondance
7. Le dev server (`npm run dev`) affiche le header avec le logo brand au lieu du carre "MU"
</verification>

<success_criteria>
- Le build Next.js (`npm run build`) passe sans erreur
- Le header utilise next/image avec swap conditionnel blanc/noir selon l'etat scrolled
- Le favicon Mobel Unique remplace le favicon Next.js par defaut dans l'onglet
- Le manifest.webmanifest est servi avec les bonnes icones PWA
- Le lien "Retour a la boutique" ouvre https://www.mobelunique.fr/ dans le meme onglet
- Aucun fichier SVG Next.js par defaut ne reste dans public/
- Les classes CSS .logo et .brandName sont supprimees, remplacees par .logoImage
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation-header/01-02-SUMMARY.md`
</output>
