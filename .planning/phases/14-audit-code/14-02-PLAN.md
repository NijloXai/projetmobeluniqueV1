---
phase: 14-audit-code
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - .planning/phases/14-audit-code/AUDIT.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - AUDIT-04

must_haves:
  truths:
    - "AUDIT.md liste tous les imports inutilises, fichiers orphelins, et exports morts (AUDIT-03)"
    - "AUDIT.md identifie les failles de securite avec niveau de severite (AUDIT-01)"
    - "AUDIT.md identifie les requetes N+1 et assets non optimises (AUDIT-02)"
    - "AUDIT.md liste les violations TypeScript strict avec localisation fichier:ligne (AUDIT-04)"
    - "Chaque finding a le format fichier:ligne + severite + description + suggestion (D-09, D-15)"
    - "Les 4 categories sont presentes : Securite, Performance, Dead Code, TypeScript (D-07)"
  artifacts:
    - path: ".planning/phases/14-audit-code/AUDIT.md"
      provides: "Rapport d'audit consolide avec 4 sections"
      min_lines: 100
      contains: "## Securite"
  key_links:
    - from: "AUDIT.md"
      to: "scripts/audit-code.ts"
      via: "JSON output parsed and formatted"
      pattern: "audit-code"
    - from: "AUDIT.md"
      to: "npx eslint"
      via: "ESLint output parsed"
      pattern: "ESLint"
    - from: "AUDIT.md"
      to: "npx knip"
      via: "knip output parsed"
      pattern: "knip"
---

<objective>
Executer les 3 outils d'audit (ESLint, knip, script custom), analyser les resultats, et consolider tout dans un AUDIT.md unique et detaille.

Purpose: Produire le livrable final de la phase — un rapport AUDIT.md qui documente TOUS les problemes identifies avec suffisamment de detail pour que Phase 16 puisse corriger sans re-analyser (D-06, D-15).
Output: .planning/phases/14-audit-code/AUDIT.md
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-audit-code/14-CONTEXT.md
@.planning/phases/14-audit-code/14-RESEARCH.md
@.planning/phases/14-audit-code/14-01-SUMMARY.md

<interfaces>
<!-- Script custom output format (cree en Plan 01) -->
From scripts/audit-code.ts:
```typescript
interface Finding {
  severity: 'Critical' | 'Warning' | 'Info'
  category: 'Securite' | 'Performance' | 'DeadCode' | 'TypeScript' | 'CSS' | 'Config'
  file: string
  line: number
  description: string
  suggestion: string
}
// Output: JSON array de Finding sur stdout
// Usage: npx tsx scripts/audit-code.ts
```

<!-- Outils disponibles apres Plan 01 -->
ESLint: `npx eslint src/ --max-warnings 9999 --format json`
knip: `npx knip --reporter json`
tsc: `npx tsc --noEmit`
Script custom: `npx tsx scripts/audit-code.ts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Executer les outils d'audit et collecter les donnees brutes</name>
  <files>.planning/phases/14-audit-code/AUDIT.md</files>
  <read_first>
    - .planning/phases/14-audit-code/14-01-SUMMARY.md (resultats installation outils)
    - .planning/phases/14-audit-code/14-CONTEXT.md (decisions D-07 a D-15 pour le format du rapport)
    - .planning/phases/14-audit-code/14-RESEARCH.md (Pattern 4 format AUDIT.md, zones a inspecter)
    - eslint.config.mjs (regles actives pour interpreter les resultats ESLint)
    - knip.json (config pour interpreter les resultats knip)
    - scripts/audit-code.ts (comprendre quels patterns sont couverts)
    - next.config.ts (a auditer manuellement pour security headers et config)
    - src/proxy.ts (middleware auth critique a examiner)
    - src/lib/schemas.ts (schemas Zod, verifier completeness)
  </read_first>
  <action>
**Etape 1 — Executer les 4 outils et capturer les outputs :**

```bash
# 1. ESLint — capturer en JSON
npx eslint src/ --max-warnings 9999 --format json > /tmp/eslint-report.json 2>&1 || true

# 2. knip — capturer en JSON
npx knip --reporter json > /tmp/knip-report.json 2>&1 || true

# 3. TypeScript check
npx tsc --noEmit 2>&1 > /tmp/tsc-report.txt || true

# 4. Script custom
npx tsx scripts/audit-code.ts > /tmp/audit-code-report.json 2>/tmp/audit-code-summary.txt || true
```

**Etape 2 — Analyser chaque output :**

Lire chaque fichier de rapport genere. Pour chaque finding, extraire :
- Fichier et ligne exacte
- Type de violation
- Message d'erreur

**Etape 3 — Revue manuelle complementaire :**

En plus des outils automatiques, faire une revue manuelle des fichiers critiques suivants (lire le code et analyser) :

- `src/proxy.ts` — Verifier la logique de refresh token, les edge cases (token expire, session invalide)
- `next.config.ts` — Verifier les security headers, remotePatterns, experimental flags
- `src/app/api/simulate/route.ts` — Route publique, verifier validation input complete (taille, type MIME, rate-limit)
- `src/lib/ai/nano-banana.ts` — Code Phase 13 recent, verifier error handling, retry logic, secret leaks
- `src/app/api/admin/generate-all/route.ts` — Logique batch, verifier error accumulation, timeout

Pour chaque finding manuel, noter fichier:ligne exact et description precise.

**Etape 4 — Consolider dans AUDIT.md :**

Creer `.planning/phases/14-audit-code/AUDIT.md` avec la structure suivante (per D-07) :

```markdown
# Audit Code — Mobel Unique

**Date :** 2026-04-09
**Perimetre :** src/ (XX fichiers TS/TSX, XX CSS modules) + configs + scripts
**Outils :** ESLint 9.39.4, knip 6.3.x, tsc --noEmit, scripts/audit-code.ts
**Phase :** 14-audit-code — documentation seulement, aucune correction

## Resume Executif

| Categorie | Critical | Warning | Info | Total |
|-----------|----------|---------|------|-------|
| Securite | X | X | X | X |
| Performance | X | X | X | X |
| Dead Code | X | X | X | X |
| TypeScript & Bonnes Pratiques | X | X | X | X |
| **Total** | **X** | **X** | **X** | **X** |

## Securite (AUDIT-01)

[Pour chaque finding securite, le format est :]

### SEC-XX: [Titre court]
**Fichier :** `src/path/to/file.ts:NN`
**Severite :** Critical | Warning | Info
**Source :** ESLint | knip | script custom | revue manuelle
**Description :** [Description precise du probleme avec le code incrimine]
**Code :**
```typescript
// 1-3 lignes du code problematique
```
**Suggestion :** [Comment corriger — assez detaille pour Phase 16]

## Performance (AUDIT-02)

[Meme format SEC-XX mais PERF-XX]

## Dead Code (AUDIT-03)

[Meme format — DEAD-XX]
[Inclure les resultats knip : fichiers orphelins, exports morts, deps inutilisees]
[Inclure les resultats ESLint : no-unused-vars]

## TypeScript & Bonnes Pratiques (AUDIT-04)

[Meme format — TS-XX]
[Inclure les violations ESLint : no-explicit-any, no-console]
[Inclure les findings du script custom : catch sans typage, async sans try/catch]
[Inclure les findings CSS : inline styles, classes non utilisees]
```

**Regles de tri des severites (per D-08) :**
- **Critical :** Faille securite exploitable ou bug runtime (route admin sans auth, injection, XSS)
- **Warning :** A corriger pour qualite du code (N+1, console.log en prod, missing validation)
- **Info :** Amelioration souhaitable (inline style, missing return type, convention CSS)

**Reporter TOUT sans seuil minimum (per D-10).** Meme les Info sont listes.

**Chaque finding DOIT avoir (per D-09, D-15) :**
- `fichier:ligne` exact (navigable dans l'IDE)
- Code incrimine (1-3 lignes)
- Description claire du probleme
- Suggestion de correction (assez detaillee pour Phase 16)

**NE PAS corriger de code (per D-14).** Si un finding est tentant a fix, le documenter mais NE PAS modifier le fichier source.

**Verifier que le rapport est complet :**
- La section Securite couvre : auth bypass, validation inputs, XSS, security headers
- La section Performance couvre : N+1 queries, console.log, assets
- La section Dead Code couvre : fichiers orphelins, exports morts, deps inutilisees, imports morts
- La section TypeScript couvre : any implicites, error handling, catch sans typage
  </action>
  <verify>
    <automated>test -f .planning/phases/14-audit-code/AUDIT.md && grep -c "## Securite\|## Performance\|## Dead Code\|## TypeScript" .planning/phases/14-audit-code/AUDIT.md && echo "Sections found" && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - AUDIT.md existe : `test -f .planning/phases/14-audit-code/AUDIT.md && echo OK`
    - AUDIT.md contient la section Securite : `grep '## Securite\|## Sécurité' .planning/phases/14-audit-code/AUDIT.md` retourne au moins une ligne
    - AUDIT.md contient la section Performance : `grep '## Performance' .planning/phases/14-audit-code/AUDIT.md` retourne au moins une ligne
    - AUDIT.md contient la section Dead Code : `grep '## Dead Code' .planning/phases/14-audit-code/AUDIT.md` retourne au moins une ligne
    - AUDIT.md contient la section TypeScript : `grep '## TypeScript' .planning/phases/14-audit-code/AUDIT.md` retourne au moins une ligne
    - AUDIT.md contient un Resume Executif : `grep 'Resume Executif\|Résumé Exécutif' .planning/phases/14-audit-code/AUDIT.md` retourne une ligne
    - AUDIT.md contient le tableau recapitulatif : `grep 'Critical.*Warning.*Info' .planning/phases/14-audit-code/AUDIT.md` retourne au moins une ligne
    - Chaque finding a un format fichier:ligne : `grep -c 'src/.*:[0-9]' .planning/phases/14-audit-code/AUDIT.md` retourne un nombre > 0
    - Chaque finding a une severite : `grep -c 'Critical\|Warning\|Info' .planning/phases/14-audit-code/AUDIT.md` retourne un nombre > 5
    - AUDIT.md fait au moins 100 lignes : `wc -l .planning/phases/14-audit-code/AUDIT.md | awk '{print ($1 >= 100) ? "OK" : "TOO SHORT"}'`
    - Aucun fichier source modifie : `git diff src/ | wc -l` retourne 0
    - `npx vitest run` passe (regression)
  </acceptance_criteria>
  <done>AUDIT.md cree dans .planning/phases/14-audit-code/ avec les 4 sections (Securite, Performance, Dead Code, TypeScript), resume executif avec compteurs par severite, chaque finding formate fichier:ligne + severite + description + code + suggestion. Le rapport est assez detaille pour que Phase 16 puisse corriger sans re-analyser. Aucun fichier source modifie.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool output parsing | Les rapports ESLint/knip/tsc sont parses pour AUDIT.md — pas de risque d'injection |
| Script custom output | JSON parse du script custom — trusted local process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | AUDIT.md | accept | Le rapport est dans .planning/ (repo prive), pas expose publiquement. Les findings documentent des vulnerabilites mais c'est le but de l'audit |
| T-14-06 | Tampering | Source files | mitigate | L'action specifie explicitement NE PAS modifier de fichier source. Verification : `git diff src/` doit etre vide |
| T-14-07 | Repudiation | AUDIT.md accuracy | mitigate | Chaque finding reference l'outil source (ESLint/knip/script/revue manuelle) pour tracabilite |
</threat_model>

<verification>
1. AUDIT.md contient les 4 sections requises (Securite, Performance, Dead Code, TypeScript)
2. Le Resume Executif a des compteurs par severite (Critical/Warning/Info)
3. Chaque finding a le format fichier:ligne + severite + description + suggestion
4. Le rapport fait au moins 100 lignes (couverture exhaustive per D-11, D-13)
5. Aucun fichier source n'a ete modifie (per D-14)
6. Les 161 tests Vitest passent toujours (regression)
</verification>

<success_criteria>
- AUDIT.md est le livrable unique de la phase (D-06, D-07)
- Les 4 requirements sont couverts : AUDIT-01 (securite), AUDIT-02 (performance), AUDIT-03 (dead code), AUDIT-04 (bonnes pratiques)
- Chaque finding est navigable dans l'IDE via fichier:ligne (D-09)
- Les suggestions de correction sont assez detaillees pour Phase 16 (D-15)
- Tout est reporte sans seuil minimum (D-10)
- Zero fichier source modifie (D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/14-audit-code/14-02-SUMMARY.md`
</output>
