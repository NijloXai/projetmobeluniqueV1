---
phase: 16-tests-e2e-corrections-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - playwright.config.ts
  - e2e/auth.setup.ts
  - e2e/fixtures/axe.ts
  - .env.test.local
  - package.json
autonomous: true
requirements:
  - E2E-01

must_haves:
  truths:
    - "npx playwright test --list affiche les tests disponibles sans erreur"
    - "@playwright/test et @axe-core/playwright sont dans devDependencies de package.json"
    - "Le navigateur Chromium est installe pour Playwright"
    - "playwright.config.ts configure webServer, projects (setup + chromium), et storageState"
    - "e2e/auth.setup.ts effectue un login admin via UI et sauvegarde les cookies"
    - "e2e/fixtures/axe.ts exporte une fixture makeAxeBuilder pour audit WCAG"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Configuration Playwright avec webServer, projects dependencies, et storageState"
      contains: "defineConfig"
    - path: "e2e/auth.setup.ts"
      provides: "Setup project qui authentifie l'admin via /admin/login et sauvegarde storageState"
      contains: "storageState"
    - path: "e2e/fixtures/axe.ts"
      provides: "Fixture Playwright pour audit accessibilite WCAG via @axe-core/playwright"
      contains: "AxeBuilder"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/auth.setup.ts"
      via: "project setup testMatch"
      pattern: "auth\\.setup\\.ts"
    - from: "playwright.config.ts"
      to: "http://localhost:3000"
      via: "webServer command npm run dev"
      pattern: "webServer"
---

<objective>
Installer et configurer Playwright pour les tests E2E : packages, navigateur Chromium, configuration avec webServer auto, setup project pour auth admin via storageState, et fixture axe-core pour accessibilite.

Purpose: Fournir l'infrastructure E2E necessaire pour ecrire les tests de parcours (Plan 03). Le setup project authentifie l'admin une fois et partage les cookies pour tous les tests admin.
Output: Playwright installe et configure, auth setup fonctionnel, fixture axe prete, `npx playwright test --list` fonctionne.
</objective>

<execution_context>
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salah/Desktop/projetmobelunique/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-tests-e2e-corrections-audit/16-CONTEXT.md
@.planning/phases/16-tests-e2e-corrections-audit/16-RESEARCH.md
@.planning/phases/16-tests-e2e-corrections-audit/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer Playwright + configurer playwright.config.ts + auth setup + fixture axe (per D-06 a D-10, D-13)</name>
  <read_first>
    - package.json
    - .gitignore
    - .env.test.local (si existe)
    - src/app/admin/login/page.tsx (pour comprendre le formulaire login et les labels)
    - src/__tests__/integration/helpers/auth.ts (pattern auth existant Phase 15.1)
    - .planning/phases/16-tests-e2e-corrections-audit/16-RESEARCH.md (Patterns 1-4, Code Examples)
  </read_first>
  <files>
    playwright.config.ts,
    e2e/auth.setup.ts,
    e2e/fixtures/axe.ts,
    .env.test.local,
    package.json
  </files>
  <action>
**Installation des packages (per D-13):**

```bash
npm install -D @playwright/test @axe-core/playwright
npx playwright install chromium
```

**Ajouter script npm dans package.json :**

Dans la section `"scripts"` de package.json, ajouter :
```json
"test:e2e": "npx playwright test",
"test:e2e:ui": "npx playwright test --ui"
```

**Creer playwright.config.ts (per D-08, D-09, D-10, D-12):**

```typescript
import { defineConfig, devices } from '@playwright/test'
import path from 'path'
import { config } from 'dotenv'

config({ path: '.env.test.local' })

const STORAGE_STATE = path.join(__dirname, 'playwright/.auth/admin.json')

export default defineConfig({
  testDir: './e2e',
  timeout: 30_000,
  retries: process.env.CI ? 2 : 0,
  workers: 1,
  reporter: [['html', { open: 'never' }], ['list']],

  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },

  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },

  projects: [
    {
      name: 'setup',
      testMatch: /auth\.setup\.ts/,
    },
    {
      name: 'chromium',
      use: {
        ...devices['Desktop Chrome'],
        viewport: { width: 1280, height: 720 },
        storageState: STORAGE_STATE,
      },
      dependencies: ['setup'],
    },
  ],
})
```

Notes de configuration :
- `workers: 1` — Supabase local a des limites de connexion, eviter parallelisme (per D-06)
- `timeout: 120_000` pour webServer — Turbopack peut prendre > 60s au premier demarrage
- `reuseExistingServer: !process.env.CI` — en dev local, reutiliser le serveur deja lance
- `storageState: STORAGE_STATE` uniquement sur le projet chromium (pas sur setup)
- Pas de NANO_BANANA_API_KEY dans l'env webServer — le factory pattern bascule automatiquement sur Mock Sharp (per D-07)
- Chromium seul comme navigateur (per D-10)

**Creer e2e/auth.setup.ts (per D-09):**

```typescript
import { test as setup, expect } from '@playwright/test'
import path from 'path'

const authFile = path.join(__dirname, '../playwright/.auth/admin.json')

setup('authenticate admin', async ({ page }) => {
  await page.goto('/admin/login')

  await page.getByLabel('Email').fill(
    process.env.TEST_ADMIN_EMAIL ?? 'admin@test.mobelunique.fr'
  )
  await page.getByLabel('Mot de passe').fill(
    process.env.TEST_ADMIN_PASSWORD ?? 'test-admin-secure-2024!'
  )
  await page.getByRole('button', { name: /connexion/i }).click()

  // Attendre la redirection vers /admin (le layout admin protege)
  await page.waitForURL('**/admin/**', { timeout: 15_000 })

  // Sauvegarder les cookies Supabase JWT pour les tests admin
  await page.context().storageState({ path: authFile })
})
```

IMPORTANT : Lire le formulaire login (`src/app/admin/login/page.tsx`) pour confirmer les labels exacts des champs. Adapter `getByLabel('Email')` et `getByLabel('Mot de passe')` si les labels sont differents (ex: `getByLabel('Adresse email')` ou `getByPlaceholder('...')`). Si le formulaire n'a pas de labels accessibles, utiliser `getByRole('textbox')` ou `data-testid`.

**Creer e2e/fixtures/axe.ts (per D-13):**

```typescript
import { test as base } from '@playwright/test'
import AxeBuilder from '@axe-core/playwright'

type AxeFixture = {
  makeAxeBuilder: () => AxeBuilder
}

export const test = base.extend<AxeFixture>({
  makeAxeBuilder: async ({ page }, use) => {
    const makeAxeBuilder = () =>
      new AxeBuilder({ page })
        .withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa'])
    await use(makeAxeBuilder)
  },
})

export { expect } from '@playwright/test'
```

**Creer le repertoire playwright/.auth/ :**

```bash
mkdir -p playwright/.auth
touch playwright/.auth/.gitkeep
```

Le repertoire `.auth/` est deja dans `.gitignore` (ajoute dans Plan 01). Le `.gitkeep` assure que le repertoire existe dans git sans le contenu.

**Configurer .env.test.local (si n'existe pas deja):**

Verifier si `.env.test.local` existe. S'il existe (pour les tests d'integration Phase 15.1), y ajouter les variables E2E si manquantes. S'il n'existe pas, le creer :

```env
# Supabase CLI local (memes valeurs que Phase 15.1)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# Auth admin E2E
TEST_ADMIN_EMAIL=admin@test.mobelunique.fr
TEST_ADMIN_PASSWORD=test-admin-secure-2024!

# Pas de NANO_BANANA_API_KEY — utilise Mock Sharp (per D-07)
```

IMPORTANT : Lire le fichier `.env.test.local` existant et les helpers d'integration Phase 15.1 (`src/__tests__/integration/helpers/auth.ts`) pour recuperer les bonnes valeurs de `NEXT_PUBLIC_SUPABASE_URL` et `NEXT_PUBLIC_SUPABASE_ANON_KEY` qui fonctionnent avec Supabase CLI local. Ne pas inventer de valeurs — utiliser celles deja en place.
  </action>
  <verify>
    <automated>cd /Users/salah/Desktop/projetmobelunique && npx playwright test --list 2>&1 | head -10 && echo "PLAYWRIGHT LIST OK"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contient `@playwright/test` dans devDependencies
    - package.json contient `@axe-core/playwright` dans devDependencies
    - package.json contient `"test:e2e"` dans scripts
    - playwright.config.ts existe et contient `defineConfig`, `webServer`, `projects`, `testDir: './e2e'`
    - playwright.config.ts contient `workers: 1`
    - playwright.config.ts contient `storageState` dans le projet chromium
    - playwright.config.ts contient `timeout: 120_000` dans webServer
    - e2e/auth.setup.ts existe et contient `storageState` et `page.goto('/admin/login')`
    - e2e/fixtures/axe.ts existe et contient `AxeBuilder` et `withTags`
    - Le repertoire playwright/.auth/ existe avec un .gitkeep
    - .env.test.local contient `NEXT_PUBLIC_SUPABASE_URL` et `TEST_ADMIN_EMAIL`
    - npx playwright test --list retourne exit code 0 (liste les tests sans erreur)
  </acceptance_criteria>
  <done>Playwright installe avec Chromium, config complete (webServer, projects, storageState), auth setup via UI login, fixture axe-core prete, env vars configurees pour Supabase CLI local</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth.setup.ts -> /admin/login | Login avec credentials de test (env vars locales, pas de credentials en dur dans le code) |
| Playwright -> localhost:3000 | Tests locaux contre serveur dev, pas de risque reseau externe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Information Disclosure | .env.test.local credentials | mitigate | Fichier .env.test.local est dans .gitignore (pattern .env.*), pas commite |
| T-16-09 | Information Disclosure | playwright/.auth/admin.json | mitigate | Repertoire dans .gitignore, contient des JWT de test local uniquement |
| T-16-10 | Spoofing | auth.setup.ts credentials | accept | Credentials de test local uniquement (admin@test.mobelunique.fr), pas de credentials prod |
</threat_model>

<verification>
```bash
# Playwright installe
npx playwright --version

# Config valide
npx playwright test --list

# Fichiers presents
test -f playwright.config.ts
test -f e2e/auth.setup.ts
test -f e2e/fixtures/axe.ts
test -d playwright/.auth
```
</verification>

<success_criteria>
- npx playwright test --list retourne exit code 0
- @playwright/test et @axe-core/playwright dans devDependencies
- playwright.config.ts configure avec webServer, projects, storageState
- Auth setup cree et pret a executer au premier run
</success_criteria>

<output>
After completion, create `.planning/phases/16-tests-e2e-corrections-audit/16-02-SUMMARY.md`
</output>
